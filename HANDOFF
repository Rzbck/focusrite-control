FOCUSRITE CONTROL / COMPANION — UNIVERSAL AI RESUME ENTRYPOINT

Trigger word: HANDOFF

If the user says only "HANDOFF", do not guess from chat history. Resume from the repository.

MANDATORY STARTUP FRESHNESS GATE — do this before proposing code, hardware work, release work, branch changes, or asking the user to run anything:
1. identify the branch that actually owns the current objective;
2. fetch the remote branch and resolve its current HEAD;
3. inspect the latest relevant commits/diff since the last validated checkpoint recorded in the handoff;
4. read docs/CURRENT_HANDOFF.md from that current branch/ref;
5. inspect current code/tests and the newest available sanitized validation/hardware evidence;
6. reconcile any newer result explicitly pasted/validated by the human user;
7. only then decide the current state and next action.

Never assume the HEAD, branch, gate state, package state, or next step from an older chat summary. An embedded SHA in a handoff is a checkpoint, not permission to skip fetching the live branch. If the live repository or a newer completed user result contradicts the written handoff, use the newer validated evidence and update docs/CURRENT_HANDOFF.md immediately.

IMMUTABLE OBJECTIVE-CONTINUITY / NO-PREMATURE-CLOSURE RULE:
- Closing a sub-question never closes its parent validation objective.
- A green software gate, complete inventory, 0 FAIL, or a static feedback oracle PASS never means hardware validation is complete while material EVAL_ONLY, MANUAL_PENDING, BASELINE_UNKNOWN, neverObserved, unexercised, or otherwise open rows remain.
- Before changing objectives, re-open the parent matrix/checklist and explicitly account for all remaining open rows.
- A local conclusion such as "Mix B-F exact restore is unavailable" applies only to that sub-question and must never be extrapolated into "all feedback hardware testing is finished".
- Tooling/release/publication/documentation work may interrupt an active hardware objective only when it directly blocks the next safe validation step. Once the blocker is removed, immediately return to the parent hardware objective.
- Every objective change must state the parent objective, exact evidence that closes it, remaining open matrix rows, and why the next objective is permitted. If any of those are missing, the objective change is forbidden.
- For the current Scarlett 18i20 work, the parent objective remains explicit hardware feedback closure until all 31 public feedback definitions/instances have been classified and every remaining safe/actionable gap has either been tested or proven non-actionable/unsupported.

Read in this order after resolving live repository state:
1. AI_PROJECT_RULES.md
2. docs/CURRENT_HANDOFF.md
3. docs/BITFOCUS_SLACK_AND_RELEASE.md
4. latest relevant commits/changes, current code/tests, and newest sanitized validation/hardware results

Evidence priority:
1. newest explicit physical-hardware evidence or explicit human-validated test/result
2. newest completed software validation/gate evidence
3. current checked-in code/tests and latest relevant commits
4. docs/CURRENT_HANDOFF.md
5. older documentation/history

Do not treat unvalidated code, a pending experiment, a planned change, or an AI assumption as stronger evidence than completed evidence. Distinguish clearly between implemented, tested, hardware-tested, pending, research-only, and unsupported.

Final objective:
Develop, validate, document, and publish a safe Bitfocus Companion module using the local Focusrite Control Server protocol. Current supported hardware claim is ONLY Scarlett 18i20 (3rd Gen) until additional hardware is physically validated.

CURRENT PROJECT PHASE:
- canonical V8 FULL hardware campaign is complete; DO NOT rerun FULL blindly;
- V8 feedback inventory covered 31 definitions / 829 instances but dynamic closure is incomplete: 190 static/oracle PASS, 639 EVAL_ONLY, and the dynamic tracker recorded 20 both-state, 12 single-state, 710 never-observed instances;
- later meter-specific evidence is stronger for meter dynamics: 14/46 meter paths closed (inputs 8/8, outputs 4/26, mixes 2/12), mismatch 0;
- Mix A L/R meter paths retain exact-baseline closure;
- Mix B-F meter baselines remain unknown/non-actionable; direct read-only research confirmed a fresh Control Server subscription also withholds required baseline fields;
- that Mix B-F sub-question is closed, but the parent feedback-hardware-closure objective is NOT closed;
- production candidate remains exact audited v0.1.16 in Companion;
- final software audit checkpoint fba6d977a59b6381ae11c736a68fc809afb55840 is green: 192/192 Node tests PASS and package build PASS; that run performed no hardware feedback test;
- normal validation/control must use the existing approved Companion Scarlett 18i20 connection as the canonical Focusrite client;
- do not create another direct Control Server client merely to inspect state Companion can already expose;
- a future direct research client is exceptional: tell the user BEFORE launch that it creates a separate Remote Devices entry and obtain explicit agreement;
- never reuse the Companion private client key from another process to avoid duplicate-identity/session collisions;
- official Bitfocus repository/name is still pending maintainer decision, but publication waiting is NOT the current parent objective while feedback closure remains open.

Current validation path:
1. build and maintain the explicit 31-definition feedback closure matrix from V8 + newer meter evidence + current source/safety rules;
2. separate HARDWARE_DYNAMIC_CLOSED, HARDWARE_STATIC_CONFIRMED, EVAL_ONLY_SAFE_ACTIONABLE, EVAL_ONLY_NONACTIONABLE, READ_ONLY_STATUS, and UNSUPPORTED/BLOCKED;
3. do not rerun FULL just to improve counts;
4. create only targeted Companion-path hardware tests for the remaining safe/actionable rows, with runtime exact-baseline checks, physical isolation where required, immediate server-confirmed restoration, and hard abort on restore failure;
5. preserve exact audited 0.1.16 in Companion; do not install debug/TestBench/audit packages over it;
6. after feedback hardware closure is genuinely complete, resume release/publication work and then wait/follow the official Bitfocus repository/naming workflow.

Living-state rule:
After every material event (software gate, hardware run, human-validated result, newly proven/rejected behavior, blocker, branch/objective change, publication decision), update docs/CURRENT_HANDOFF.md before handing the project to another AI. Record what was actually validated, exact branch/checkpoint where useful, whether hardware writes occurred, what remains pending, and the immediate next step. Never leave a stale green state after a later failure.

User convention across repositories:
Use a root file named HANDOFF with the same purpose in every project. Then the user can say only "HANDOFF" to tell an AI to locate the project resume entrypoint, fetch/reconcile the live repository state, read the canonical current rules/state, and continue from evidence instead of reconstructing the project from old chats.
