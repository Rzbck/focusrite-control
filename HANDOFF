FOCUSRITE CONTROL / COMPANION — UNIVERSAL AI RESUME ENTRYPOINT

Trigger word: HANDOFF

MANDATORY STARTUP FRESHNESS GATE — REPO-WIDE RECENCY FIRST
When the user says only `HANDOFF`, do not resume from chat history, uploaded files, an embedded SHA, a copied handoff, or the default branch. Resume from the LIVE repository.

Before proposing code, hardware work, release work, branch changes, or asking the user to run anything:
1. Establish the current date/time.
2. Inspect recent REMOTE branch movement across the repository, not only `main` and not only default-branch commit search results.
3. Identify the newest MATERIAL movements by commit time.
4. Choose the objective-owning branch using BOTH recency and relevance.
5. Resolve that branch's current remote HEAD and inspect newer commits/diff since the last validated checkpoint; inspect divergence/merge-base if needed.
6. Read this root `HANDOFF`, `AI_PROJECT_RULES.md`, and `docs/CURRENT_HANDOFF.md` from that LIVE ref.
7. Inspect current affected code/tests and newest sanitized validation evidence.
8. Reconcile any newer completed user/hardware result; a newer completed physical/human result outranks stale written state.
9. Only then state where the project is and choose the next action.

A default-branch commit search can miss work on another branch. An embedded SHA or document timestamp is context only, never permission to skip live Git verification.

OPERATOR WORKFLOW PRIORITY — PROJECT LAUNCHERS FIRST
This is a permanent usability rule.
- Prefer `UPDATE.bat` for normal branch update/sync.
- Prefer `UPDATE_AND_RUN.bat` for update + normal software validation.
- Prefer `RUN.bat` when the checkout is already current and the normal software gate is required.
- Prefer the exact `testbench\RUN_*.cmd` launcher for targeted TestBench/hardware work.
- Do NOT make the user type ad-hoc PowerShell, raw Git commands, Node commands, or one-off shell snippets when an existing project launcher already performs the operation.
- Manual shell/Git/PowerShell commands are LAST RESORT only when the launcher itself is blocked/broken or cannot expose the required information. Explain why, use the smallest recovery command possible, then return immediately to the launcher workflow.
- Do not rebuild a second tool/workflow for behavior already present in the repository. Inspect and reuse the established launcher/import/test path first.
- Keep linked-worktree handling conservative. Updating the current branch is normal. If a different selected branch is already active in another worktree, report the owning worktree and stop safely; do not auto-jump directories or attach the same branch twice.

IMMUTABLE OBJECTIVE-CONTINUITY / NO-PREMATURE-CLOSURE RULE
- Closing a sub-question never closes its parent validation objective.
- A green software gate, complete inventory, 0 FAIL, or static feedback oracle PASS never means hardware validation is complete while material EVAL_ONLY, MANUAL_PENDING, BASELINE_UNKNOWN, neverObserved, unexercised, or otherwise open rows remain.
- Before changing objectives, re-open the parent matrix/checklist and explicitly account for all remaining open matrix rows.
- A local conclusion such as `Mix B-F exact restore is unavailable` applies only to that sub-question and must never be extrapolated into `all feedback hardware testing is finished`.
- Tooling/release/publication/documentation work may interrupt an active hardware objective only when it is a direct blocker for the next safe validation step. Once the blocker is removed, immediately return to the parent hardware objective.
- Every objective change must state the parent objective, exact evidence that closes it, remaining open matrix rows, and why the next objective is permitted. If any are missing, the objective change is forbidden.
- For the current Scarlett 18i20 work, the parent objective remains explicit hardware feedback closure until all 31 public feedback definitions/instances have been classified and every remaining safe/actionable gap has either been tested or proven non-actionable/unsupported.

CURRENT LIVE OBJECTIVE
Branch: `testbench/meter-routing-exact-restore`
Parent objective: explicit hardware feedback closure across all 31 public feedback definitions/instances until every remaining safe/actionable gap is tested or proven non-actionable/unsupported.
Production candidate in Companion: exact audited 0.1.16.
Do not rerun FULL blindly and do not install a TestBench/debug package over 0.1.16.

LATEST COMPLETED USER TEST RESULT — 2026-08-24
Launcher: `testbench\RUN_MIX_FEEDBACK_CLOSURE.cmd`
User checkout during this completed run: `e9c4a528315b`.
Observed:
- targeted software self-check: **32/34 PASS, 2 FAIL**;
- failure 1: root HANDOFF had lost the explicit immutable objective-continuity phrases required by the existing regression contract;
- failure 2: `test/mix-feedback-closure.test.js` searched for the file-name mention `MixFeedbackPreparationCheck.js` in the syntax self-check instead of the executed `call :RUN_PREP_CHECK` guard, so its ordering assertion became stale after PAGE2_AUTO integration;
- launcher stopped during `[0/3]` exactly as intended;
- read-only Remote Devices preflight was NOT launched in this run;
- PAGE2_AUTO was NOT launched in this run;
- Focusrite hardware writes: 0;
- Companion Page 2 mutations: 0;
- hardware restore required: NO.

Source-side corrections prepared after this user result:
- restore the full immutable objective-continuity block in root HANDOFF instead of weakening the regression;
- restore the repo-wide freshness wording already required by `test/update-and-run-context.test.js`;
- update the Mix launcher ordering test to target executed `call :RUN_PREFLIGHT` then executed `call :RUN_PREP_CHECK`;
- do not change the Mix hardware engine, fail-safe runner, or launcher execution order for these software-test failures.
These corrections remain pending the user's local launcher self-check; do not call them software-tested PASS yet.

LATEST UPDATE-LAUNCHER FAILURE — 2026-08-24
A subsequent normal `UPDATE.bat` run exposed a separate updater defect before the user could receive those test fixes:
- displayed repository path was malformed as `E:_Project\focusrite-control` rather than an absolute canonical path;
- local HEAD and remote HEAD displayed `UNKNOWN`;
- automatic linked-worktree routing selected the same malformed path;
- dirty detection reported `M UPDATE.bat`;
- safety stash was created but `UPDATE.bat` remained modified after the stash, so updater aborted with no merge/reset.

Diagnosis and source correction:
- the tracked `UPDATE.bat` blob had been written with CRLF into Git while `.gitattributes` requires Windows launchers to be canonically LF in Git and checked out CRLF; that can recreate a self-dirty launcher immediately after checkout/stash;
- the automatic linked-worktree directory jump added unnecessary path manipulation and is removed;
- current `UPDATE.bat` is republished as a canonical LF Git blob;
- it resolves the actual repository root with `git rev-parse --show-toplevel`, then reports real local/remote/final HEAD values;
- stale-index refresh, safety stash, explicit selected-branch fetch, and `git pull --ff-only` remain;
- if a different selected branch is owned by another worktree, updater reports `Worktree proprietaire` and stops safely instead of auto-jumping;
- `test/update-branch-fetch.test.js` already enforces canonical LF blobs for tracked Windows launchers;
- `test/update-and-run-context.test.js` now locks canonical-root resolution and conservative linked-worktree behavior.

This updater correction is implemented/source-reviewed but not yet user-local validated. Do not call it PASS yet.

LAST COMPLETED READ-ONLY PAGE 2 CLASSIFICATION
Earlier completed launcher run at `804d977809ff` observed:
- targeted software self-check: 20/20 PASS;
- Remote Devices/model/connection preflight: PASS;
- exact model Scarlett 18i20 (3rd Gen);
- existing canonical `Companion Scarlett 18i20` client authorised;
- r9 page audit: 42 SAFE setters + 829 feedback probes + 31 definitions;
- module version 0.1.16;
- live shape: 8 inputs / 26 outputs / 24 mixer slots / 12 lanes;
- evidence coverage: 1436/1436 inventory, snapshot 1340/1340, core 21/21, feedback 829/31;
- output availability: AVAILABLE=22, UNKNOWN=4;
- Page 2 identity: `STALE_FOCUSRITE_TESTBENCH_HARNESS`;
- Page 2 controls: 769;
- `replacement-candidate=YES`;
- Focusrite hardware writes: 0;
- Companion Page 2 mutations: 0;
- hardware restore required: NO.

Interpretation: Page 2 is a recognized older Focusrite TestBench capability-lab harness, not arbitrary user content. The read-only classification sub-step is complete.

EXISTING PAGE 2 WORKFLOW — REUSE IT
`FullTestBenchCompanionImportV7.js` already implements the V8 `PAGE2_AUTO` path used historically by `RUN_SAFE_HARDWARE_TESTS.cmd`: it imports the generated current capability-lab page through Companion, replaces only Page 2, keeps Page 1 r9 intact, remaps to the existing Focusrite connection, refuses connection recreation, reaudits Page 2/other pages/connections, and sends no Focusrite hardware write. The targeted Mix launcher is wired to reuse this existing path only for the recognized stale TestBench classification. User/other/unverified pages remain blocked from auto replacement.

PERMANENT SAFETY
- Supported hardware claim only Scarlett 18i20 (3rd Gen).
- Monitor gain item 1677 read-only; never re-add Monitor set/adjust/raw write.
- No invented input preamp gain, direct per-input hardware mute, per-channel phantom switching, Mic Kill, or physical Monitor level write.
- Dynamic Focusrite Control Server TCP port and device ID only.
- Writes require authorization of this module's own server-assigned client ID.
- Feedback/state only from server-confirmed state; no optimistic success.
- No unknown/unsafe raw writes, firmware/reset/restore/snapshot commands, or writes to read-only/status/meter items.
- No write to explicit UNKNOWN output availability.
- Reuse the existing approved `Companion Scarlett 18i20` connection; do not create a second direct client merely to inspect state Companion already exposes.
- Do not update Focusrite software, firmware, routing or hardware settings outside an explicitly agreed test.

RETAINED HARDWARE EVIDENCE
- Feedback parent matrix: 31 definitions / 829 instances; static/oracle 190 PASS / 639 EVAL_ONLY / 0 FAIL; dynamic tracker 20 both-state / 12 single-state / 710 neverObserved / 0 FAIL.
- Meter closure: 14/46 paths closed; inputs 8/8, outputs 4/26, mixes 2/12, mismatch 0; hardware restore YES; Page 2 restore YES.
- Mix A L/R meter closure remains valid.
- Mix B-F meter baseline sub-question remains nonactionable because exact Playback-strip baselines are unavailable.
- Targeted Core feedback: 18/18 SKIP_BASELINE_UNKNOWN, zero writes, zero FAIL, zero restore quarantine; currently nonactionable in this bootstrap state.

EXACT IMMEDIATE NEXT STEP
1. Perform the live freshness gate.
2. Because the user's currently installed updater is itself the broken version and cannot replace itself cleanly, one final minimal bootstrap is allowed to fetch the live objective branch, restore only `UPDATE.bat` from that remote ref, and fast-forward. This is LAST RESORT because the launcher itself is the blocker.
3. Immediately return to normal `UPDATE.bat`. Its output must show a canonical repository path and real local/remote/final HEAD values.
4. Once updater validation succeeds, run `testbench\RUN_MIX_FEEDBACK_CLOSURE.cmd` again.
5. `[0/3]` must pass completely before any preflight or hardware work.
6. If the recognized stale Page 2 classification recurs, use the existing `PAGE2_AUTO` prompt; then allow the launcher to redo read-only preflight + Page 2 audit.
7. Proceed to `MIX_FEEDBACK` / `ALL_ISOLATED` only under the launcher's normal safety conditions, then capture the complete targeted hardware result.
8. Do not rerun FULL/Core/SAFE/broad meter routing/direct probes/package install as a substitute.

Living-state rule: after every material user/software/hardware result or blocker, update both this root `HANDOFF` and `docs/CURRENT_HANDOFF.md` before handoff. Record actual validation level and do not claim pending work passed.
