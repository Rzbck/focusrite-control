FOCUSRITE CONTROL / COMPANION — UNIVERSAL AI RESUME ENTRYPOINT

Trigger word: HANDOFF

MANDATORY STARTUP FRESHNESS GATE — REPO-WIDE RECENCY FIRST
When the user says only `HANDOFF`, do not resume from chat history, uploaded files, an embedded SHA, a copied handoff, or the default branch. Resume from the LIVE repository.

Before proposing code, hardware work, release work, branch changes, or asking the user to run anything:
1. Establish current date/time.
2. Inspect recent REMOTE branch movement across the repository, not only `main`.
3. Identify the newest MATERIAL movements by commit time.
4. Choose the objective-owning branch using BOTH recency and relevance.
5. Resolve that branch's current remote HEAD and inspect newer commits/diff since the last validated checkpoint.
6. Read live `HANDOFF`, `AI_PROJECT_RULES.md`, `docs/CURRENT_HANDOFF.md`, `docs/PROTOCOL.md`, `docs/STATE_CONTRACT.md`, and `docs/COLD_START_READBACK.md`.
7. Reconcile any newer completed user/hardware result; a newer completed physical/human result outranks stale written state.
8. Only then choose the next action.

A default-branch commit search can miss work on another branch. An embedded SHA is context only and never permission to skip live Git verification.

MANDATORY EVIDENCE / INFERENCE GATE — NEVER TURN UNKNOWN INTO UNSUPPORTED
Keep these levels separate:
1. OFFICIAL PRODUCT BEHAVIOUR;
2. SCHEMA_PRESENT;
3. SESSION_STATE_OBSERVED;
4. IMPLEMENTED;
5. HARDWARE_WRITE_CONFIRMED;
6. HARDWARE_DYNAMIC_CLOSED.

`UNKNOWN`, blank variable, missing cache entry, `BASELINE_UNKNOWN`, `SKIP_BASELINE_UNKNOWN`, or sparse initial state means only not observed in this client session unless stronger evidence proves otherwise. It is NOT evidence that the schema item is absent, false, unsupported, or impossible.

If older physical/session evidence contradicts current cache coverage, keep the question READBACK/MATERIALISATION RESEARCH OPEN until reconciled.

A reversible hardware test must require only state genuinely necessary for exact restoration of the property being changed. Do not impose unrelated prerequisite tuples merely because an older harness grouped them.

OPERATOR WORKFLOW PRIORITY — PROJECT LAUNCHERS FIRST
- `UPDATE.bat` for normal sync.
- `UPDATE_AND_RUN.bat` for update + normal software gate.
- `RUN.bat` when already current.
- exact `testbench\RUN_*.cmd` launcher for targeted TestBench/hardware work.
- Manual shell/Git/PowerShell is last resort only when the launcher itself is broken or cannot expose the required diagnostic.
- Do not build a second workflow for behavior already present.

IMMUTABLE OBJECTIVE-CONTINUITY / NO-PREMATURE-CLOSURE RULE
- Closing a sub-question never closes its parent validation objective.
- A green software gate, 0 FAIL, inventory completeness, or static oracle PASS does not close the parent hardware-validation objective while material EVAL_ONLY, MANUAL_PENDING, BASELINE_UNKNOWN, neverObserved, unexercised, or otherwise open rows remain.
- Before changing objectives, explicitly account for all remaining open matrix rows.
- objective change is forbidden while relevant remaining open matrix rows exist, unless the user explicitly changes the project objective.
- Tooling/release/documentation work may interrupt hardware validation only as a direct blocker for the next safe validation step; once removed, return to the parent hardware objective.
- Parent objective remains explicit hardware feedback closure for all 31 public feedback definitions/instances.

CURRENT LIVE OBJECTIVE
Branch: `testbench/meter-routing-exact-restore`.
Parent objective: explicit hardware feedback closure.
Canonical production candidate remains audited 0.1.16.
Research/readback build **0.1.17 is software-validated, packaged, loaded on the existing authorised Companion connection, and observed against the real Scarlett 18i20 (3rd Gen)**.
Do not rerun FULL blindly.

SOFTWARE GATE — COMPLETE PASS
User-host source HEAD `515e9cf2f3e9`:
- immutable dependencies PASS;
- Prettier PASS;
- ESLint PASS;
- source manifest PASS;
- Node tests **216/216 PASS / 0 FAIL**;
- package build PASS: `focusrite-scarlett-18i20-0.1.17.tgz`.

The later remote commits after this package are TestBench/docs-only; they do not alter the validated 0.1.17 module package.

LATEST REAL 0.1.17 READ-ONLY PROVENANCE RESULT — 2026-08-24
User loaded/selects 0.1.17 on the existing Companion Focusrite connection. Preflight confirmed:
- exact Scarlett 18i20 (3rd Gen);
- module 0.1.17;
- existing module client authorised;
- live shape 8 inputs / 26 outputs / 24 mixer slots / 12 lanes;
- evidence coverage 1436/1436;
- Playback source slot 3 / Playback 1 / stereo.

The read-only Mix provenance probe was completed twice with no Focusrite write, no Companion button press and no routing mutation. Both runs produced the same stable Playback-slot pattern:
- Mix A-F left: gain KNOWN `[set]`;
- Mix A-F right: gain UNKNOWN `[never-observed]`;
- all 12 mute states UNKNOWN `[never-observed]`;
- all 12 solo states UNKNOWN `[never-observed]`;
- exact tuple 0/12.

The 30-second UI navigation phase caused zero additional materialisation. Therefore ordinary Output Routing selection/navigation is now tested as **not sufficient** to materialise the missing Mix mute/solo state in this session.

USER UI EVIDENCE — IMPORTANT CORRECTION
User screenshots from the same session show Focusrite Control Output Routing is organised by physical output destination, not by visible tabs called Mix A-F.

Observed visible routing:
- Monitor Outputs 1-2: Custom Mix;
- Line Outputs 3-4: Playback 3-4;
- Line Outputs 5-6: Playback 5-6;
- Line Outputs 7-8: Playback 7-8;
- Line Outputs 9-10: Playback 9-10;
- S/PDIF Outputs 1-2: Playback 11-12.

Official Focusrite documentation confirms a Custom Mix is assigned to a chosen output and that assigning Custom Mix enables its mixer.

Do NOT infer a fixed protocol mapping such as `Mix A = Monitor 1-2` yet. The schema contains 12 mono lanes / six stereo Mix A-F, but the exact runtime output↔Mix assignment remains RESEARCH_OPEN.

Current parser already sees per-output `assignMix` and `assignTalkbackMix` IDs, but these remain research-only because argument/value semantics are unvalidated. Do not expose or write them merely to discover the mapping.

ADDITIONAL UI CROSS-CHECK — UI_OBSERVED / NOT NEW HARDWARE CLOSURE
Additional user screenshots from the same session show:
- output source choices `Playback (DAW)`, `Hardware Input`, `Custom Mix`, `Custom Mix + Talkback`;
- Input Settings Line/Instrument selector only on Analogue 1-2;
- Air and Pad controls visible for Analogue 1-8;
- Device Settings Speaker Switching Enable/Disable;
- Monitor Controls choices `1-2`, `1-4`, `1-6`, `1-8`, `All`, `None`;
- Talkback source and level controls;
- `Retain 48V` persistence setting;
- the existing Companion Scarlett 18i20 Remote Devices client visibly approved.

Official Focusrite docs corroborate: INST is only for inputs 1-2; Air/Pad cover all eight analogue channels; `Custom Mix + Talkback` adds Talkback to the selected output Custom Mix; Monitor Controls can target analogue output groups.

These screenshots are private evidence and must not be committed. They do not prove TCP value semantics and do not create new public controls. In particular:
- do not invent input preamp gain, direct per-input mute, or per-channel phantom switching;
- `Retain 48V` remains persistence only;
- `monitor_preset`, `input_mode`, `talkback_source`, and `phantom_persistence` are already dynamically closed and should not be retested;
- Focusrite warns Monitor Controls reassignment can make affected output level jump to full scale, so never use that selector merely to materialise readback;
- Speaker Switching/ALT can alter physical monitor routing, so any later `monitor_alt_enable` / `monitor_alt` test requires explicit isolation and known restore state.

NEXT DIRECT BLOCKER — READ-ONLY OUTPUT→MIX MAPPING
Use the existing 0.1.17 variables instead of raw assignment values. `output_N_source_name` already resolves a raw output source ID to a human-readable Playback or Mix name using current parsed schema state.

`testbench/MeterMixPlaybackBaselineReadOnlyProbe.js` has been extended on the branch to print a sanitized `OUTPUT ROUTING SNAPSHOT` from existing `output_N_name`, `output_N_source_name`, and `output_N_stereo` variables. This is TestBench-only: no module version bump, no new package, no new client, no write path.

The user has NOT yet synchronized this TestBench-only extension.
Immediate action remains:
1. if an old probe prompt is open, type `DONE`; do not run another navigation cycle;
2. run `UPDATE.bat` only;
3. do not rerun the 216-test gate and do not reimport 0.1.17;
4. rerun `testbench\RUN_METER_MIX_BASELINE_READONLY.cmd`;
5. copy the `OUTPUT ROUTING SNAPSHOT` block;
6. at the prompt, type `DONE`.

Use that result to map the active Custom Mix destination to protocol Mix A-F. Only after the mapping is server-confirmed should a single property-specific manually guided Mute materialisation experiment be designed. Solo follows separately. Do not write routing or `assign-mix` to manufacture evidence.

TEST ORDER AFTER THE READ-ONLY MAPPING — DO NOT SKIP AHEAD
1. **Mix Mute materialisation** — one mapped active Custom Mix, one known Playback strip, manual Focusrite-Control Mute cycle only under physical isolation; observe server state first/after, restore exactly. No Companion write yet.
2. **Mix Mute action/feedback closure** — only if the Mute baseline itself becomes server-confirmed; redesign harness to require Mute baseline only, while observing collateral Gain/Solo state without making them prerequisites.
3. **Mix Solo** — repeat independently only after Mute semantics are understood.
4. **Input Air/Pad readback gaps** — later property-specific manual materialisation on an isolated/non-live input, then targeted Companion closure only from server-confirmed baseline. Air/Pad remain eight-channel product controls; do not infer gain/mute/phantom features.
5. **Monitor Mute/Dim readback gaps** — later only with speakers/headphones physically safe and exact baseline available.
6. **Monitor ALT/ALT-enable** — last among Core monitoring controls because Speaker Switching changes physical output routing. `monitor_preset` is already closed; do not touch Monitor Controls scope merely for coverage.
7. Remaining output/mix meter gaps use passive/natural signal or already-proven exact-restore routing; no score-driven routing changes.

MIX MUTE/SOLO STATUS
- `mix_mute`: RESEARCH_OPEN / EVAL_ONLY;
- `mix_solo`: RESEARCH_OPEN / EVAL_ONLY.

The old 0/12 full-tuple campaign must not be rerun unchanged. It proved a session readback gap, not unsupported hardware.

RETAINED PARENT EVIDENCE
- 31 definitions / 829 instances;
- static/oracle 190 PASS / 639 EVAL_ONLY / 0 FAIL;
- dynamic 20 both-state / 12 single-state / 710 neverObserved / 0 FAIL;
- meter closure 14/46: inputs 8/8, outputs 4/26, mixes 2/12, mismatch 0;
- targeted Core 18/18 SKIP_BASELINE_UNKNOWN, zero writes/FAIL/restore quarantine — readback evidence, not capability absence.

PERMANENT SAFETY
- Supported hardware claim only Scarlett 18i20 (3rd Gen).
- Monitor gain item 1677 read-only.
- No invented input preamp gain, direct per-input hardware mute, per-channel phantom switching, Mic Kill, or physical Monitor level write.
- Dynamic Focusrite Control Server TCP port and device ID only.
- Writes require authorization of this module's own server-assigned client ID.
- Feedback/state only from server-confirmed state; no optimistic success.
- No unknown/unsafe raw writes, firmware/reset/restore/snapshot commands, or writes to read-only/status/meter items.
- No write to explicit UNKNOWN output availability.
- Reuse the existing approved Companion connection; no second direct client by default; never copy/reuse its private client key.
- Do not update Focusrite software, firmware, routing or hardware settings outside an explicitly agreed test.

Living-state rule: after every material user/software/hardware result or blocker, update both root `HANDOFF` and `docs/CURRENT_HANDOFF.md`. Record actual validation level and do not claim pending work passed.
