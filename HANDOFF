FOCUSRITE CONTROL / COMPANION — UNIVERSAL AI RESUME ENTRYPOINT

Trigger word: HANDOFF

If the user says only `HANDOFF`, do not guess from chat history, uploaded files, an embedded SHA, a copied handoff, or the default branch. Resume from the LIVE repository.

MANDATORY STARTUP FRESHNESS GATE — REPO-WIDE RECENCY FIRST
Do this before proposing code, hardware work, release work, branch changes, or asking the user to run anything.

1. Establish the current date/time.
2. Inspect recent REMOTE branch movement across the repository, not only `main` and not only default-branch commit search results.
3. Identify the newest MATERIAL movements by commit time.
4. Choose the objective-owning branch using BOTH recency and relevance.
5. Resolve that remote branch's current HEAD and inspect relevant commits/diff since the last validated checkpoint. If the checkpoint is not an ancestor, inspect divergence/merge-base.
6. Read this root `HANDOFF`, `AI_PROJECT_RULES.md`, and `docs/CURRENT_HANDOFF.md` from that LIVE ref.
7. Inspect current affected code/tests and newest sanitized validation evidence.
8. Reconcile any newer completed physical/human result from the user.
9. Only then state where the project is and choose the next action.

A repository-wide/default-branch commit search can miss work on another branch. An embedded SHA or document timestamp is context only, never permission to skip live Git verification.

OPERATOR WORKFLOW PRIORITY — PROJECT LAUNCHERS FIRST
This is a permanent usability rule for future conversations.

- Prefer the repository's normal Windows launchers and menus for user instructions: `UPDATE.bat`, `UPDATE_AND_RUN.bat`, `RUN.bat`, and the appropriate `testbench\\RUN_*.cmd` launcher.
- If the task is only to update the current branch, prefer `UPDATE.bat` rather than giving the user raw `git fetch` / `git merge` commands.
- If the task is update + normal project validation, prefer `UPDATE_AND_RUN.bat`.
- If the checkout is already current and only validation is needed, prefer `RUN.bat` or the exact targeted TestBench launcher.
- For hardware validation, use the dedicated `testbench\\RUN_*.cmd` workflow that already contains safety prompts, preflight, logging and restore handling.
- Do NOT make the user type ad-hoc PowerShell, raw Git commands, Node commands, or one-off shell snippets when an existing project launcher already performs the operation.
- Manual shell/Git/PowerShell commands are LAST RESORT for diagnosis or recovery when the launcher itself is blocked, broken, or cannot expose the required information. Explain why the launcher cannot be used before giving such commands.
- After repairing a launcher failure, return immediately to the normal launcher workflow instead of leaving the user on manual commands.
- Do not rebuild a second tool/workflow for behavior already present in the repository. Inspect and reuse the established launcher/import/test path first.

Evidence priority after freshness resolution:
1. newest completed physical/human hardware result;
2. newest completed software gate/result;
3. current code/tests and relevant newer commits on the live objective branch;
4. live `docs/CURRENT_HANDOFF.md`;
5. older docs/uploads/captures/chat summaries.

Always distinguish **hardware-tested**, **software-tested**, **implemented**, **schema-observed**, **research-only**, **pending**, and **unsupported**.

IMMUTABLE OBJECTIVE CONTINUITY
- Closing a sub-question never closes its parent validation objective.
- Green software, complete inventory, 0 FAIL, or static oracle PASS never means hardware validation is complete while material `EVAL_ONLY`, `MANUAL_PENDING`, `BASELINE_UNKNOWN`, `neverObserved`, unexercised, or otherwise open rows remain.
- Tooling/release/publication/documentation work may interrupt an active hardware objective only when it directly blocks the next safe validation step. Once fixed, return immediately to the parent hardware objective.
- For the current Scarlett 18i20 work, the parent objective remains explicit hardware feedback closure until all 31 public feedback definitions/instances are classified and every remaining safe/actionable gap has been tested or proven non-actionable/unsupported.

CURRENT LIVE OBJECTIVE
Branch: `testbench/meter-routing-exact-restore`
Parent objective: explicit hardware feedback closure.
Production candidate in Companion: exact audited 0.1.16.
Do not rerun FULL blindly and do not install a TestBench/debug package over 0.1.16.

LATEST COMPLETED USER RESULT — 2026-08-24
Launcher: `testbench\\RUN_MIX_FEEDBACK_CLOSURE.cmd`
User checkout during this completed run: `804d977809ff`.

Observed result:
- targeted software self-check: **20/20 PASS**;
- Remote Devices/model/connection preflight: PASS;
- exact model: Scarlett 18i20 (3rd Gen);
- canonical `Companion Scarlett 18i20` client authorised;
- r9 page audit: 42 SAFE setters + 829 feedback probes + 31 definitions;
- module version: 0.1.16;
- live shape: 8 inputs / 26 outputs / 24 mixer slots / 12 lanes;
- evidence coverage: 1436/1436 inventory, snapshot 1340/1340, core 21/21, feedback 829/31;
- output availability: AVAILABLE=22, UNKNOWN=4;
- Companion Page 2 identity: `STALE_FOCUSRITE_TESTBENCH_HARNESS`;
- Page 2 controls: 769;
- `replacement-candidate=YES`;
- Focusrite hardware writes: 0;
- Companion Page 2 mutations: 0;
- hardware restore required: NO.

Interpretation:
Page 2 is NOT arbitrary user content. It is a recognized older Focusrite TestBench capability-lab harness. The read-only classification phase is complete.

EXISTING PAGE 2 WORKFLOW — REUSE IT, DO NOT REBUILD IT
The repository already contains the established V8 Page 2 auto-preparation path:
- `FullTestBenchCompanionImportV7.js` generates/imports the current capability-lab page through Companion's local import path;
- it replaces only Page 2;
- keeps Page 1 r9 intact;
- remaps to the existing Focusrite Companion connection;
- refuses connection recreation;
- reaudits Page 2, other pages and connection preservation;
- presses no Focusrite button and sends no Focusrite hardware write;
- `RUN_SAFE_HARDWARE_TESTS.cmd` already used this as the historical `PAGE2_AUTO` flow.

The targeted Mix launcher has now been wired to REUSE this existing `PAGE2_AUTO` path when the read-only checker returns the recognized stale-harness state. A page classified as user/other/unverified remains blocked from automatic replacement.

Current prepared branch state after the user's 20/20 run:
- `MixFeedbackPreparationCheck.js` distinguishes generic PREP_REQUIRED from recognized stale TestBench harness eligible for existing `PAGE2_AUTO`;
- `RUN_MIX_FEEDBACK_CLOSURE.cmd` offers the existing `PAGE2_AUTO` path for that recognized stale harness, then reruns read-only preflight + Page 2 verification before any hardware confirmation;
- no new parallel Page 2 tool was added;
- no production `src/` behavior was changed by this targeted preparation work.

PERMANENT SAFETY
- Supported hardware claim only Scarlett 18i20 (3rd Gen).
- Monitor gain item 1677 read-only; never re-add Monitor set/adjust/raw write.
- No invented input preamp gain, direct per-input hardware mute, per-channel phantom switching, Mic Kill, or physical Monitor level write.
- Dynamic Focusrite Control Server TCP port and device ID only.
- Writes require authorization of this module's own server-assigned client ID.
- Feedback/state only from server-confirmed state; no optimistic success.
- No unknown/unsafe raw writes, firmware/reset/restore/snapshot commands, or writes to read-only/status/meter items.
- No write to explicit UNKNOWN output availability.
- Reuse the existing approved `Companion Scarlett 18i20` connection; do not create a second direct client merely to inspect state Companion already exposes.
- Do not update Focusrite software, firmware, routing or hardware settings outside an explicitly agreed test.

RETAINED HARDWARE EVIDENCE
- Meter closure: 14/46 paths closed; inputs 8/8, outputs 4/26, mixes 2/12, mismatch 0; hardware restore YES; Page 2 restore YES.
- Mix A L/R meter closure remains valid.
- Mix B-F meter baseline sub-question remains nonactionable because exact Playback-strip baselines are unavailable.
- Targeted Core feedback: 18/18 SKIP_BASELINE_UNKNOWN, zero writes, zero FAIL, zero restore quarantine; currently nonactionable in this bootstrap state.
- Feedback parent matrix: 31 definitions / 829 instances; static/oracle 190 PASS / 639 EVAL_ONLY / 0 FAIL; dynamic tracker 20 both-state / 12 single-state / 710 neverObserved / 0 FAIL.

EXACT IMMEDIATE NEXT STEP
1. Perform the live freshness gate.
2. Use the normal launcher workflow, not raw shell commands.
3. On the user's Windows checkout, use `UPDATE.bat` to update `testbench/meter-routing-exact-restore` to the current remote branch.
4. Then run `testbench\\RUN_MIX_FEEDBACK_CLOSURE.cmd`.
5. The targeted self-check and read-only preflight run first.
6. When the recognized stale Page 2 state is detected, the launcher should offer `PAGE2_AUTO` using the EXISTING V8 importer.
7. The user may confirm `PAGE2_AUTO`; that operation changes Companion Page 2 only and performs no Focusrite hardware write.
8. The launcher must then re-run read-only preflight and exact Page 2 verification.
9. Only after Page 2 is exact/current may the launcher reach `MIX_FEEDBACK` / `ALL_ISOLATED` hardware confirmations.
10. Resume the targeted Mix mute/solo feedback closure, not FULL/Core/SAFE/broad meter routing.

Living-state rule:
After every material event, update `docs/CURRENT_HANDOFF.md` AND this root `HANDOFF` before handoff. Record actual validation level, branch/checkpoint, whether hardware writes occurred, what remains pending, and the immediate next step.
