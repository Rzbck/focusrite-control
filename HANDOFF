FOCUSRITE CONTROL / COMPANION — UNIVERSAL AI RESUME ENTRYPOINT

Trigger word: HANDOFF
Updated: 2026-08-26
Current objective branch: testbench/meter-routing-exact-restore
Current development build: 0.1.21
Supported hardware claim: Scarlett 18i20 (3rd Gen) ONLY.

MANDATORY STARTUP FRESHNESS GATE
Before any decision, verify the live remote HEAD of testbench/meter-routing-exact-restore, inspect newer material commits, then read this file, docs/CURRENT_HANDOFF.md, docs/PUBLIC_ACTION_SURFACE_AUDIT_2026-08-26.md, docs/FEEDBACK_HARDWARE_CLOSURE_MATRIX.md, docs/HARDWARE_TEST_HISTORY.md and relevant current source/tests/evidence.

Evidence order: newest explicit physical hardware/user-host result -> completed direct-write evidence/current code/tests -> current handoff -> matrix/docs -> older captures/assumptions.

Always distinguish HARDWARE_DYNAMIC_CLOSED, HARDWARE_WRITE_CONFIRMED, SESSION_STATE_OBSERVED, SCHEMA_PRESENT, IMPLEMENTED, RESEARCH_ONLY, CONFIGURATION_UNAVAILABLE, UNKNOWN and UNSUPPORTED. UNKNOWN/blank/neverObserved is never proof of absence.

PROJECT LAUNCHERS FIRST
- UPDATE.bat = normal sync.
- UPDATE_AND_RUN.bat = sync + full local software gate.
- RUN.bat = software gate when already current.
- RUN_FINAL_HARDWARE_AUDIT.bat = full retained-public-write + Custom Mix final workflow; do not rerun now merely for repetition.
- RUN_FINAL_CUSTOM_MIX_RESUME.bat = read-only Phase B/C resume; final cumulative closure has now completed.
- exact testbench\RUN_*.cmd = targeted TestBench/research only.
- manual Git/PowerShell/Node = last resort.

DEVELOPMENT VERSIONING
Do not publish different Companion package bytes under the same development version. Current packaged production build is 0.1.21. The next packaged production/runtime/help/manifest change after 0.1.21 must bump again. TestBench/docs-only changes do not require another package-version bump. Stable public target remains v1.0.0 unless Bitfocus maintainers direct otherwise.

CURRENT STATUS — V1 HARDWARE / SOFTWARE CLOSURE
The broad hardware/protocol investigation is CLOSED FOR THE V1 PUBLIC SCOPE by explicit evidence or deliberate write withholding.

Newest user-host software gate at the final TestBench-only closure checkpoint:
- Node 22.23.2;
- Yarn 4.17.0;
- immutable dependencies PASS;
- Prettier PASS;
- ESLint PASS;
- source manifest PASS;
- 306/306 Node tests PASS;
- Companion package build PASS;
- generated focusrite-scarlett-18i20-0.1.21.tgz.

Classification: 0.1.21 SOFTWARE-GREEN on the user host.

Newest final hardware closure:
- prior V5 Phase A 0.1.21 accepted by the read-only resume gate as clean and recent;
- retained public write smoke: 42/42 PASS;
- hard abort: false;
- exact restore/global safety: clean;
- reconnect: PASS;
- output_pair_source absent from the V5 public smoke because it is deliberately WITHHELD;
- no new hardware write was needed during the final resume.

Final cumulative Custom Mix closure:
- mix_mute representative closed, mismatch 0;
- mix_solo representative closed, mismatch 0;
- mix_talkback representative closed, mismatch 0;
- fader representative: 7 changed paths;
- pan representative: 4 changed paths;
- Stereo/Mono representative: 2 changed paths;
- routing to Custom Mix: 7 Output pairs observed;
- Custom Mix meters: 12/12 closed, mismatch 0;
- FINAL CUSTOM MIX COVERAGE: COMPLETE.

The final resume correctly skipped another REC because the cumulative evidence was already complete. This is not a missing test: it is the intended reuse of previously captured read-only hardware evidence after the cumulative meter merge was corrected.

OUTPUT_PAIR_SOURCE DECISION
Older V8 pair topology evidence was re-read and is not sufficient for the modern two-member public contract. V3/V4 repeatedly failed strict two-member closure even with reciprocal schema pair metadata. Therefore for v1:
- output_pair_source is WITHHELD from installed public actions and presets;
- V4 remains historical failure evidence;
- V5 never creates or presses output_pair_source;
- do not weaken the oracle to obtain a PASS;
- do not reinterpret this as Stereo unsupported.

STEREO / MONO / CUSTOM MIX READBACK
Physical Focusrite Control operation and broad read-only REC work DID exercise visible Stereo/Mono and Custom Mix behavior. Retained server-confirmed readback evidence includes:
- faders;
- pan;
- Mute;
- Solo;
- source/stereo topology and visible Stereo/Mono changes;
- Talkback state;
- all 12/12 Custom Mix meters;
- currently available Output meters.

This is HARDWARE_DYNAMIC_CLOSED / SESSION_STATE_OBSERVED readback evidence. It is not automatically generic Companion write proof for output_stereo, mixer_slot_stereo or mix_* writes. Those v1 writes remain withheld.

FINAL V1 PUBLIC WRITE SURFACE
Kept public writes:
Monitor:
- monitor_mute
- monitor_dim
- monitor_talkback
- monitor_preset
Hardware Inputs:
- input_air
- input_pad
- input_mode
- input_mode_cycle
- input_nickname
Outputs, filtered by exact model/evidence/server availability:
- output_mute on validated direct members only
- output_gain_set / output_gain_adjust on validated analogue gain targets
- output_source on validated direct targets/direct source families
- output_nickname on validated direct targets
Device/settings:
- device_nickname
- phantom_persistence
- talkback_source
- reconnect

WITHHELD PUBLIC WRITES FOR V1
Readable state may remain where supported, but normal v1 actions/presets are removed:
- monitor_alt_enable
- monitor_alt
- output_stereo
- output_pair_source
- mixer_slot_source
- mixer_slot_stereo
- mix_mute
- mix_solo
- mix_gain_set
- mix_gain_adjust
- mix_pan
- mix_talkback
- device_preset
- clock_source
- sample_rate
- spdif_mode
- advanced_raw_set

Withholding is a v1 write-surface decision, not an unsupported-hardware claim.

OUTPUT POLICY
- direct Mute stays withheld on right/pair-owned members;
- pair-owned right Source stays withheld from direct routing;
- output_pair_source stays withheld;
- Monitor Outputs 1-2 direct Gain stays withheld;
- known no-effect Gain/Nickname paths stay withheld;
- Output Stereo write stays withheld globally;
- Outputs 21-24 remain write-blocked even if a future configuration reports available=true until explicitly hardware-tested in that available configuration;
- explicit available=false or UNKNOWN blocks writes;
- filtered actions/presets refresh when server-confirmed Output availability materialises/changes and callbacks still re-check live availability.

CUSTOM MIX / ASSIGN-MIX
User-facing terms are Custom Mix, Hardware Inputs, Software (DAW) Playback, Outputs, Stereo, Mute, MAIN and ALT. Do not instruct users with internal Mix A-F labels.

assign-mix remains:
- 26/26 SCHEMA_PRESENT;
- 0/26 materialised in tested sessions;
- raw semantics UNKNOWN;
- write transaction UNKNOWN;
- no public action/preset/feedback;
- no raw write.

Do not rerun NAVIGATE_MIXES and do not write assign-mix. It is not a v1 blocker.

PERMANENT SAFETY / FEATURE BOUNDARIES
- Scarlett 18i20 (3rd Gen) only unless real hardware testing expands scope.
- Monitor gain item 1677 is read-only; never re-add Monitor Set/Adjust/presets/raw write without new hardware proof.
- Never invent analogue input preamp Gain, direct per-input hardware Mute, per-channel phantom, Mic Kill or physical Monitor level control.
- Focusrite Control Server TCP port and device ID are dynamic; never hardcode.
- Writes require Remote Devices authorization matched to this module's own server-assigned client ID.
- Feedback/state is server-confirmed only; never optimistic.
- No unknown/unsafe raw writes, firmware/reset/restore/snapshot, meter/status writes or writes to UNKNOWN/available=false.
- Do not update Focusrite software/firmware/routing/settings outside an explicitly agreed test.
- Preserve privacy and MIT/third-party attribution; do not claim all protocol knowledge was independently discovered.

CURRENT NEXT ACTION
Hardware validation is complete for the frozen v1 public scope. Do NOT rerun the hardware audit merely for repetition.

Next:
1. perform an exact archive audit on the exact focusrite-scarlett-18i20-0.1.21.tgz generated/used on the user host;
2. require archive SHA-256, exact contents, package/manifest version coherence, bundled public-surface/forbidden-feature regression, privacy scan and attribution check;
3. record only sanitized audit results in tracked docs;
4. do not change public module scope/name yet;
5. wait for the official Bitfocus repository/naming decision.

PUBLICATION STATE
Repository request is already posted in Bitfocus Companion Slack #module-development. Bryce Seifert suggested focusrite-control may be a better repository/module scope because the transport is Focusrite Control Server and offered hardware for future testing. Project replied that only Scarlett 18i20 (3rd Gen) is validated and is open to Bitfocus's preferred naming.

WHEN THE OFFICIAL BITFOCUS REPOSITORY EXISTS
1. inspect exact repo name, default branch, seed files and permissions;
2. compare them with the cleaned current RC;
3. use the expected branch/PR workflow instead of overwriting;
4. run Bitfocus CI and local tests;
5. keep stable public target v1.0.0 unless maintainers direct otherwise;
6. only submit a Developer Portal tag after hardware/action audit and CI are clean.
