FOCUSRITE CONTROL / COMPANION — UNIVERSAL AI RESUME ENTRYPOINT

Trigger word: HANDOFF

MANDATORY STARTUP FRESHNESS GATE — REPO-WIDE RECENCY FIRST
When the user says only `HANDOFF`, do not resume from chat history, uploaded files, an embedded SHA, a copied handoff, or the default branch. Resume from the LIVE repository.

Before proposing code, hardware work, release work, branch changes, or asking the user to run anything:
1. Establish current date/time.
2. Inspect recent remote branch movement across the repository, not only `main`.
3. Identify the newest MATERIAL movements by commit time.
4. Choose the objective-owning branch using BOTH recency and relevance.
5. Resolve that branch's current remote HEAD and inspect newer commits/diff since the last validated checkpoint.
6. Read live `HANDOFF`, `AI_PROJECT_RULES.md`, `docs/CURRENT_HANDOFF.md`, `docs/PROTOCOL.md`, `docs/STATE_CONTRACT.md`, `docs/COLD_START_READBACK.md`, `docs/FEEDBACK_HARDWARE_CLOSURE_MATRIX.md`, and relevant current source/tests/evidence.
7. Reconcile any newer completed user/hardware result; newer completed physical/user evidence outranks stale written state.
8. Only then choose the next action.

A default-branch commit search can miss work on another branch. An embedded SHA is context only and is never permission to skip live Git verification.

MANDATORY EVIDENCE / INFERENCE GATE — NEVER TURN UNKNOWN INTO UNSUPPORTED
Keep separate:
1. OFFICIAL PRODUCT BEHAVIOUR;
2. SCHEMA_PRESENT;
3. SESSION_STATE_OBSERVED;
4. IMPLEMENTED;
5. HARDWARE_WRITE_CONFIRMED;
6. HARDWARE_DYNAMIC_CLOSED.

`UNKNOWN`, blank variable, missing cache entry, `BASELINE_UNKNOWN`, `SKIP_BASELINE_UNKNOWN`, or sparse initial state means only not observed in this client session unless stronger evidence proves otherwise. It is NOT evidence that a schema item is absent, false, unsupported, impossible, or permanently non-actionable.

If older physical/session evidence contradicts current cache coverage or newer UI/hardware evidence, keep the question RESEARCH_OPEN until reconciled. A reversible test requires only the server-confirmed state genuinely needed to restore the property/topology being changed; never guess a missing baseline.

PROJECT LAUNCHERS FIRST
- `UPDATE.bat` for normal sync.
- `UPDATE_AND_RUN.bat` for update + normal software gate.
- `RUN.bat` when already current.
- exact `testbench\RUN_*.cmd` launcher for targeted TestBench/hardware work.
- Manual shell/Git/PowerShell is last resort only when a project launcher is broken or cannot expose the required diagnostic.
- Do NOT make the user type ad-hoc PowerShell, raw Git commands, Node commands when the normal launcher workflow can do the work.
- Do not rebuild a second tool/workflow for behavior already present in the repository.

IMMUTABLE OBJECTIVE-CONTINUITY / NO-PREMATURE-CLOSURE RULE
- Closing a sub-question never closes its parent validation objective.
- Parent objective remains **explicit hardware feedback closure** across all 31 public feedback definitions/instances.
- A green software gate, 0 FAIL, inventory completeness, static oracle PASS, or closure of one Mix instance does not close the parent hardware-validation objective while material `EVAL_ONLY`, `MANUAL_PENDING`, `BASELINE_UNKNOWN`, `neverObserved`, unexercised, or otherwise open rows remain.
- Before changing objectives, explicitly account for all remaining open matrix rows.
- objective change is forbidden while relevant remaining open matrix rows exist, unless the user explicitly changes the project objective.
- Tooling/release/documentation work may interrupt hardware validation only as a direct blocker for the next safe validation step; when that blocker is removed, return to the parent hardware objective.
- Do not rerun FULL merely to improve counts and do not divert to publication while actionable closure remains.

Remote Devices authorization — mandatory before any write
- Focusrite Control → Device Settings → Remote Devices must show the existing `Companion Scarlett 18i20` client approved before any write-capable test.
- Always reuse the existing Companion Focusrite connection; do not delete/recreate it merely to obtain another client identity.
- Missing approval is `AUTHORIZATION/PREFLIGHT BLOCKED`, not a hardware-control failure.
- No extra direct clients by default.
- Never reuse/copy the Companion private client key into another process.
- See `docs/REMOTE_DEVICES_AUTHORIZATION.md`.

CURRENT LIVE OBJECTIVE
Branch: `testbench/meter-routing-exact-restore`.
Supported hardware claim: Scarlett 18i20 (3rd Gen) only.
Parent objective: explicit hardware feedback closure.
Canonical production candidate remains audited **0.1.16**.
Prior research/readback build **0.1.17** is software-validated, packaged, loaded on the existing authorised Companion connection, and exercised on the physical 18i20.
Current research build **0.1.18** is SOURCE_IMPLEMENTED for the autonomous mono/stereo Mix diagnostic, but its complete user-host software gate is still PENDING. Do not claim 0.1.18 is validated or hardware-tested yet.

RETAINED 0.1.17 SOFTWARE GATE
User-host source HEAD `515e9cf2f3e9`:
- dependencies PASS;
- Prettier PASS;
- ESLint PASS;
- source manifest PASS;
- Node tests 216/216 PASS;
- package build PASS: `focusrite-scarlett-18i20-0.1.17.tgz`.

That gate proves 0.1.17 only. 0.1.18 changes production policy narrowly enough that a new full gate and package build are mandatory before hardware.

FIRST 0.1.18 USER-HOST GATE — FORMAT BLOCKER FIXED, FULL GATE STILL PENDING
On 2026-08-25 the user synchronised the objective branch to HEAD `986da507e19d`, Node 22.23.2/Yarn 4.17.0 started correctly and immutable dependencies passed. Prettier then stopped the gate at step 2/6 on exactly five style-only files and printed the exact expected diff without modifying source. ESLint, source-manifest validation, Node tests and package build therefore did not run; no hardware command/write and no automatic Git promotion occurred. Those five Prettier-only diffs have now been applied with no intended logic change. A fresh `UPDATE_AND_RUN.bat` is mandatory before 0.1.18 can be called software-validated or packaged.

LATEST STRONG MIX HARDWARE RESULT
Completed 2026-08-24 using 0.1.17, the existing authorised Companion client, and `RUN_MIX_FEEDBACK_CLOSURE.cmd`, with Playback slot 3 / Playback 1 in a stereo topology.

- Mix A Left Mute: HARDWARE_DYNAMIC_CLOSED — server variable + rendered feedback `false -> true -> false`; exact restore confirmed.
- Mix A Left Solo: HARDWARE_DYNAMIC_CLOSED — same action/server/feedback/restore closure.
- Mix A Right Mute direct write: transition NOT CONFIRMED under the tested stereo topology; exact baseline restored.
- Mix A Right Solo direct write: transition NOT CONFIRMED under the tested stereo topology; exact baseline restored.
- Mix B-F: 20 `SKIP_BASELINE_UNKNOWN` in that run.
- summary: DYNAMIC_CLOSED 2 / FAIL 2 / RESTORE_QUARANTINE 0.
- hardware restore YES; Page 2 restore YES.

This was not a restore incident. Do not rerun the same direct-right stereo test unchanged and do not infer that the Right lane is globally pair-owned/unwritable/unsupported.

RUNTIME MONO/STEREO CORRECTION — NEWER UI EVIDENCE
The operator supplied Focusrite Control screenshots showing that source presentation can switch at runtime between individual mono channels and linked stereo pairs for Software Playback, Analogue inputs, S/PDIF, and ADAT families where available.

The operator then changed the previously tested linked `Playback 1-2` presentation to separate mono `Playback 1` and `Playback 2` strips. Latest known physical starting state is therefore **Playback 1 + Playback 2 mono**. Preserve it unless a newer live observation says otherwise.

Classification: UI_OBSERVED / product behaviour. It proves runtime topology exists but not the exact Control Server transaction sequence used by the official client.

Repository belief correction:
- old direct SINGLE-ITEM mixer-slot source writes on tested slots 1-4 produced no useful transition;
- old direct SINGLE-ITEM mixer-slot stereo writes on tested slots 3-4 produced no useful transition;
- those results are no longer allowed to imply feature/capability absence;
- generic/public mixer-slot source/stereo and Advanced Raw writes remain withheld until useful exact-restore grouped semantics are proven.

Correct current classification:
- `mixer_slot_stereo`: RESEARCH_OPEN / EVAL_ONLY for pair/group/transaction semantics;
- `mixer_slot_source`: RESEARCH_OPEN / EVAL_ONLY where needed to explain official grouped semantics;
- public support has NOT been promoted.

USER AUTHORIZATION FOR THE TARGETED AUTONOMOUS TEST
The user explicitly requested that the TestBench handle mono/stereo itself and not require manual topology switching between phases. The dedicated research campaign may temporarily change known mixer-slot stereo topology only under these guards:
- exact current stereo baseline for every touched slot;
- existing authorised Companion connection only;
- no guessed state;
- no direct TCP helper or raw write;
- source IDs/names observed as collateral state and not written;
- every topology transition server-confirmed;
- exact original topology/source state restored and confirmed;
- any unconfirmed restore => HARD ABORT / QUARANTINE;
- this permission is research/TestBench-only and is not public feature validation.

0.1.18 SOURCE IMPLEMENTATION — COMPLETE, VALIDATION PENDING
`package.json` now identifies research version **0.1.18**.

`src/definition-policy.js`:
- keeps `mixer_slot_source` hidden;
- keeps generic/raw mixer-slot source/stereo policy blocked;
- exposes `mixer_slot_stereo` only when the existing diagnostic `Expose all mixer slot variables` option is enabled;
- research action accepts explicit `on`/`off` only, no Toggle;
- refuses invalid slot/state and missing/invalid current server baseline;
- is still 18i20-only through the normal supported-model policy.

`testbench/MixFeedbackClosureRunner.js` now extends the EXISTING workflow rather than creating another helper:
1. reads live Playback source/name/stereo state;
2. reuses the previous sanitized exact target only if the same slot/name still exists with exact Mix baseline coverage; otherwise requires a unique best target;
3. runs current-topology Mute/Solo where exact;
4. starting from two adjacent known-mono Playback members, dynamically finds the odd/even Playback mate rather than hardcoding slots 3/4;
5. creates one Companion button step containing exactly two `mixer_slot_stereo` ON actions and one exact two-action restore step;
6. monitors both stereo variables plus source IDs/names;
7. requires server-confirmed stereo transition and unchanged sources before stereo `side=both` Mix testing;
8. re-reads live Mute/Solo baselines under stereo before testing;
9. restores both stereo flags in `finally` and requires exact original source/topology state;
10. topology restore failure hard-aborts/quarantines;
11. paired no-transition restores and stops safely; next research is official-client grouped/atomic multi-item `<set>` semantics, never raw guessing.

`testbench/RUN_MIX_FEEDBACK_CLOSURE.cmd` is aligned with 0.1.18:
- explains the autonomous topology phase;
- keeps a read-only software/preflight/Page2 stage before confirmations;
- targeted self-check now includes Mix closure/preparation, Page2 contracts, V8 generic evidence, state safety, and `src/definition-policy.js` syntax;
- requires `MIX_FEEDBACK` and `ALL_ISOLATED` once;
- explicitly tells the operator not to manually touch mono/stereo, Mute, Solo, or faders during hardware;
- does not build/install the Companion package and creates no direct TCP client.

DOCUMENTATION / BELIEF RECONCILIATION
- `testbench/FullTestBenchProfilesV8.js` comments now say mixer-slot `noEffect*` is direct single-item evidence only.
- `src/hardware-policy.js` now says the same while leaving generic/raw blocking behavior unchanged.
- `docs/COLD_START_READBACK.md` no longer asks for obsolete manual Solo materialisation; it records the completed Mix A closures and autonomous 0.1.18 objective.
- `docs/FEEDBACK_HARDWARE_CLOSURE_MATRIX.md` now classifies mixer-slot source/stereo as RESEARCH_OPEN rather than capability-absent/unsupported and records Mix A Left dynamic closures plus Right topology-dependent research.

CURRENT MIX STATUS
- `mix_mute`: PARTIAL — Mix A Left HARDWARE_DYNAMIC_CLOSED; Mix A Right direct stereo no-transition with exact restore; autonomous mono/stereo differential pending; Mix B-F open.
- `mix_solo`: PARTIAL — same classification.
- `mixer_slot_stereo`: RESEARCH_OPEN — paired 0.1.18 TestBench source implemented, software/hardware validation pending.
- `mixer_slot_source`: RESEARCH_OPEN but no 0.1.18 source write is exposed or attempted.

RETAINED PARENT EVIDENCE
- 31 definitions / 829 instances.
- historical V8 static/oracle 190 PASS / 639 EVAL_ONLY / 0 FAIL.
- historical V8 dynamic 20 both-state / 12 single-state / 710 neverObserved / 0 FAIL.
- later meter closure 14/46: inputs 8/8, outputs 4/26, mixes 2/12, mismatch 0.
- targeted Core 18/18 SKIP_BASELINE_UNKNOWN, zero writes/FAIL/restore quarantine — readback evidence, not capability absence.

EXACT IMMEDIATE NEXT USER ACTION — SOFTWARE ONLY
Do not ask the user to run hardware yet and do not ask them to switch mono/stereo manually.

Run only:
`UPDATE_AND_RUN.bat`
Stay on `testbench/meter-routing-exact-restore` / choose the current branch.

The required 0.1.18 gate is:
- dependencies PASS;
- Prettier PASS;
- ESLint PASS;
- source manifest PASS;
- all Node tests PASS;
- package build PASS and package name/version confirms `focusrite-scarlett-18i20-0.1.18.tgz`.

Do not infer PASS from source inspection. If the gate fails, diagnose the whole failure chain and fix once before any hardware.

ONLY AFTER A FULL GREEN 0.1.18 USER-HOST GATE
1. Import/load `focusrite-scarlett-18i20-0.1.18.tgz`.
2. Select 0.1.18 on the **existing** Companion Focusrite connection; do not recreate it.
3. Keep/enable `Expose all mixer slot variables` for the research action gate.
4. Confirm the same existing `Companion Scarlett 18i20` Remote Devices entry remains approved.
5. Leave Playback 1 and Playback 2 in their current mono state.
6. Keep Monitor/speakers/headphones physically safe.
7. Run only `testbench\RUN_MIX_FEEDBACK_CLOSURE.cmd`.
8. Use `PAGE2_AUTO` only if the launcher positively identifies a recognised stale TestBench Page 2.
9. Confirm `MIX_FEEDBACK`, then `ALL_ISOLATED` once.
10. Touch nothing in Focusrite Control during the hardware phase; TestBench owns topology transition and exact restoration.

If paired `mixer_slot_stereo` actions produce no useful transition but restore exactly, do not repeat blindly. Preserve that evidence and research official-client grouped/atomic-set semantics next.

PERMANENT SAFETY
- Scarlett 18i20 (3rd Gen) only.
- Monitor gain item 1677 read-only.
- No invented input preamp gain, direct per-input hardware mute, per-channel phantom switching, Mic Kill, or physical Monitor level write.
- Dynamic Focusrite Control Server port and device ID only.
- Writes require this module's own authorised server-assigned client identity.
- Feedback/state only from server-confirmed state; no optimistic success.
- No unknown/unsafe raw writes, firmware/reset/restore/snapshot commands, or meter/status writes.
- No write to explicit UNKNOWN output availability.
- No Focusrite software/firmware update or unrelated routing change without explicit agreement.
- Keep public source/privacy clean and preserve relevant attribution.

Living-state rule: after every material software/hardware/user result or blocker, update both root `HANDOFF` and `docs/CURRENT_HANDOFF.md`. Record actual validation level; pending work is never PASS.
