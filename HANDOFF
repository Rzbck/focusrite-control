FOCUSRITE CONTROL / COMPANION — UNIVERSAL AI RESUME ENTRYPOINT

Trigger word: HANDOFF
Updated: 2026-08-26
Current objective branch: testbench/meter-routing-exact-restore
Current development build: 0.1.21
Supported hardware claim: Scarlett 18i20 (3rd Gen) ONLY.

MANDATORY STARTUP FRESHNESS GATE
When resuming, verify the LIVE repository first. Inspect repository-wide branch movement, resolve the current remote HEAD of the objective-owning branch, inspect newer commits/diff, then read this file, docs/CURRENT_HANDOFF.md, docs/PUBLIC_ACTION_SURFACE_AUDIT_2026-08-26.md, docs/FEEDBACK_HARDWARE_CLOSURE_MATRIX.md, and relevant current source/tests/evidence.

Evidence order: newest explicit physical hardware/user-host result -> completed direct-write evidence/current code/tests -> current handoff -> matrix/docs -> older captures/assumptions.

Always distinguish HARDWARE_DYNAMIC_CLOSED, HARDWARE_WRITE_CONFIRMED, SESSION_STATE_OBSERVED, SCHEMA_PRESENT, IMPLEMENTED, RESEARCH_ONLY, CONFIGURATION_UNAVAILABLE, UNKNOWN, and UNSUPPORTED. UNKNOWN/blank/neverObserved is never proof of absence.

PROJECT LAUNCHERS FIRST
- UPDATE.bat = normal sync.
- UPDATE_AND_RUN.bat = sync + full local software gate.
- RUN.bat = software gate when already current.
- RUN_FINAL_HARDWARE_AUDIT.bat = final retained-public-write smoke + cumulative Custom Mix read-only validation after a green software gate.
- exact testbench\RUN_*.cmd = targeted TestBench/hardware work.
- manual Git/PowerShell/Node = last resort only.

DEVELOPMENT VERSIONING
Do not publish different Companion package bytes under the same development version. Packaged production bytes changed after 0.1.20, so the current development build is 0.1.21. The next packaged change after 0.1.21 must bump again. TestBench/docs-only changes do not require a package-version bump. Stable public target remains v1.0.0 unless Bitfocus maintainers direct otherwise.

CURRENT OBJECTIVE
The broad hardware feedback/protocol investigation is CLOSED FOR THE V1 SCOPE by explicit evidence or deliberate write withholding.

The immediate objective is to gate the new 0.1.21 package, then run ONE final hardware workflow:
1. Phase A validates only the retained public v1 writes with server-confirmed baseline, transition, exact restore and collateral audit;
2. Phase B runs the existing read-only recorder while the operator traverses visible Focusrite Control Custom Mix controls;
3. Phase C accumulates sanitized Custom Mix coverage.

Do not weaken a hardware oracle merely to turn a repeated NO_TRANSITION into PASS.

LATEST HARDWARE RESULT — PUBLIC-SURFACE V4
Newest explicit physical hardware/user-host result for the release smoke:
- module 0.1.20;
- V4 reciprocal source-pair metadata;
- SAFE Core PASS=3 FAIL=0 SKIP=18;
- 52 release tests;
- PASS 42;
- FAIL 10;
- hardAbort=false;
- reconnect PASS;
- global restore audit PASS;
- all ten FAIL results are output_pair_source NO_TRANSITION;
- all runnable direct Output Source/Gain/Nickname tests passed with server-confirmed transition + exact restore;
- Input nickname/mode-cycle, Device nickname, Phantom Persistence and Monitor preset also passed where runnable.

This result is stronger than the older pair-routing oracle because it requires BOTH Output members to reach the requested reciprocal source pair.

OUTPUT_PAIR_SOURCE RECLASSIFICATION
The old V8 pair-topology conclusion was re-read. V8 remains useful topology/ownership evidence, but its pair oracle could pass when the requested left member changed and the right member remained on its original source. Therefore it did not prove the modern public output_pair_source contract.

V3/V4 then repeatedly showed the same full-pair failure, and V4 removed the previous display-name pairing guess by using reciprocal parser/schema metadata.

Decision for v1:
- output_pair_source is WITHHELD from the installed public action surface;
- no public preset may use it;
- the implementation/research primitive may remain internally for historical/research use;
- the final V5 release smoke does not create or press output_pair_source buttons;
- do not reinterpret this as "Stereo unsupported".

STEREO / MONO EVIDENCE — DO NOT LOSE THIS
Physical Focusrite Control operation and broad read-only REC sessions DID exercise visible Stereo/Mono topology. The retained server-confirmed readback evidence is strong for:
- Custom Mix faders;
- pan;
- Mute;
- Solo;
- source/stereo topology, including visible Stereo/Mono changes;
- Talkback state;
- all 12/12 Custom Mix meters;
- current available Output meters.

This is HARDWARE_DYNAMIC_CLOSED/readback evidence for the observed UI/state paths. It is NOT the same transaction as output_pair_source, and it does not create generic Companion write proof for output_stereo or mixer_slot_stereo. Those write actions remain withheld while truthful readback stays available.

OUTPUT-LIFECYCLE REPAIR RETAINED
A prior 0.1.20 smoke exposed a cold-start Output definition regression: action choices could be filtered while availability was still unknown and never rebuilt after availability materialised.

src/main.js now refreshes actions/presets in a debounced way on ready and on Output availability materialisation/change. Ordinary state packets do not cause definition rebuilds. Callback-time availability checks still fail closed. This repair remains valid and is regression-tested by test/output-definition-refresh.test.js.

CURRENT 0.1.21 PRODUCTION CHANGE
Packaged runtime policy changed, so this is a real package-version bump:
- package.json = 0.1.21;
- src/definition-policy.js adds output_pair_source to V1_WITHHELD_ACTIONS;
- installed public Companion definitions remove output_pair_source;
- presets using output_pair_source are removed by the same withheld-action policy;
- readback/feedback remain server-confirmed;
- no new write family is added;
- no raw bypass is added;
- no hardware policy is loosened.

CURRENT FINAL TESTBENCH
V4 is retained as historical evidence of the 10/10 pair-route failure.
V5 is the final public-surface smoke for 0.1.21:
- it reuses the exact-restore/collateral machinery;
- it excludes output_pair_source from expected public actions;
- it never builds or presses output_pair_source;
- it keeps current live configuration as baseline;
- Monitor preset remains last;
- restore/baseline/collateral hard-abort semantics remain fail-closed.

RUN_V1_RELEASE_SMOKE.cmd and RUN_FINAL_HARDWARE_AUDIT.cmd now target V5 / 0.1.21.

CURRENT SOFTWARE STATUS
0.1.21 is SOFTWARE-GATE-PENDING.
Do NOT claim software-green yet.
Do NOT import 0.1.21 into Companion yet.
Do NOT run hardware yet.

The latest fully green packaged user-host checkpoint remains an earlier 0.1.20 gate. Because 0.1.21 changes packaged production policy and help/version bytes, a fresh user-host full gate is mandatory.

EXACT NEXT ACTION
1. Run the checked-in UPDATE_AND_RUN.bat on testbench/meter-routing-exact-restore.
2. Require the complete gate: dependencies, Prettier, ESLint, source manifest, all Node tests, Companion package build.
3. Send the complete gate log before any hardware run.
4. If and only if the whole 0.1.21 gate is green, import the freshly generated focusrite-scarlett-18i20-0.1.21.tgz into Companion.
5. Keep the existing Focusrite Companion connection and its Remote Devices identity/approval; do not delete/recreate it.
6. Keep Focusrite Control open and leave the current configuration as-is.
7. If requested, enable only Expose mixer diagnostic variables (read-only).
8. Physically isolate/mute downstream Outputs and lower physical Monitor/headphone levels before Phase A.
9. Run root RUN_FINAL_HARDWARE_AUDIT.bat.
10. Phase A uses V5 and contains no output_pair_source write. A RESTORE_FAILURE, PREWRITE_DRIFT, COLLATERAL_DRIFT or equivalent hard abort stops before Phase B.
11. Phase B uses the existing broad recorder READ-ONLY while the operator traverses visible Custom Mix, Hardware Inputs, Software (DAW) Playback, Stereo, Mute and the requested visible controls.
12. Phase C accumulates sanitized Custom Mix coverage.
13. After clean hardware validation, run a fresh exact package/privacy/forbidden-feature audit on the exact 0.1.21 archive used.

Do not run unrelated sample-rate/Digital-I/O changes, firmware/software updates, unknown raw writes, NAVIGATE_MIXES or assign-mix writes.

FINAL V1 PUBLIC WRITE SURFACE
Authoritative audit: docs/PUBLIC_ACTION_SURFACE_AUDIT_2026-08-26.md.

KEPT PUBLIC WRITES
Monitor:
- monitor_mute
- monitor_dim
- monitor_talkback
- monitor_preset

Hardware Inputs:
- input_air
- input_pad
- input_mode
- input_mode_cycle
- input_nickname

Outputs, filtered by exact model/evidence/server availability:
- output_mute on validated direct members only
- output_gain_set / output_gain_adjust on validated analogue gain targets
- output_source on validated direct targets/direct source families
- output_nickname on validated direct targets

Device/settings:
- device_nickname
- phantom_persistence
- talkback_source
- reconnect

WITHHELD PUBLIC WRITES FOR V1
Readable state may remain, but normal v1 write actions/presets are removed:
- monitor_alt_enable
- monitor_alt
- output_stereo
- output_pair_source
- mixer_slot_source
- mixer_slot_stereo
- mix_mute
- mix_solo
- mix_gain_set
- mix_gain_adjust
- mix_pan
- mix_talkback
- device_preset
- clock_source
- sample_rate
- spdif_mode
- advanced_raw_set

Withholding is a v1 write-surface decision, not an unsupported-hardware claim. Readback evidence can strengthen classification without re-exposing a write.

OUTPUT POLICY
- right/pair-owned members are withheld for direct Mute writes;
- pair-owned right Source is withheld from direct routing;
- output_pair_source is withheld from public v1;
- Monitor Outputs 1-2 direct Gain withheld;
- known no-effect Gain/Nickname paths withheld;
- Output Stereo write withheld globally;
- Outputs 21-24 remain write-blocked even if a future configuration reports available=true until explicitly hardware-tested in that available configuration;
- explicit available=false / UNKNOWN blocks writes;
- action/preset definitions refresh when server-confirmed Output availability materialises or changes, while callbacks still re-check live availability and fail closed.

CUSTOM MIX / USER TERMINOLOGY
User-facing instructions MUST use Focusrite Control terms: Custom Mix, Hardware Inputs, Software (DAW) Playback, Outputs, Stereo, Mute, MAIN, ALT. Do not ask the user to manipulate internal protocol/TestBench Mix A-F names.

Focusrite Control presents simply Custom Mix. Internal mix IDs are not reliably mapped to the visible UI.

assign-mix remains:
- 26/26 SCHEMA_PRESENT;
- 0/26 materialised in active tested sessions;
- raw semantics UNKNOWN;
- write transaction UNKNOWN;
- no public action/preset/feedback;
- no raw write.

Do not rerun NAVIGATE_MIXES. Do not write assign-mix. It is not a v1 blocker.

PASSIVE REC STATE RULE
A read-only/passive recorder does NOT require the user to restore Focusrite Control to its starting state merely because the final snapshot differs.

Exact baseline/restoration remains mandatory for write-capable reversible hardware tests where rollback is part of the safety contract.

PERMANENT SAFETY / FEATURE BOUNDARIES
- Scarlett 18i20 (3rd Gen) only unless real hardware testing expands scope.
- Monitor gain item 1677 read-only; never re-add Monitor Set/Adjust actions, presets or raw write without new hardware proof.
- Never invent analogue input preamp Gain, direct per-input hardware Mute, per-channel phantom power, Mic Kill or physical Monitor level control.
- Focusrite Control Server TCP port and device ID are dynamic.
- Writes only after Remote Devices authorisation matched to this module's own server-assigned client ID.
- Server-confirmed feedback/state only, never optimistic.
- No write to UNKNOWN or explicit available=false.
- No unknown/unsafe raw writes, firmware/reset/restore/snapshot or meter/status writes.
- Do not update Focusrite software/firmware without explicit agreement.
- Preserve privacy and required MIT/third-party attribution; do not claim all protocol knowledge was independently discovered.

PUBLICATION STATE
Repository request is already posted in Bitfocus Companion Slack #module-development. Bryce Seifert suggested focusrite-control may be the better repository scope and offered hardware for future testing.

Keep public hardware scope at Scarlett 18i20 (3rd Gen) only. Do not rename/broaden public scope before the official Bitfocus repository/naming decision.

WHEN THE OFFICIAL BITFOCUS REPOSITORY EXISTS
1. inspect exact repo name, default branch, seed files and permissions;
2. compare with the cleaned current RC;
3. use expected branch/PR workflow rather than overwriting;
4. run Bitfocus CI and local tests;
5. keep stable target v1.0.0 unless maintainers direct otherwise;
6. only submit the Developer Portal tag after official CI and hardware/action audit are clean.
