FOCUSRITE CONTROL / COMPANION — UNIVERSAL AI RESUME ENTRYPOINT

Trigger word: HANDOFF

MANDATORY STARTUP FRESHNESS GATE — REPO-WIDE RECENCY FIRST
When the user says only `HANDOFF`, do not resume from chat history, uploaded files, copied handoffs, an embedded SHA, or the default branch. Resume from the LIVE repository.

Before proposing code, hardware work, release work, branch changes, or asking the user to run anything:
1. Establish current date/time.
2. Inspect recent remote branch movement across the repository, not only `main`.
3. Identify the newest MATERIAL movements by commit time.
4. Choose the objective-owning branch using BOTH recency and relevance.
5. Resolve that branch's current remote HEAD and inspect newer commits/diff since the last validated checkpoint.
6. Read live `HANDOFF`, `AI_PROJECT_RULES.md`, `docs/CURRENT_HANDOFF.md`, `docs/PROTOCOL.md`, `docs/STATE_CONTRACT.md`, `docs/COLD_START_READBACK.md`, `docs/FEEDBACK_HARDWARE_CLOSURE_MATRIX.md`, and relevant current source/tests/evidence.
7. Reconcile any newer completed user/hardware result before choosing the next action; newer completed physical/human result outranks stale written state.
8. Only then choose the next action.

A default-branch commit search can miss work on another branch. An embedded SHA is context only and is never permission to skip live Git verification.

MANDATORY EVIDENCE / INFERENCE GATE — NEVER TURN UNKNOWN INTO UNSUPPORTED
Keep separate: OFFICIAL PRODUCT BEHAVIOUR / SCHEMA_PRESENT / SESSION_STATE_OBSERVED / IMPLEMENTED / HARDWARE_WRITE_CONFIRMED / HARDWARE_DYNAMIC_CLOSED.
`UNKNOWN`, blank, missing cache, `BASELINE_UNKNOWN`, `SKIP_BASELINE_UNKNOWN`, sparse state or `neverObserved` means only not observed in this client session absent stronger evidence. It is not proof that a control is false, absent, unsupported or impossible.

PROJECT LAUNCHERS FIRST
- `UPDATE.bat` for normal sync.
- `UPDATE_AND_RUN.bat` for update + normal software gate.
- `RUN.bat` when already current.
- exact `testbench\RUN_*.cmd` launcher for targeted TestBench/hardware work.
- Manual shell/Git/PowerShell/Node is last resort only when the normal launcher is itself broken or cannot expose the required diagnostic.
- Do NOT make the user type ad-hoc PowerShell, raw Git commands, Node commands when the normal launcher workflow can do the work.
- Do not rebuild a second tool/workflow for behavior already present in the repository.

IMMUTABLE OBJECTIVE-CONTINUITY / NO-PREMATURE-CLOSURE RULE
Closing a sub-question never closes its parent validation objective. Parent objective remains explicit hardware feedback closure across all 31 public feedback definitions/instances while material `EVAL_ONLY`, `MANUAL_PENDING`, `BASELINE_UNKNOWN`, `neverObserved`, unexercised or otherwise open rows remain.
- Tooling, release, documentation or repository work may interrupt hardware validation only when it is a direct blocker for the next safe validation step.
- Once that direct blocker is removed, return to the parent hardware objective.
- Before changing objectives, explicitly account for remaining open matrix rows.
- objective change is forbidden while relevant remaining open matrix rows exist unless the user explicitly changes the project objective.
Publication is not the current objective.

REMOTE DEVICES AUTHORIZATION — MANDATORY BEFORE ANY WRITE
- Focusrite Control → Device Settings → Remote Devices must show the existing `Companion Scarlett 18i20` client approved.
- Reuse the existing Companion Focusrite connection; do not delete/recreate it for testing.
- Missing approval is AUTHORIZATION/PREFLIGHT BLOCKED, not a hardware failure.
- No extra direct clients by default.
- Never reuse/copy the Companion private client key into another process.
- See `docs/REMOTE_DEVICES_AUTHORIZATION.md`.

CURRENT LIVE OBJECTIVE
Branch: `testbench/meter-routing-exact-restore`.
Supported hardware claim: Scarlett 18i20 (3rd Gen) only.
Parent objective: explicit hardware feedback closure.
Canonical production candidate remains audited 0.1.16.
Research 0.1.17 is software validated, packaged and real-hardware exercised.
Research 0.1.18 was software validated and exercised through the latest Mix materialisation campaign.
Research 0.1.19 is now the current READ-ONLY assign-mix characterisation build: SOURCE_IMPLEMENTED / USER-HOST FULL GATE PENDING / HARDWARE READBACK PENDING.

LAST FULLY GREEN USER-HOST SOFTWARE CHECKPOINT
`UPDATE_AND_RUN.bat` at exact source/TestBench checkpoint `7486e7200d05a517e2c38e70991e1df72a50d8e8` completed:
- portable Node 22.23.2 / Yarn 4.17.0 PASS;
- dependencies PASS;
- Prettier PASS;
- ESLint PASS;
- source manifest PASS;
- Node tests **244/244 PASS / 0 FAIL**;
- Companion package build PASS: `focusrite-scarlett-18i20-0.1.18.tgz`.
The launcher did not install or activate the package and performed no hardware write.

LATEST HARDWARE RESULT — OUTPUT_PAIR_SOURCE TO MIX A WRITE ATTEMPTED / NO CONFIRMED TRANSITION / EXACT RESTORE
The latest `RUN_MIX_FEEDBACK_CLOSURE.cmd` run on the existing authorised 0.1.18 Companion connection completed targeted self-check **77/77 PASS**, exact model/preflight/authorization/Page 2 checks, then:

Path A — Playback topology materialisation:
- `slot 3 :: Playback 1 :: unknown`;
- `slot 4 :: Playback 2 :: unknown`;
- later Playback odd anchors had server-observed stereo flags;
- Playback 1/2 original `mixer_slot_stereo` values were not observed, so the topology-changing path correctly made **0 writes**.

Path B — existing `output_pair_source` fallback:
- selected non-Monitor outputs **3-4** with exact baseline `Playback 3 + Playback 4`;
- selected Playback coverage target slots 3/4 (`Playback 1 + Playback 2`) without requiring topology state;
- imported one temporary guarded `output_pair_source` button;
- a real output-pair route write was attempted toward Mix A;
- server state did **not** confirm Mix A L/R on outputs 3-4;
- exact original output sources Playback 3/4 were then server-confirmed restored;
- temporary Page 2 was restored;
- fresh Mix exact-baseline coverage remained slot 3=0 / slot 4=0;
- final result was a fully restored `NO-OP SAFE` with no Mix Mute/Solo continuation.

NARROW INFERENCE ONLY
Do NOT call `output_pair_source` globally broken. The new evidence is limited to: one pair-aware `source`-item attempt using Mix A lane IDs on Line 3-4 produced no server-confirmed route transition and restored exactly. Existing normal paired Playback-source routing evidence remains separate.

The output schema also contains a distinct `assign-mix` control per output, separate from `source`, plus `assign-talkback-mix`. Exact `assign-mix` value semantics and official transaction behavior are not yet validated. The latest no-transition makes this a material research hypothesis, not a proven explanation. Do NOT repeat the same Mix-A-via-`source` attempt blindly on more output pairs.

CURRENT 0.1.19 READ-ONLY ASSIGN-MIX RESEARCH — SOURCE IMPLEMENTED / FULL GATE PENDING
0.1.19 intentionally adds READBACK ONLY. It does not add any new write family.

`package.json`:
- research version bumped from 0.1.18 to 0.1.19 so this instrumentation remains traceable.

`src/variables.js` behind the existing diagnostic `Expose all mixer slot variables` gate:
- exposes `output_N_assign_mix_class` only when that output has schema `assignMix`;
- exposes `output_N_assign_mix_provenance`;
- raw server values are converted in-memory to deterministic per-refresh opaque equality classes `V1`, `V2`, ... before Companion variables are exposed;
- same token means same currently observed server value; token numbers have NO semantic meaning;
- no raw assign-mix value is exposed by these research variables.

Safety remains unchanged:
- `assign-mix` is still excluded from `device.writableIds`;
- no `assign-mix` action, preset, public feedback or Advanced Raw write was added;
- normal hardware policy was not broadened;
- no direct TCP client/read command was added.

Existing `testbench/MeterMixPlaybackBaselineReadOnlyProbe.js` and existing `RUN_METER_MIX_BASELINE_READONLY.cmd` were extended rather than creating a duplicate workflow:
- launcher now requires research 0.1.19 and the same diagnostic variable gate;
- probe reads output source name/stereo plus opaque assign-mix class/provenance;
- report stores only schema-present/known/class/provenance, never raw assign-mix values or item IDs;
- `DONE` is sufficient for the next observation; no manual routing change is requested;
- no Companion button press, no Page 2 replacement and no Focusrite write exist in this probe.

New regressions cover diagnostic gating, opaque equality classes, provenance, parsed schema presence, `assign-mix` exclusion from writable IDs, report privacy and read-only launcher behavior.

LATEST STRONG MIX HARDWARE RESULT RETAINED FROM 0.1.17
Under the then-tested stereo Playback topology:
- Mix A Left Mute: HARDWARE_DYNAMIC_CLOSED, false → true → false, server variable + rendered feedback + exact restore.
- Mix A Left Solo: HARDWARE_DYNAMIC_CLOSED, same.
- Mix A Right Mute direct stereo write: no transition, exact restore.
- Mix A Right Solo direct stereo write: no transition, exact restore.
- Mix B-F: 20 `SKIP_BASELINE_UNKNOWN`.
- restore quarantine 0; hardware restore YES; Page 2 restore YES.
Do not infer global Right-lane ownership/unwritability/unsupported from that stereo-only result.

CURRENT MIX / ROUTING STATUS
- `mix_mute`: PARTIAL.
- `mix_solo`: PARTIAL.
- `mixer_slot_stereo`: RESEARCH_OPEN; latest Playback 1/2 topology is SESSION_STATE_OBSERVED as UNKNOWN, so topology write remains withheld.
- `mixer_slot_source`: RESEARCH_OPEN; no source write exposed/attempted in current research.
- `output_pair_source`: PARTIAL evidence; normal pair-aware routing exists, but the tested Mix-A-via-source attempt on outputs 3-4 had no confirmed transition and exact restore.
- output `assign-mix`: SCHEMA_PRESENT / READ-ONLY CHARACTERISATION IMPLEMENTED / SEMANTICS UNKNOWN / NO WRITE PATH.

RETAINED PARENT EVIDENCE
31 definitions / 829 instances; historical static/oracle 190 PASS / 639 EVAL_ONLY / 0 FAIL; historical dynamic 20 both-state / 12 single-state / 710 neverObserved / 0 FAIL; meter closure 14/46 (inputs 8/8, outputs 4/26, mixes 2/12); targeted Core 18/18 SKIP_BASELINE_UNKNOWN with zero writes/FAIL/restore quarantine.

EXACT IMMEDIATE NEXT ACTION — 0.1.19 SOFTWARE GATE, THEN READ-ONLY HARDWARE OBSERVATION
1. Do NOT run another write-capable Mix/routing campaign yet.
2. Run `UPDATE_AND_RUN.bat` and stay on `testbench/meter-routing-exact-restore`.
3. Require dependencies PASS, Prettier PASS, ESLint PASS, source manifest PASS, ALL Node tests PASS and package build PASS for 0.1.19.
4. Only after a fully green gate, import `focusrite-scarlett-18i20-0.1.19.tgz` and select 0.1.19 on the EXISTING Companion Focusrite connection; do not recreate it.
5. Keep `Expose all mixer slot variables` enabled and verify the same `Companion Scarlett 18i20` Remote Devices client remains approved.
6. Run only `testbench\RUN_METER_MIX_BASELINE_READONLY.cmd`.
7. Type `DONE` when prompted. Do not manually change routing, Mute/Solo, mono/stereo or faders for this observation.
8. Preserve/paste the complete output, especially `Assign-mix readback coverage` and all output rows with `assignMix=...` tokens/provenance.
9. This next probe is strictly READ-ONLY. Its purpose is to determine whether the current Custom Mix destination(s) and direct Playback destinations show distinct server-observed `assign-mix` state classes before considering any write semantics.

After that read-only result, return to the parent hardware objective. Any future `assign-mix` write requires positive semantics + exact baseline/restore proof first; do not expose or raw-write it speculatively.

PERMANENT SAFETY
Scarlett 18i20 (3rd Gen) only. Monitor gain item 1677 read-only. No invented input preamp gain, direct per-input hardware mute, per-channel phantom, Mic Kill or physical Monitor level write. Dynamic Control Server port/device ID only. Server-confirmed feedback/state only. No unsafe raw writes, firmware/reset/restore/snapshot commands, or meter/status writes. No write to explicit UNKNOWN output availability. No Focusrite software/firmware update or unrelated routing change without explicit agreement. Preserve privacy and attribution.

Living-state rule: after every material software/hardware/user result or blocker, update both root `HANDOFF` and `docs/CURRENT_HANDOFF.md`. Pending work is never PASS.
