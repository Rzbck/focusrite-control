FOCUSRITE CONTROL / COMPANION — UNIVERSAL AI RESUME ENTRYPOINT

Trigger word: HANDOFF
Updated: 2026-08-26
Current objective branch: testbench/meter-routing-exact-restore
Supported hardware claim: Scarlett 18i20 (3rd Gen) ONLY.
Parent objective: explicit hardware feedback/protocol closure before public release.
Publication is NOT the current objective.

MANDATORY STARTUP FRESHNESS GATE
When resuming, verify the LIVE repository first. Inspect repository-wide branch movement, resolve the current remote HEAD of the objective-owning branch, inspect newer commits/diff, then read this file, docs/CURRENT_HANDOFF.md, docs/FEEDBACK_HARDWARE_CLOSURE_MATRIX.md, and relevant current source/tests/evidence. Newest explicit physical hardware/user-host result overrides older captures or assumptions.

Always distinguish HARDWARE_DYNAMIC_CLOSED, HARDWARE_WRITE_CONFIRMED, SESSION_STATE_OBSERVED, SCHEMA_PRESENT, IMPLEMENTED, RESEARCH_ONLY, CONFIGURATION_UNAVAILABLE, UNKNOWN, and UNSUPPORTED. UNKNOWN/blank/neverObserved is never proof of absence.

PROJECT LAUNCHERS FIRST
- UPDATE.bat = normal sync.
- UPDATE_AND_RUN.bat = sync + full local software gate.
- RUN.bat = software gate when already current.
- exact testbench\RUN_*.cmd = targeted TestBench/hardware work.
- manual Git/PowerShell/Node is last resort only.

LATEST FULLY GREEN SOFTWARE CHECKPOINT
Exact user-host HEAD e8d7e72ec5e50e42903cf8057acbeb63aaca4ba7 passed the complete local gate on 2026-08-26: Node 22.23.2, Yarn 4.17.0, dependencies PASS, Prettier PASS, ESLint PASS, source manifest PASS, 279/279 Node tests PASS, Companion package PASS, focusrite-scarlett-18i20-0.1.19.tgz. No hardware write from the gate.

NEWEST HARDWARE RESULT — ALT / SPEAKER SWITCHING + METER CLOSURE
Sanitized LATEST_MANUAL_FEEDBACK_SWEEP reportVersion 6 updated 2026-08-26T06:29:16.831Z, module 0.1.19.

Exact supplied report SHA-256:
308a78f3b48391dec292f634a8eb0082ee0111da42a2977c9ea61e074bfa06f9

Recorder:
- read-only harness=true;
- hardwareWritesByHarness=false;
- companionButtonPressesByHarness=false;
- 829 probes / 31 feedback definitions / 46 meters;
- duration 165060 ms;
- 11 feedback transitions;
- 11 confirmed PASS;
- 0 transient race;
- 0 confirmed mismatch.

ALT / SPEAKER SWITCHING NOW CLOSED FOR FEEDBACK/READBACK
monitor_alt_enable produced 3 clean PASS transitions and both boolean states.
monitor_alt produced 4 clean PASS transitions and both boolean states.
No mismatch or race.

Human Output 3 availability changed in lock-step with Speaker Switching enable: true->false, false->true, true->false. Treat this as runtime ownership/availability behavior. Never hardcode it. This REC proves UI-driven server readback, not the Companion write transaction; ALT actions still belong in the final action-surface audit.

METERS NOW CLOSED FOR EVERY CURRENTLY AVAILABLE PATH
Newest aggregate:
- 46 total;
- 42 floor + movement closed;
- 4 floor-only;
- 0 movement-only;
- 0 neverObserved;
- 0 mismatch.

Breakdown:
- Inputs 8/8 closed;
- currently available Outputs 22/22 closed;
- Custom Mix meters 12/12 closed;
- human Outputs 21-24 remain floor-only because available=false in the current configuration: CONFIGURATION_UNAVAILABLE, not unsupported.

There are NO remaining Custom Mix meter gaps. Do not alter sample rate or Digital I/O mode merely to force Outputs 21-24 available.

CUSTOM MIX VIEW NAVIGATION IS NOT A PROTOCOL REQUIREMENT
Simply selecting another Output/Custom Mix to view in Focusrite Control does not need to emit server state. The viewed page is application UI state. The REC did observe real output source changes when several Outputs were actually routed from Playback/digital sources to Custom Mix. That is the useful protocol state. Do not chase a separate "currently viewed Custom Mix" field.

ASSIGN-MIX — STOP CHASING
assign-mix remains 26/26 SCHEMA_PRESENT and 0/26 materialised values through active Playback/Analogue/Custom Mix/digital routing. Exact semantics/write transaction remain UNKNOWN. No public/raw action exists. Do not rerun NAVIGATE_MIXES and do not write assign-mix. It is not a v1 blocker.

CUSTOM MIX / MIXER TOPOLOGY RETAINED FROM PREVIOUS BROAD REC
Previous reportVersion 6 at 05:59 UTC produced 193/193 PASS transitions, zero mismatch, strong official-UI SESSION_STATE_OBSERVED evidence for mixer-slot Stereo/Source materialisation across multiple pairs, broad Custom Mix Mute/Solo/fader/pan readback, and representative analogue/digital Output Mute/Stereo/Source readback.

Generic mixer-slot Source write remains withheld. Mixer-slot Stereo remains diagnostic-gated. Public mix_talkback remains withheld. UI readback must never be promoted into generic Companion write proof without explicit evidence.

CURRENT SESSION STATE DRIFT
The newest read-only REC did not restore user changes. At stop:
- Speaker Switching / ALT Enable remained true;
- ALT select ended false (MAIN);
- human Output 3 availability ended false while Speaker Switching remained enabled;
- several Outputs were left routed to Custom Mix;
- a previously active Custom Mix Talkback state returned to false;
- opaque Output 1/2 gain classes changed, but numeric values are intentionally not stored.
Do not claim exact restoration.

RESULT RETENTION / PRIVACY
Local testbench/results/ is intentionally gitignored. Do not upload arbitrary generated diagnostics/screenshots/raw captures. Material sanitized evidence is preserved in tracked docs with timestamp + SHA-256. New exact evidence document:
- docs/HARDWARE_VALIDATION_2026-08-26_ALT_METERS.md

Never publish serial, private hostname, client key, raw XML/captures, endpoints, private IDs, diagnostics containing private values, or user-specific paths.

USER-FACING TERMINOLOGY
Do not instruct the user with internal TestBench Mix A-F names. Use the Focusrite Control terms visible to the user: Custom Mix, Hardware Inputs, Software (DAW) Playback, Outputs, Stereo, Mute, MAIN, ALT.

PERMANENT SAFETY / FEATURE BOUNDARIES
- Scarlett 18i20 (3rd Gen) only unless real testing expands scope.
- Monitor gain item 1677 remains read-only; never re-add Monitor set/adjust actions, presets, or raw-write access without new hardware proof.
- Never invent analogue input preamp gain, direct per-input hardware mute, per-channel phantom power, Mic Kill, or physical Monitor level control.
- Focusrite Control Server TCP port and device ID are dynamic.
- Writes require Remote Devices authorization matched to this module's own server-assigned client ID.
- Feedback/state must be server-confirmed; never optimistic.
- No unknown/unsafe raw writes, firmware/reset/restore/snapshot, or meter/status writes.
- Never write explicit UNKNOWN or available=false.
- Do not update Focusrite software/firmware or alter sample rate, clocking, Digital I/O or unrelated routing merely for coverage.
- Reversible hardware tests require exact baseline/restoration; restore failure = quarantine/hard abort.
- Preserve relevant MIT/third-party attribution; do not claim all protocol knowledge was independently discovered.

PUBLICATION STATE
Repository request is already posted in Bitfocus Companion Slack #module-development. Bryce Seifert suggested focusrite-control may be the better repository scope and offered hardware. Keep public hardware scope at Scarlett 18i20 (3rd Gen) only. Wait for official repository/naming decision. Stable public target remains v1.0.0 unless maintainers direct otherwise.

IMMEDIATE NEXT ACTION
Do NOT rerun the broad REC for ALT or meters. Those readback objectives are closed for the current configuration.

Remaining material work is the PUBLIC ACTION WRITE-SURFACE AUDIT:
1. audit Companion monitor_alt / monitor_alt_enable writes against the newly proven UI behavior;
2. audit public Custom Mix writes: Mute, Solo, fader set/adjust, pan — representative exact-restorable proof or constrain/withhold unproven combinations;
3. decide v1 policy for disruptive actions device_preset, clock_source, sample_rate, spdif_mode; safest default is withhold unless deliberate approved testing is requested;
4. decide whether nickname actions need a low-risk synthetic exact-restore test or remain implemented/schema-observed only;
5. audit every Output action option still visible after hardware-policy.js filtering against retained direct-write evidence.

No work should target assign-mix, unavailable Outputs 21-24, Monitor gain 1677, firmware/reset/restore/snapshot, or forbidden non-features.

Living-state rule: after every material software/hardware/user result or blocker, update BOTH root HANDOFF and docs/CURRENT_HANDOFF.md. Pending is never PASS.
