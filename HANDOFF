FOCUSRITE CONTROL / COMPANION — UNIVERSAL AI RESUME ENTRYPOINT

Trigger word: HANDOFF

MANDATORY STARTUP FRESHNESS GATE — REPO-WIDE RECENCY FIRST
When the user says only `HANDOFF`, do not resume from chat history, uploaded files, an embedded SHA, a copied handoff, or the default branch. Resume from the LIVE repository.

Before proposing code, hardware work, release work, branch changes, or asking the user to run anything:
1. Establish the current date/time.
2. Inspect recent REMOTE branch movement across the repository, not only `main` and not only default-branch commit search results.
3. Identify the newest MATERIAL movements by commit time.
4. Choose the objective-owning branch using BOTH recency and relevance.
5. Resolve that branch's current remote HEAD and inspect newer commits/diff since the last validated checkpoint; inspect divergence/merge-base if needed.
6. Read this root `HANDOFF`, `AI_PROJECT_RULES.md`, and `docs/CURRENT_HANDOFF.md` from that LIVE ref.
7. Inspect current affected code/tests and newest sanitized validation evidence.
8. Reconcile any newer completed user/hardware result; a newer completed physical/human result outranks stale written state.
9. Only then state where the project is and choose the next action.

A default-branch commit search can miss work on another branch. An embedded SHA or document timestamp is context only, never permission to skip live Git verification. Do not guess from chat history when live Git verification is available. Any embedded SHA is context only.

OPERATOR WORKFLOW PRIORITY — PROJECT LAUNCHERS FIRST
This is a permanent usability rule.
- Prefer `UPDATE.bat` for normal branch update/sync.
- Prefer `UPDATE_AND_RUN.bat` for update + normal software validation.
- Prefer `RUN.bat` when the checkout is already current and the normal software gate is required.
- Prefer the exact `testbench\RUN_*.cmd` launcher for targeted TestBench/hardware work.
- Do NOT make the user type ad-hoc PowerShell, raw Git commands, Node commands, or one-off shell snippets when an existing project launcher already performs the operation.
- Manual shell/Git/PowerShell commands are LAST RESORT only when the launcher itself is blocked/broken or cannot expose the required information. Explain why, use the smallest recovery command possible, then return immediately to the launcher workflow.
- Do not rebuild a second tool/workflow for behavior already present in the repository. Inspect and reuse the established launcher/import/test path first.
- Keep linked-worktree handling conservative. Updating the current branch is normal. If a different selected branch is already active in another worktree, report the owning worktree and stop safely; do not auto-jump directories or attach the same branch twice.

IMMUTABLE OBJECTIVE-CONTINUITY / NO-PREMATURE-CLOSURE RULE
- Closing a sub-question never closes its parent validation objective.
- A green software gate, complete inventory, 0 FAIL, or static feedback oracle PASS never means hardware validation is complete while material EVAL_ONLY, MANUAL_PENDING, BASELINE_UNKNOWN, neverObserved, unexercised, or otherwise open rows remain.
- Before changing objectives, re-open the parent matrix/checklist and explicitly account for all remaining open matrix rows.
- A local conclusion applies only to that sub-question and does not close the parent hardware-validation objective.
- Tooling/release/publication/documentation work may interrupt an active hardware objective only when it is a direct blocker for the next safe validation step. Once the direct blocker is removed, immediately return to the parent hardware objective.
- Every objective change must state the parent objective, exact evidence that closes it, remaining open matrix rows, and why the next objective is permitted. If any are missing, the objective change is forbidden.
- For the current Scarlett 18i20 work, the parent objective remains explicit hardware feedback closure until all 31 public feedback definitions/instances have been classified and every remaining safe/actionable gap has either been tested or proven non-actionable/unsupported.

CURRENT LIVE OBJECTIVE
Branch: `testbench/meter-routing-exact-restore`
Parent objective: explicit hardware feedback closure across all 31 public feedback definitions/instances until every remaining safe/actionable gap is tested or proven non-actionable/unsupported.
Production candidate in Companion: exact audited 0.1.16.
Do not rerun FULL blindly and do not install a TestBench/debug package over 0.1.16.

LATEST COMPLETED USER TEST RESULT — 2026-08-24 17:59+02:00
Launcher: `testbench\RUN_MIX_FEEDBACK_CLOSURE.cmd`
User checkout: `9c12a4eb27fe`.

Completed evidence:
- targeted software self-check: **34/34 PASS**;
- Remote Devices / connection preflight: PASS;
- exact model Scarlett 18i20 (3rd Gen);
- existing `Companion Scarlett 18i20` client authorised;
- initial Page 2: `STALE_FOCUSRITE_TESTBENCH_HARNESS`, 769 controls, replacement-candidate YES;
- existing `PAGE2_AUTO` path: PASS;
- connection preservation: PASS, no new connection;
- Focusrite hardware writes during PAGE2_AUTO: 0;
- final read-only Page 2 verification after PAGE2_AUTO: `CURRENT_EXACT_NAME`, 768 controls, `PREP_READY`, snapshot `5a4f6d39578ea335`;
- user then explicitly confirmed `MIX_FEEDBACK` and `ALL_ISOLATED` under the launcher safety prompt.

Critical runner result:
- before the first hardware write, `MixFeedbackClosureRunner.js` correctly ran its own fresh `prepareLab()` guard;
- that fresh capture rebuilt a different snapshot-derived V8 signature and reclassified the same 768-control TestBench Page 2 as `STALE_FOCUSRITE_TESTBENCH_HARNESS`, replacement-candidate YES;
- runner returned `PREP_REQUIRED`;
- runner hardware writes: **0**;
- runner Page 2 mutations: **0**;
- hardware restore required: **NO**;
- therefore this is NOT a Scarlett failure and NOT a restore failure.

ROOT CAUSE — SNAPSHOT SIGNATURE DRIFT
`FullTestBenchPageV4.computeHarnessSignature()` hashes the public runtime snapshot, test sources and batches. `buildExtendedPageV4()` embeds that signature in the Page 2 name. `prepareLab()` only considers the page exact when its name matches the newly rebuilt snapshot-derived page name. Therefore a legitimate runtime value change between the final preparation checker and the runner can make the just-audited V8 page appear stale without any hardware write or unsafe Page 2 change.

SOURCE FIX PREPARED — PENDING USER-LOCAL VALIDATION
The Mix runner now keeps the fresh server snapshot/baselines but can accept the immediately previous V8 snapshot signature only after an additional strict read-only structural audit:
- Page 2 must already classify as `STALE_FOCUSRITE_TESTBENCH_HARNESS` with `safeReplacementCandidate=true`;
- Page 2 control count must equal the freshly built V8 batch count;
- every expected V8 position must exist;
- every control must contain exactly the expected number of actions;
- action definition families must match at every position;
- exactly one Focusrite instance may be referenced;
- module ID must be `focusrite-scarlett-18i20` and module version must be exact 0.1.16;
- the resolved live Focusrite connection must still correspond to the audited r9 connection;
- user/other/unverified pages, action-family drift, extra controls and wrong module identity remain fail-closed `PREP_REQUIRED` before playback detection or any write.

If that compatibility audit passes, the runner logs `Capability Lab Page 2 compatibility` and proceeds using the fresh server snapshot for Playback detection and exact per-lane baselines. The temporary Mix page remains mute/solo-only. At completion the runner restores a fresh audited capability-lab page rebuilt from that current snapshot.

Regression coverage added:
- `test/mix-feedback-preparation.test.js` reproduces compatible snapshot-signature drift and rejects wrong action family, extra control, wrong module and user page;
- `test/mix-feedback-closure.test.js` locks compatibility audit before Playback detection and preserves PREP_REQUIRED vs restore-failure semantics.
No GitHub commit status/CI is attached to these commits, so do not call this software-tested PASS until the user's local `[0/3]` succeeds.

UPDATE LAUNCHER STATE
A simplified updater fix also exists remotely but the user's checkout `9c12a4e` predates it. The old local updater can self-dirty/mangle worktree paths, so one last minimal bootstrap may still be required to receive the current objective branch. After that, normal `UPDATE.bat` must be used and should show a canonical repository path plus real local/remote/final HEAD values. Do not reintroduce automatic worktree directory jumping.

PERMANENT SAFETY
- Supported hardware claim only Scarlett 18i20 (3rd Gen).
- Monitor gain item 1677 read-only; never re-add Monitor set/adjust/raw write.
- No invented input preamp gain, direct per-input hardware mute, per-channel phantom switching, Mic Kill, or physical Monitor level write.
- Dynamic Focusrite Control Server TCP port and device ID only.
- Writes require authorization of this module's own server-assigned client ID.
- Feedback/state only from server-confirmed state; no optimistic success.
- No unknown/unsafe raw writes, firmware/reset/restore/snapshot commands, or writes to read-only/status/meter items.
- No write to explicit UNKNOWN output availability.
- Reuse the existing approved `Companion Scarlett 18i20` connection; do not create a second direct client merely to inspect state Companion already exposes.
- Do not update Focusrite software, firmware, routing or hardware settings outside an explicitly agreed test.

RETAINED HARDWARE EVIDENCE
- Feedback parent matrix: 31 definitions / 829 instances; static/oracle 190 PASS / 639 EVAL_ONLY / 0 FAIL; dynamic tracker 20 both-state / 12 single-state / 710 neverObserved / 0 FAIL.
- Meter closure: 14/46 paths closed; inputs 8/8, outputs 4/26, mixes 2/12, mismatch 0; hardware restore YES; Page 2 restore YES.
- Mix A L/R meter closure remains valid.
- Mix B-F meter baseline sub-question remains nonactionable because exact Playback-strip baselines are unavailable.
- Targeted Core feedback: 18/18 SKIP_BASELINE_UNKNOWN, zero writes, zero FAIL, zero restore quarantine; currently nonactionable.

EXACT IMMEDIATE NEXT STEP
1. Perform the live freshness gate and resolve the current remote HEAD of `testbench/meter-routing-exact-restore`.
2. Because local checkout `9c12a4e` still contains the pre-fix updater, use only the established minimal bootstrap if normal `UPDATE.bat` is still self-blocked; return immediately to launchers afterward.
3. Receive the current Mix snapshot-drift runner + tests.
4. Run `testbench\RUN_MIX_FEEDBACK_CLOSURE.cmd` again; do not rerun FULL/Core/SAFE/broad meter/direct probes.
5. `[0/3]` must pass completely. Current test count is expected to increase because a new strict compatibility regression was added.
6. PAGE2_AUTO may be offered if Page 2 is stale; use the existing path only when the checker says it is a recognized replacement candidate.
7. Proceed to `MIX_FEEDBACK` / `ALL_ISOLATED` only under normal launcher safety conditions.
8. In `[3/3]`, expected good path is either exact current Page 2 or `PASS Capability Lab Page 2 compatibility` before Playback detection. Any other drift remains PREP_REQUIRED with zero writes.
9. Capture the full targeted hardware summary and exact restoration status before updating the parent matrix.

Living-state rule: after every material user/software/hardware result or blocker, update both this root `HANDOFF` and `docs/CURRENT_HANDOFF.md`. Record actual validation level and do not claim pending work passed.
