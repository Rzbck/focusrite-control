FOCUSRITE CONTROL / COMPANION — UNIVERSAL AI RESUME ENTRYPOINT

Trigger word: HANDOFF

Updated: 2026-08-25 15:53+02:00
Current objective branch: testbench/meter-routing-exact-restore
Last fully validated code/test HEAD: 8cc803b714e14cd50c88e2d702470c1d9f313d06
Supported hardware claim: Scarlett 18i20 (3rd Gen) ONLY.
Parent objective: explicit hardware feedback closure across the 31 public feedback definitions / 829 instances.
Publication is NOT the current objective.

MANDATORY STARTUP FRESHNESS GATE
When the user says only HANDOFF, do not resume from chat history, copied handoffs, uploaded historical files, embedded SHAs, or the default branch. Resume from the LIVE repository.

REPO-WIDE RECENCY FIRST
Before proposing code, hardware work, release work, branch changes, or asking the user to run anything:
1. establish current date/time;
2. inspect remote branch movement across the repository, not only `main`;
3. identify the newest MATERIAL movements by commit time;
4. choose the objective-owning branch using BOTH recency and relevance;
5. resolve that branch's current remote HEAD and inspect newer commits/diff since the last validated checkpoint;
6. read live HANDOFF, AI_PROJECT_RULES.md, docs/CURRENT_HANDOFF.md, docs/PROTOCOL.md, docs/STATE_CONTRACT.md, docs/COLD_START_READBACK.md, docs/FEEDBACK_HARDWARE_CLOSURE_MATRIX.md, and relevant current source/tests/evidence;
7. reconcile any newer completed user/hardware result before choosing the next action;
8. only then continue.

A default-branch commit search can miss work on another branch. Live Git verification is mandatory. An embedded SHA is context only, never permission to skip live Git verification.

EVIDENCE ORDER / INFERENCE GATE
When information conflicts, prioritize:
1. newest explicit physical-hardware / completed user-host result;
2. current checked-in code/tests;
3. docs/CURRENT_HANDOFF.md;
4. broader current project docs;
5. older captures/assumptions.

Always distinguish OFFICIAL PRODUCT BEHAVIOUR / SCHEMA_PRESENT / SESSION_STATE_OBSERVED / IMPLEMENTED / HARDWARE_WRITE_CONFIRMED / HARDWARE_DYNAMIC_CLOSED.
UNKNOWN, blank, BASELINE_UNKNOWN, SKIP_BASELINE_UNKNOWN, sparse state, or never-observed means only not observed in that client session absent stronger evidence. Never convert missing state into unsupported/absent/impossible.

PROJECT LAUNCHERS FIRST
- UPDATE.bat = normal sync.
- UPDATE_AND_RUN.bat = sync + full local software gate.
- RUN.bat = software gate when already current.
- exact testbench\\RUN_*.cmd = targeted TestBench/hardware work.
- Manual Git/PowerShell/Node is last resort only when the normal launcher itself is broken or cannot expose the required diagnostic.

Do NOT make the user type ad-hoc PowerShell, raw Git commands, Node commands when the checked-in launcher already performs the required workflow.
Do not rebuild a second tool/workflow for behavior already present in the repository.

CURRENT USER-HOST WORKFLOW STATE
The direct UPDATE.bat branch-switch bug is CLOSED by real Windows execution. The fixed updater stayed on testbench/meter-routing-exact-restore, fetched the explicit remote branch, fast-forwarded correctly, reported local/remote HEAD correctly, and terminated without the former second-bootstrap failure.

The accidental detour to debug/official-client-passive-session is CLOSED as a workflow mistake. That branch is old/divergent and must NOT be merged or used as the current hardware-validation base.

Local TestBench recovery is complete: 127 ignored result files + 8 generated artifacts were restored, no tracked TestBench source required recovery, and git status stayed clean.

LATEST FULLY GREEN SOFTWARE CHECKPOINT
The user ran UPDATE_AND_RUN.bat on exact local HEAD 8cc803b714e14cd50c88e2d702470c1d9f313d06.
Observed:
- Node 22.23.2;
- Yarn 4.17.0 via Corepack;
- dependencies PASS;
- Prettier PASS;
- ESLint PASS;
- source manifest PASS;
- Node tests 250/250 PASS / 0 FAIL;
- Companion package build PASS;
- package focusrite-scarlett-18i20-0.1.19.tgz built only, not installed/activated by the gate;
- no hardware write.

This closes the fresh current-HEAD software blocker for the corrected meter-routing operator surfaces. The immediately previous executable checkpoint 4915b9e64d712fcf03f2d7d2e52fcda8f886de88 had already passed the same 250/250 software gate and was the checkout used for the latest read-only meter hardware observation. Subsequent handoff-only commits that record the 8cc result do not change src/, protocol logic, or TestBench hardware behavior and do not require replacing local 8cc before the next targeted read-only meter pass.

LATEST HARDWARE RESULT — READ-ONLY METER V2
On the previous fully validated local checkout 4915b9e64d712fcf03f2d7d2e52fcda8f886de88, the user ran testbench\\RUN_METER_FEEDBACK_CLOSURE.cmd.
Preflight PASS:
- Scarlett 18i20 (3rd Gen);
- module 0.1.19;
- existing Companion Focusrite connection authorised;
- 46 meter paths with rendered-feedback + independent numeric oracle.

The harness performed no Focusrite write, no Companion button press, and no routing change.
Final v2 session result:
- 21/46 PASS_FLOOR_AND_MOVEMENT;
- 17 floor-only;
- 8 movement-only;
- 0 never-observed;
- 0 mismatch.

Per-family v2 session:
- input_meter: 1/8 closed, 7 floor-only;
- output_meter: 16/26 closed, 10 floor-only;
- mix_meter: 4/12 closed, 8 movement-only.

Do NOT downgrade the parent input_meter evidence to 1/8. Earlier stronger hardware evidence already closed input_meter 8/8. Retained aggregate meter closure is therefore:
- input_meter: 8/8 HARDWARE_DYNAMIC_CLOSED;
- output_meter: 16/26 HARDWARE_DYNAMIC_CLOSED, 10 open;
- mix_meter: 4/12 HARDWARE_DYNAMIC_CLOSED, 8 open.

Exact output floor-only residuals: Outputs 14, 16, 17, 18, 19, 20, 21, 22, 23, 24.
Outputs 21-24 remain no-write because availability is UNKNOWN.
Exact mix movement-only residuals: Mix B L/R, Mix C L/R, Mix D L/R, Mix E right, Mix F right. These need floor evidence, not more SIGNAL. Prefer read-only SILENT only if their existing sources can be stopped without changing Focusrite routing; otherwise leave MANUAL_PENDING.

The sanitized accumulator is local only at testbench\\results\\LATEST_METER_FEEDBACK_CLOSURE.json. Review before any publication.

CURRENT DIRECT BLOCKER BEFORE WRITE-CAPABLE METER ROUTING — CLOSED IN SOFTWARE
The old operator mismatch in testbench\\RUN_METER_ROUTING_EXACT_RESTORE.cmd and METER_ROUTING_EXACT_RESTORE.md pinned package 0.1.16 even though the executable preflight derives EXPECTED_MODULE_VERSION from package.json, currently 0.1.19.

The narrow correction is now software-gated green on exact HEAD 8cc803b714e14cd50c88e2d702470c1d9f313d06:
- launcher requires the existing connection and module corresponding to package.json, and explicitly says not to recreate the Focusrite connection;
- guide follows package.json/current research 0.1.19 instead of 0.1.16;
- meter-routing regression forbids the stale 0.1.16 launcher pin and requires package.json-backed wording;
- MeterRoutingClosure.js hardware logic was NOT changed;
- full current-HEAD gate: 250/250 tests + package build PASS.

This removes the software blocker only. It does not itself grant hardware permission or close any hardware row.

WRITE-CAPABLE METER ROUTING SAFETY
The existing campaign is limited to already-audited Companion actions and exact restoration:
- temporary mixer strip gain/mute/solo and validated output_pair_source only;
- no direct Focusrite Control Server <set>;
- no Mixer Slot Source or Mixer Slot Stereo write;
- no direct pair-owned right-member Source write;
- no output write for UNKNOWN/UNAVAILABLE availability;
- no Monitor gain 1677;
- no Advanced Raw;
- no firmware/reset/restore/snapshot/device-preset/clock/sample-rate/SPDIF/meter writes;
- exact server-confirmed baselines and exact restore required;
- physical Monitor/headphone/speaker isolation confirmations required;
- restore failure or Page 2 restore failure = hard abort/quarantine.

LATEST COMPLETED READ-ONLY ASSIGN-MIX HARDWARE OBSERVATION
0.1.19 RUN_METER_MIX_BASELINE_READONLY.cmd completed:
- assign-mix schema present 26/26 outputs;
- current value observed 0/26, all UNKNOWN[never-observed];
- no hardware write, Companion press, Page 2 replacement, or routing change.

Classification: SCHEMA_PRESENT; read-only instrumentation IMPLEMENTED; exact value semantics UNKNOWN; official write transaction semantics UNKNOWN; public/raw write surface ABSENT.
Do not interpret 0/26 as unsupported/false. Do not rerun NAVIGATE_MIXES.

CURRENT ROUTING / MIX EVIDENCE
Latest guarded Mix-A-via-source test on non-Monitor Line Outputs 3-4:
- exact original source baseline Playback 3 + Playback 4;
- one real pair-aware source write attempted toward Mix A;
- no server-confirmed Mix A transition;
- exact Playback 3/4 state server-confirmed restored;
- classification WRITE_ATTEMPTED / NO_CONFIRMED_TRANSITION / EXACT_RESTORE_CONFIRMED for that operation only.
Do not generalize into a global output_pair_source failure and do not repeat the same Mix-A-via-source attempt blindly.

Retained strong Mix evidence:
- Mix A Left Mute: HARDWARE_DYNAMIC_CLOSED false → true → false with exact restore;
- Mix A Left Solo: HARDWARE_DYNAMIC_CLOSED false → true → false with exact restore;
- Mix A Right direct Mute/Solo under tested stereo topology: no transition, exact restore;
- Mix B-F remain readback/baseline-open where exact state was not observed.

NO DIRECT ASSIGN-MIX WRITE
Direct assign-mix write remains blocked by restoration evidence, not declared unsupported. The changed property has never been server-observed on the proposed target, so there is no exact baseline/restoration oracle.
Do not add an assign-mix action, preset, Advanced Raw write, or speculative raw write.

OBJECTIVE-CONTINUITY / NO PREMATURE CLOSURE
Closing a sub-question never closes its parent validation objective. In particular, one tooling fix, one research hypothesis, one meter family, or one green software gate does not close the parent hardware-validation objective while material EVAL_ONLY, MANUAL_PENDING, BASELINE_UNKNOWN, neverObserved, unexercised, or otherwise open rows remain.

Tooling/release/documentation work may interrupt the hardware objective only when it is a direct blocker for the next safe validation step. Once that direct blocker is removed, return to the parent hardware objective.
Before any objective change, re-open the parent matrix and account for the remaining open matrix rows. If those rows and the evidence closing the current objective cannot be stated, the objective change is forbidden.

REMOTE DEVICES AUTHORIZATION — BEFORE ANY WRITE
Focusrite Control → Device Settings → Remote Devices must show the existing Companion Scarlett 18i20 approved.
Reuse the existing Companion Focusrite connection. Do not delete/recreate it for testing. Authorization must match this module's own server-assigned client ID. Missing approval = AUTHORIZATION/PREFLIGHT BLOCKED, not hardware failure. Never reuse/copy the Companion private client key into another process. Direct Control Server research clients must never run concurrently with normal Companion write-capable validation.
Read docs/REMOTE_DEVICES_AUTHORIZATION.md before any write-capable hardware campaign.
No extra direct clients by default.

PERMANENT SAFETY
- Scarlett 18i20 (3rd Gen) only.
- Monitor gain item 1677 is read-only; never re-add Monitor set/adjust actions, presets, or raw-write access without new hardware proof.
- Never invent analogue input preamp gain, direct per-input hardware mute, per-channel phantom, Mic Kill, or physical Monitor level control.
- Focusrite Control Server TCP port and device ID are dynamic; never hardcode.
- Writes require Remote Devices authorization matched to this module's own server-assigned client ID.
- Feedback/state must be server-confirmed; no optimistic success.
- No unsafe/unknown raw item writes, firmware/reset/restore/snapshot commands, or meter/status writes.
- Never write to explicit UNKNOWN output availability.
- Do not update Focusrite software/firmware or unrelated routing/hardware settings without explicit agreement.
- Reversible tests require exact baseline/restore for the property being changed; restore failure = quarantine/hard abort.
- Never publish real serials, private hostname, client key, raw private XML/captures, diagnostics, private IDs, or user-specific paths.
- Preserve relevant MIT/third-party attribution; do not claim all protocol knowledge was independently discovered.

NO GITHUB ACTIONS IN THIS DEVELOPMENT REPOSITORY
Validation is local through the checked-in launchers and Node/Yarn gate. Follow official Bitfocus CI only once an official Bitfocus repository exists and maintainers require it.

PUBLICATION STATE
Repository/naming request is already posted in Bitfocus Companion Slack #module-development. Bryce Seifert suggested focusrite-control may be a better repository/module scope because the transport is Focusrite Control Server and offered hardware for future testing. Validated scope remains Scarlett 18i20 (3rd Gen) only. Wait for the official Bitfocus repository/naming decision before changing public scope. Stable public release target remains v1.0.0 unless maintainers direct otherwise.

IMMEDIATE NEXT ACTION
1. Keep the exact validated local checkout 8cc803b714e14cd50c88e2d702470c1d9f313d06 for the next targeted read-only pass; do not resync merely for handoff-only commits.
2. Without changing Focusrite routing, stop the existing playback/physical signals feeding the currently active Mix paths if that can be done safely outside Focusrite routing.
3. Run testbench\\RUN_METER_FEEDBACK_CLOSURE.cmd.
4. Enter SILENT once the relevant sources are actually quiet, then enter DONE. Do not add another SIGNAL pass for the eight Mix residuals; they already have movement evidence.
5. If the eight Mix lanes do not reach -128 dBFS without changing Focusrite routing, leave them MANUAL_PENDING rather than forcing a route change merely for score.
6. Only after reviewing that read-only result should the write-capable meter-routing campaign be considered for available output residuals 14 and 16-20. Outputs 21-24 receive no writes while availability remains UNKNOWN.
7. Before any write-capable meter routing, allow its read-only preparation to complete first; then lower physical Monitor, isolate speakers/headphones, verify the existing authorised connection, and explicitly confirm ROUTE_METERS then ALL_ISOLATED.
8. Any hardware or Page 2 restoration failure hard-aborts/quarantines further campaigns.
9. Do not rerun NAVIGATE_MIXES, do not write assign-mix, and do not repeat Mix-A-via-source blindly.

Living-state rule: after every material software/hardware/user result or blocker, update BOTH root HANDOFF and docs/CURRENT_HANDOFF.md. Pending work is never PASS.
