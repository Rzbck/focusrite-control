FOCUSRITE CONTROL / COMPANION — UNIVERSAL AI RESUME ENTRYPOINT

Trigger word: HANDOFF

Updated: 2026-08-25 18:07+02:00
Current objective branch: testbench/meter-routing-exact-restore
Last fully validated code/test HEAD: 41e6afdf3e816074e807b1ca4a1c2ec0a717e4a4
Supported hardware claim: Scarlett 18i20 (3rd Gen) ONLY.
Parent objective: explicit hardware feedback closure across the 31 public feedback definitions / 829 instances.
Publication is NOT the current objective.

MANDATORY STARTUP FRESHNESS GATE
When the user says only HANDOFF, do not resume from chat history, copied handoffs, uploaded historical files, embedded SHAs, or the default branch. Resume from the LIVE repository.

REPO-WIDE RECENCY FIRST
Before proposing code, hardware work, release work, branch changes, or asking the user to run anything:
1. establish current date/time;
2. inspect remote branch movement across the repository, not only `main`;
3. identify the newest MATERIAL movements by commit time;
4. choose the objective-owning branch using BOTH recency and relevance;
5. resolve that branch's current remote HEAD and inspect newer commits/diff since the last validated checkpoint;
6. read live HANDOFF, AI_PROJECT_RULES.md, docs/CURRENT_HANDOFF.md, docs/PROTOCOL.md, docs/STATE_CONTRACT.md, docs/COLD_START_READBACK.md, docs/FEEDBACK_HARDWARE_CLOSURE_MATRIX.md, and relevant current source/tests/evidence;
7. reconcile any newer completed user/hardware result before choosing the next action;
8. only then continue.

A default-branch commit search can miss work on another branch. Live Git verification is mandatory. An embedded SHA is context only, never permission to skip live Git verification.

EVIDENCE ORDER / INFERENCE GATE
When information conflicts, prioritize:
1. newest explicit physical-hardware / completed user-host result;
2. current checked-in code/tests;
3. docs/CURRENT_HANDOFF.md;
4. broader current project docs;
5. older captures/assumptions.

Always distinguish OFFICIAL PRODUCT BEHAVIOUR / SCHEMA_PRESENT / SESSION_STATE_OBSERVED / IMPLEMENTED / HARDWARE_WRITE_CONFIRMED / HARDWARE_DYNAMIC_CLOSED.
UNKNOWN, blank, BASELINE_UNKNOWN, SKIP_BASELINE_UNKNOWN, sparse state, or never-observed means only not observed in that client session absent stronger evidence. Never convert missing state into unsupported/absent/impossible.

PROJECT LAUNCHERS FIRST
- UPDATE.bat = normal sync.
- UPDATE_AND_RUN.bat = sync + full local software gate.
- RUN.bat = software gate when already current.
- exact testbench\\RUN_*.cmd = targeted TestBench/hardware work.
- Manual Git/PowerShell/Node is last resort only when the normal launcher itself is broken or cannot expose the required diagnostic.

Do NOT make the user type ad-hoc PowerShell, raw Git commands, Node commands when the checked-in launcher already performs the required workflow. Do not rebuild a second tool/workflow for behavior already present in the repository.

LATEST FULLY GREEN SOFTWARE CHECKPOINT
The user ran UPDATE_AND_RUN.bat on exact local HEAD 41e6afdf3e816074e807b1ca4a1c2ec0a717e4a4.
Observed:
- Node 22.23.2;
- Yarn 4.17.0 via Corepack;
- dependencies PASS;
- Prettier PASS;
- ESLint PASS;
- source manifest PASS;
- Node tests 256/256 PASS / 0 FAIL;
- Companion package build PASS;
- package focusrite-scarlett-18i20-0.1.19.tgz built only, not installed/activated by the gate;
- no hardware test and no hardware write occurred.

This supersedes the older 8cc803b software checkpoint. The ManualFeedbackSweep implementation, continuous 46-meter observer, VB-Audio Matrix separation/merge logic, safety regressions and living-handoff contracts are software-gate validated on the user host at 41e6afd.

LATEST HARDWARE RESULT — FREE MANUAL SWEEP / METER EVIDENCE
The user then ran the manual sweep and freely moved Scarlett / Focusrite Control controls while VB-Audio Matrix audio was active. The harness itself remained read-only: hardwareWritesByHarness=false and companionButtonPressesByHarness=false.

The uploaded sanitized report `LATEST_MANUAL_FEEDBACK_SWEEP.json` is valid and records:
- model Scarlett 18i20 (3rd Gen), module 0.1.19;
- 829 feedback probes / 31 definitions;
- 783 non-meter probes + 46 meter probes;
- priorMeterEvidenceLoaded=46;
- `steps: []` because the operator did not use the old label/CAPTURE/RESTORED prompts while moving controls freely;
- therefore NO non-meter Air/Pad/Mute/etc. transition may be claimed from this run;
- the free control movements are not recoverable from this report and the user must NOT be asked to repeat the whole session merely to satisfy the old prompt design.

Meter observation DID run continuously and produced strong new hardware evidence:
- total 46;
- closed PASS_FLOOR_AND_MOVEMENT = 35/46;
- floor-only = 4;
- movement-only = 7;
- never-observed = 0;
- mismatch = 0;
- roughly 943-952 samples per meter path.

Retained aggregate meter closure is now:
- input_meter: 8/8 HARDWARE_DYNAMIC_CLOSED;
- output_meter: 22/26 HARDWARE_DYNAMIC_CLOSED;
- mix_meter: 5/12 HARDWARE_DYNAMIC_CLOSED;
- persistent meter feedback/oracle mismatch: 0.

Exact output status after this sweep:
- Outputs 1-20: PASS_FLOOR_AND_MOVEMENT;
- Outputs 21-24: floor-only, max=-128, no movement observed;
- Outputs 25-26: PASS_FLOOR_AND_MOVEMENT.

This newly closes Output 14 and Outputs 16-20, which were the previously useful write-capable meter-routing candidates. Outputs 21-24 remain no-write because availability is UNKNOWN. Therefore there is currently NO justified output-meter routing write target left.

Exact Mix status after this sweep:
- closed: Mix A L/R, Mix D right, Mix E left, Mix F left;
- movement-only: Mix B L/R, Mix C L/R, Mix D left, Mix E right, Mix F right;
- seven Mix meter paths remain open for missing floor evidence.

Do NOT rerun RUN_METER_FEEDBACK_CLOSURE.cmd under unchanged conditions. Do NOT ask the user to repeat the just-completed free manual sweep. Any future meter work must be justified only by the seven residual Mix paths; do not re-exercise already-closed outputs.

MANUAL FEEDBACK SWEEP — IMPORTANT DESIGN LIMITATION DISCOVERED
The current checked-in ManualFeedbackSweep is SOFTWARE-GREEN but its non-meter attribution workflow expects explicit label -> CAPTURE -> RESTORED interaction. That interaction did not match the user's intended free-running “leave it open while I move everything” workflow.

Classification of the completed run:
- meters: HARDWARE OBSERVATION USEFUL / 35 of 46 closed / 0 mismatch;
- non-meter controls: NOT CAPTURED BY THIS REPORT because `steps` is empty;
- this is a harness UX/design mismatch, not evidence that the non-meter feedbacks failed;
- do not infer unsupported/broken from `steps: []`.

If a future passive/free control logger is implemented, it should automatically poll/render changed non-meter feedback markers plus their server-variable oracle with timestamps and no per-control prompts. Do not make that tooling rewrite the user's immediate burden and do not require the user to repeat this whole session.

CURRENT DIRECT SOFTWARE BLOCKER BEFORE ANY WRITE-CAPABLE METER RUN
The old broad MeterRoutingClosure.js still sweeps every snapshot Mix lane and every eligible output pair. The latest free sweep materially changes the residual target set:
- all previously useful output targets 14 and 16-20 are now CLOSED;
- Outputs 21-24 remain excluded while availability is UNKNOWN;
- only seven Mix residuals remain: Mix B L/R, Mix C L/R, Mix D left, Mix E right, Mix F right;
- already-closed lanes/pairs must not be re-exercised merely for coverage score.

Do NOT run the broad current RUN_METER_ROUTING_EXACT_RESTORE.cmd as-is. Reevaluate whether any write-capable meter campaign is worthwhile at all before implementing it.

REMOTE DEVICES AUTHORIZATION — BEFORE ANY WRITE
Read docs/REMOTE_DEVICES_AUTHORIZATION.md before any write-capable hardware campaign.
Focusrite Control → Device Settings → Remote Devices must show the existing Companion Scarlett 18i20 approved.
Reuse the existing Companion Focusrite connection. Do not delete/recreate it for testing. Authorization must match this module's own server-assigned client ID. Missing approval = AUTHORIZATION/PREFLIGHT BLOCKED, not hardware failure. Never reuse/copy the Companion private client key into another process. No extra direct clients by default. Direct Control Server research clients must never run concurrently with normal Companion write-capable validation.

LATEST COMPLETED READ-ONLY ASSIGN-MIX OBSERVATION
- assign-mix schema present 26/26 outputs;
- current value observed 0/26, all UNKNOWN[never-observed] in that session;
- no hardware write, Companion press, Page 2 replacement, or routing change.
Classification: SCHEMA_PRESENT; read-only instrumentation IMPLEMENTED; exact value semantics UNKNOWN; official write transaction semantics UNKNOWN; public/raw write surface ABSENT.
Do not interpret 0/26 as unsupported/false. Do not rerun NAVIGATE_MIXES. Do not write assign-mix.

RETAINED ROUTING / MIX EVIDENCE
Latest guarded Mix-A-via-source test on non-Monitor Line Outputs 3-4:
- exact original source baseline Playback 3 + Playback 4;
- one pair-aware write attempted toward Mix A;
- no server-confirmed Mix A transition;
- exact Playback 3/4 state restored;
- classification WRITE_ATTEMPTED / NO_CONFIRMED_TRANSITION / EXACT_RESTORE_CONFIRMED for that operation only.
Do not generalize globally and do not repeat Mix-A-via-source blindly.

Retained strong Mix evidence:
- Mix A Left Mute: HARDWARE_DYNAMIC_CLOSED false → true → false with exact restore;
- Mix A Left Solo: HARDWARE_DYNAMIC_CLOSED false → true → false with exact restore;
- Mix A Right direct Mute/Solo under tested stereo topology: no transition, exact restore;
- Mix B-F remain open where exact non-meter baselines were not observed.

OBJECTIVE-CONTINUITY / NO PREMATURE CLOSURE
Closing a sub-question never closes its parent validation objective. One tooling fix, one research hypothesis, one meter family, or one green software gate does not close the parent hardware-validation objective while material EVAL_ONLY, MANUAL_PENDING, BASELINE_UNKNOWN, neverObserved, unexercised, or otherwise open rows remain.

Tooling/release/documentation work may interrupt the hardware objective only when it is a direct blocker for the next safe validation step. Once that direct blocker is removed, return to the parent hardware objective. Before any objective change, re-open the parent matrix and account for the remaining open matrix rows. If those rows and the evidence closing the current objective cannot be stated, the objective change is forbidden.

PERMANENT SAFETY
- Scarlett 18i20 (3rd Gen) only.
- Monitor gain item 1677 is read-only; never re-add Monitor set/adjust actions, presets, or raw-write access without new hardware proof.
- Never invent analogue input preamp gain, direct per-input hardware mute, per-channel phantom, Mic Kill, or physical Monitor level control.
- Focusrite Control Server TCP port and device ID are dynamic; never hardcode.
- Writes require Remote Devices authorization matched to this module's own server-assigned client ID.
- Feedback/state must be server-confirmed; no optimistic success.
- No unsafe/unknown raw item writes, firmware/reset/restore/snapshot commands, or meter/status writes.
- Never write to explicit UNKNOWN output availability.
- Do not update Focusrite software/firmware or unrelated routing/hardware settings without explicit agreement.
- Reversible tests require exact baseline/restore; restore failure = quarantine/hard abort.
- Never publish real serials, private hostname, client key, raw private XML/captures, diagnostics, private IDs, or user-specific paths.
- Preserve relevant MIT/third-party attribution; do not claim all protocol knowledge was independently discovered.

NO GITHUB ACTIONS IN THIS DEVELOPMENT REPOSITORY
Validation is local through the checked-in launchers and Node/Yarn gate. Follow official Bitfocus CI only once an official repository exists and maintainers require it.

PUBLICATION STATE
Repository/naming request is already posted in Bitfocus Companion Slack #module-development. Bryce Seifert suggested focusrite-control may be a better repository/module scope because the transport is Focusrite Control Server and offered hardware for future testing. Validated scope remains Scarlett 18i20 (3rd Gen) only. Wait for the official Bitfocus repository/naming decision before changing public scope. Stable public release target remains v1.0.0 unless maintainers direct otherwise.

IMMEDIATE NEXT ACTION
1. Do NOT ask the user to repeat the completed free manual sweep.
2. Do NOT rerun RUN_METER_FEEDBACK_CLOSURE.cmd under unchanged conditions.
3. Do NOT run the broad current RUN_METER_ROUTING_EXACT_RESTORE.cmd as-is.
4. Treat meter evidence from the uploaded manual-sweep report as retained: input 8/8, output 22/26, mix 5/12, mismatch 0.
5. Output-meter write-capable targeting is no longer justified: 14 and 16-20 are closed; 21-24 stay no-write UNKNOWN availability.
6. The only remaining meter residuals are seven Mix paths needing floor: B L/R, C L/R, D left, E right, F right.
7. The free non-meter control movements were not captured (`steps: []`); do not claim PASS/FAIL from this run for Air/Pad/Mute/etc.
8. If further non-meter evidence is truly necessary, first redesign the logger to be automatic/free-running and decide whether existing retained hardware evidence is already sufficient before asking the user for any new manual work.
9. Keep Device Preset, Clock Source, Sample Rate, S/PDIF, firmware/reset/restore/snapshot and Monitor gain 1677 excluded.

Living-state rule: after every material software/hardware/user result or blocker, update BOTH root HANDOFF and docs/CURRENT_HANDOFF.md. Pending work is never PASS.
