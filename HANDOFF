FOCUSRITE CONTROL / COMPANION — UNIVERSAL AI RESUME ENTRYPOINT

Trigger word: HANDOFF
Updated: 2026-08-26
Current objective branch: testbench/meter-routing-exact-restore
Current development build: 0.1.20
Supported hardware claim: Scarlett 18i20 (3rd Gen) ONLY.

MANDATORY STARTUP FRESHNESS GATE
When resuming, verify the LIVE repository first. Inspect repository-wide branch movement, resolve the current remote HEAD of the objective-owning branch, inspect newer commits/diff, then read this file, docs/CURRENT_HANDOFF.md, docs/PUBLIC_ACTION_SURFACE_AUDIT_2026-08-26.md, docs/FEEDBACK_HARDWARE_CLOSURE_MATRIX.md, and relevant current source/tests/evidence.

Evidence order: newest explicit physical hardware/user-host result -> completed direct-write evidence/current code/tests -> current handoff -> matrix/docs -> older captures/assumptions.

Always distinguish HARDWARE_DYNAMIC_CLOSED, HARDWARE_WRITE_CONFIRMED, SESSION_STATE_OBSERVED, SCHEMA_PRESENT, IMPLEMENTED, RESEARCH_ONLY, CONFIGURATION_UNAVAILABLE, UNKNOWN, and UNSUPPORTED. UNKNOWN/blank/neverObserved is never proof of absence.

PROJECT LAUNCHERS FIRST
- UPDATE.bat = normal sync.
- UPDATE_AND_RUN.bat = sync + full local software gate.
- RUN.bat = software gate when already current.
- exact testbench\RUN_*.cmd = targeted TestBench/hardware work.
- manual Git/PowerShell/Node = last resort only.

CURRENT OBJECTIVE
The broad hardware feedback/protocol investigation is CLOSED FOR THE V1 SCOPE by explicit evidence or deliberate write withholding.

A post-audit V1 RELEASE SMOKE exposed one real runtime regression in the restrictive 0.1.20 Output definition lifecycle: Output actions could be filtered while server-confirmed availability was still unknown during cold start and were not rebuilt when availability later materialised. The smoke produced 39 Output NO_TRANSITION results while Input/Device/Monitor actions worked and the global exact-restore audit passed. This was treated as a module runtime defect, not as new hardware evidence against the Output write paths.

The lifecycle repair is implemented in src/main.js and regression-covered by test/output-definition-refresh.test.js. It refreshes actions/presets in a debounced way at ready and when Output availability changes; ordinary state packets do not cause definition rebuilds. It does not broaden the public write surface or bypass availability safety.

GREEN SOFTWARE/PACKAGE CHECKPOINT FOR THE REPAIR
Exact tested code/package checkpoint:
05a6c1801d75012fef864358c2f80c3758934ad7

User-host UPDATE_AND_RUN.bat on 2026-08-26 completed successfully:
- Node 22.23.2 PASS;
- Yarn 4.17.0 PASS;
- immutable dependencies PASS;
- Prettier PASS;
- ESLint PASS;
- source manifest PASS;
- 295/295 Node tests PASS;
- Companion package build PASS;
- focusrite-scarlett-18i20-0.1.20.tgz generated.

The new lifecycle regressions explicitly passed:
- ready plus initial Output availability materialisation refreshes the filtered write surface once;
- ordinary state does not rebuild definitions but later Output availability changes do.

No Focusrite hardware write was performed by this software gate.

IMPORTANT ARTIFACT STATUS
The older exact artifact audit with SHA-256 cfa4ba62c11e2a91780122eb38a0a0570d6122e0c5fc7d91652008a6838a5716 belongs to the earlier fd76b4e6... package and is HISTORICAL for the repaired runtime.

Because src/main.js changed after that audit, the newly generated 05a6c180... focusrite-scarlett-18i20-0.1.20.tgz must receive a new exact package/privacy/forbidden-feature audit before the repaired RC is called final. Do not reuse the old archive SHA as proof for the new package.

CURRENT PROJECT STATE
SOFTWARE GATE GREEN FOR THE OUTPUT-LIFECYCLE REPAIR.

NEXT REQUIRED VALIDATION IS ONE TARGETED V1 RELEASE SMOKE USING THE NEWLY GENERATED 0.1.20 PACKAGE, followed by a fresh exact artifact audit.

Do not run another broad REC or FULL campaign. Do not broaden hardware scope.

EXACT NEXT ACTION
1. In Companion, import the newly generated focusrite-scarlett-18i20-0.1.20.tgz from checkpoint 05a6c1801d75012fef864358c2f80c3758934ad7.
2. Keep/reuse the existing Focusrite connection and select Module Version 0.1.20.
3. Run the normal read-only preflight first so the existing module client/Remote Devices authorisation is confirmed.
4. Then run the canonical V1 release smoke V3 only.
5. Preserve its exact restoration contract. No manual state reset is required before the smoke merely because the prior smoke restored successfully.
6. If the smoke is clean, repeat the exact package/privacy/forbidden-feature audit on the NEW .tgz and then close the repaired RC.

Do not ask for unrelated hardware manipulation, REC, sample-rate changes, Digital I/O changes, firmware/software updates, or additional broad coverage.

0.1.20 GATE HISTORY
- first attempt: dependencies PASS, Prettier blocked on two formatting-only files; later fixed;
- second attempt: Prettier/ESLint/manifest PASS, 273/282 tests PASS with 9 stale-policy/history regressions; all diagnosed and repaired;
- third attempt: Prettier-only failure on src/definition-policy.js formatting; fixed;
- fourth attempt: 282/283 tests PASS, one brittle HANDOFF wording assertion; fixed;
- fd76b4e6... then achieved 283/283 + package PASS and its exact archive audit passed;
- subsequent real V1 RELEASE SMOKE exposed the cold-start Output-definition lifecycle defect despite the synthetic package audit;
- 05a6c180... repairs that lifecycle and now passes the complete user-host gate at 295/295 + package PASS;
- targeted hardware smoke and fresh exact artifact audit are still required before final repaired-RC closure.

FINAL V1 PUBLIC WRITE SURFACE
Authoritative audit: docs/PUBLIC_ACTION_SURFACE_AUDIT_2026-08-26.md.

KEPT PUBLIC WRITES
Monitor:
- monitor_mute
- monitor_dim
- monitor_talkback
- monitor_preset

Hardware Inputs:
- input_air
- input_pad
- input_mode
- input_mode_cycle
- input_nickname

Outputs, filtered by exact model/evidence/server availability:
- output_mute on validated direct members only
- output_gain_set / output_gain_adjust on validated analogue gain targets
- output_source on validated direct targets/direct source families
- output_pair_source on validated pairs/direct stereo source families
- output_nickname on validated direct targets

Device/settings:
- device_nickname
- phantom_persistence
- talkback_source
- reconnect

WITHHELD PUBLIC WRITES FOR V1
Readable state may remain, but normal v1 write actions/presets are removed:
- monitor_alt_enable
- monitor_alt
- output_stereo
- mixer_slot_source
- mixer_slot_stereo
- mix_mute
- mix_solo
- mix_gain_set
- mix_gain_adjust
- mix_pan
- mix_talkback
- device_preset
- clock_source
- sample_rate
- spdif_mode
- advanced_raw_set

Withholding is a v1 write-surface decision, not an unsupported-hardware claim.

OUTPUT POLICY
- right/pair-owned members are withheld for direct Mute writes;
- pair-owned right Source is withheld from direct routing while validated pair routing remains available;
- Monitor Outputs 1-2 direct Gain withheld;
- known no-effect Gain/Nickname paths withheld;
- Output Stereo write withheld globally;
- Outputs 21-24 remain write-blocked even if a future configuration reports available=true until explicitly hardware-tested in that available configuration;
- explicit available=false / UNKNOWN blocks writes;
- action/preset definitions must refresh when server-confirmed Output availability materialises or changes, but callbacks still re-check live availability and fail closed.

CUSTOM MIX / USER TERMINOLOGY
User-facing instructions MUST use Focusrite Control terms: Custom Mix, Hardware Inputs, Software (DAW) Playback, Outputs, Stereo, Mute, MAIN, ALT. Do not ask the user to manipulate internal protocol/TestBench Mix A-F names.

Focusrite Control presents simply Custom Mix. Internal mix IDs are not reliably mapped to the visible UI. Output Source/Pair Source actions do not offer internal Custom Mix source IDs, and stale attempts are callback-blocked.

assign-mix remains:
- 26/26 SCHEMA_PRESENT;
- 0/26 materialised in active tested sessions;
- raw semantics UNKNOWN;
- write transaction UNKNOWN;
- no public action/preset/feedback;
- no raw write.

Do not rerun NAVIGATE_MIXES. Do not write assign-mix. It is not a v1 blocker.

NEWEST BROAD READBACK EVIDENCE
Latest sanitized read-only REC: 2026-08-26T06:29:16.831Z, module 0.1.19.
Fingerprint:
- size 606632 bytes;
- SHA-256 308a78f3b48391dec292f634a8eb0082ee0111da42a2977c9ea61e074bfa06f9.

Result:
- read-only harness;
- zero harness hardware writes;
- zero Companion button presses;
- 829 probes / 31 feedback definitions / 46 meters;
- 11 transitions / 11 PASS / 0 race / 0 mismatch.

ALT / SPEAKER SWITCHING
Feedback/readback is HARDWARE_DYNAMIC_CLOSED:
- monitor_alt_enable: both states, 3 PASS transitions;
- monitor_alt: both states, 4 PASS transitions;
- Output 3 availability changed with Speaker Switching ownership;
- 0 race / 0 mismatch.

ALT writes remain withheld for v1 because Companion direct-write evidence did not equivalently close those transactions.

METERS
- Inputs 8/8 closed;
- available Outputs 22/22 closed;
- Custom Mix 12/12 closed;
- total 42/46 floor + movement closed;
- Outputs 21-24 are available=false: CONFIGURATION_UNAVAILABLE, not unsupported.

No remaining meter test. Do not change Sample Rate or Digital I/O merely for coverage.

PASSIVE REC STATE RULE
A read-only/passive recorder does NOT require the user to restore Focusrite Control to its starting state merely because the final snapshot differs.

Exact baseline/restoration remains mandatory for write-capable reversible hardware tests where rollback is part of the safety contract.

PERMANENT SAFETY / FEATURE BOUNDARIES
- Scarlett 18i20 (3rd Gen) only unless real hardware testing expands scope.
- Monitor gain item 1677 read-only; never re-add Monitor Set/Adjust actions, presets or raw write without new hardware proof.
- Never invent analogue input preamp Gain, direct per-input hardware Mute, per-channel phantom power, Mic Kill or physical Monitor level control.
- Focusrite Control Server TCP port and device ID are dynamic.
- Writes only after Remote Devices authorisation matched to this module's own server-assigned client ID.
- Server-confirmed feedback/state only, never optimistic.
- No write to UNKNOWN or explicit available=false.
- No unknown/unsafe raw writes, firmware/reset/restore/snapshot or meter/status writes.
- Do not update Focusrite software/firmware without explicit agreement.
- Preserve privacy and required MIT/third-party attribution; do not claim all protocol knowledge was independently discovered.

PUBLICATION STATE
Repository request is already posted in Bitfocus Companion Slack #module-development. Bryce Seifert suggested focusrite-control may be the better repository scope and offered hardware for future testing.

Keep public hardware scope at Scarlett 18i20 (3rd Gen) only. Do not rename/broaden public scope before the official Bitfocus repository/naming decision.

WHEN THE OFFICIAL BITFOCUS REPOSITORY EXISTS
1. inspect exact repo name, default branch, seed files and permissions;
2. compare with the cleaned current RC;
3. use expected branch/PR workflow rather than overwriting;
4. run Bitfocus CI and local tests;
5. keep stable target v1.0.0 unless maintainers direct otherwise;
6. only submit the Developer Portal tag after official CI and hardware/action audit are clean.
