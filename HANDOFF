FOCUSRITE CONTROL / COMPANION — UNIVERSAL AI RESUME ENTRYPOINT

Trigger word: HANDOFF
Updated: 2026-08-25
Current objective branch: testbench/meter-routing-exact-restore
Supported hardware claim: Scarlett 18i20 (3rd Gen) ONLY.
Parent objective: explicit hardware feedback/protocol closure before public release.
Publication is NOT the current objective.

MANDATORY STARTUP FRESHNESS GATE
When the user says HANDOFF or asks to resume, verify the LIVE repository first. Do not resume from chat history, copied handoffs, uploaded historical files, embedded SHAs, or main alone. Resolve the current remote HEAD of the objective-owning branch, inspect newer commits/diff, then read this file plus docs/CURRENT_HANDOFF.md and relevant current source/tests/evidence. Reconcile any newer completed user/hardware result before choosing the next action. An embedded SHA is context only, never permission to skip live Git verification.

REPO-WIDE RECENCY FIRST
Before proposing code, hardware work, release work, branch changes, or asking the user to run anything:
1. inspect remote branch movement across the repository, not only `main`;
2. identify the newest MATERIAL movements by commit time;
3. choose the objective-owning branch using BOTH recency and relevance;
4. resolve that branch's current remote HEAD and inspect newer commits/diff since the last validated checkpoint;
5. reconcile any newer completed physical/human result before continuing.
A default-branch commit search can miss work on another branch. Live Git verification is mandatory.

EVIDENCE ORDER
When information conflicts, prioritize:
1. newest explicit physical-hardware / completed user-host result;
2. current checked-in code/tests;
3. docs/CURRENT_HANDOFF.md;
4. docs/FEEDBACK_HARDWARE_CLOSURE_MATRIX.md and broader current docs;
5. older captures/assumptions.

Always distinguish HARDWARE_DYNAMIC_CLOSED, HARDWARE_WRITE_CONFIRMED, SESSION_STATE_OBSERVED, SCHEMA_PRESENT, IMPLEMENTED, RESEARCH_ONLY, CONFIGURATION_UNAVAILABLE, UNKNOWN, and UNSUPPORTED. UNKNOWN/blank/neverObserved is never proof of absence.

PROJECT LAUNCHERS FIRST
- UPDATE.bat = normal sync.
- UPDATE_AND_RUN.bat = sync + full local software gate.
- RUN.bat = software gate when already current.
- exact testbench\RUN_*.cmd = targeted TestBench/hardware work.
- manual Git/PowerShell/Node is last resort only when a checked-in launcher is broken or insufficient.

Do NOT make the user type ad-hoc PowerShell, raw Git commands, Node commands when the checked-in launcher already performs the required workflow.
Do not rebuild a second tool/workflow for behavior already present in the repository.

TEXT-WRITE HYGIENE
After modifying tracked text through GitHub, preserve the final newline and verify the resulting file/expected formatting before asking the user to rerun the full gate. The user-host gate must not be used merely to discover avoidable formatting mistakes.

OBJECTIVE-CONTINUITY / NO PREMATURE CLOSURE
Closing a sub-question never closes its parent validation objective. A tooling fix, one research hypothesis, one meter family, one green software gate, or one solved routing sub-question does not close the parent hardware-validation objective while material EVAL_ONLY, MANUAL_PENDING, BASELINE_UNKNOWN, neverObserved, unexercised, or otherwise open rows remain.
Tooling/release/documentation work may interrupt the hardware objective only when it is a direct blocker for the next safe validation step. Once that direct blocker is removed, return to the parent hardware objective. Before any objective change, re-open the parent matrix and account for the remaining open matrix rows. If those rows and the evidence closing the current objective cannot be stated, the objective change is forbidden.
The parent objective remains explicit hardware feedback closure before public release.

LATEST FULLY GREEN SOFTWARE CHECKPOINT
Exact user-host HEAD 6bbf1b3fe162b67272397e5ce82940419c59080c passed the complete local gate:
- Node 22.23.2;
- Yarn 4.17.0;
- dependencies PASS;
- Prettier PASS;
- ESLint PASS;
- source manifest PASS;
- Node tests 272/272 PASS / 0 FAIL;
- Companion package build PASS;
- package focusrite-scarlett-18i20-0.1.19.tgz;
- no hardware test/write performed by the gate.

This supersedes 9127b063 as the latest fully validated software checkpoint. The free-running Line 3-4 recorder rewrite is software-gate validated at 6bbf1b3. No src/ or production protocol/write path changed in that recorder rewrite.

LATEST COMPLETED HARDWARE/RECORDER RESULT — MANUAL FEEDBACK SWEEP
Latest reconciled sanitized reportVersion 5, updated 2026-08-25T18:11:50.399Z:
- Scarlett 18i20 (3rd Gen), module 0.1.19;
- read-only harness, zero harness hardware writes, zero harness Companion presses;
- 51 feedback transitions;
- 50 confirmed PASS;
- 1 TRANSIENT_RACE (Monitor Talkback fast reversal);
- 0 confirmed feedback mismatch.
Retained closure:
- input_air Inputs 1-8 HARDWARE_DYNAMIC_CLOSED;
- input_pad Inputs 1-8 HARDWARE_DYNAMIC_CLOSED;
- input_mode Inputs 1-2 Line/Inst HARDWARE_DYNAMIC_CLOSED;
- monitor_dim HARDWARE_DYNAMIC_CLOSED;
- monitor_mute HARDWARE_DYNAMIC_CLOSED;
- monitor_talkback retains stronger prior closure.
No broad Air/Pad/Mode/DIM/Mute retest is needed.

LATEST LINE OUTPUTS 3-4 FREE-RECORDER RESULT — COMPLETED
User-host run of testbench\RUN_OUTPUT_ROUTING_LINE34_CAPTURE.cmd completed successfully on the software-gate-validated recorder.
Safety/result:
- read-only harness; zero Focusrite writes; zero Companion button presses;
- 19 routing-state changes captured;
- final source/stereo restoration CONFIRMED;
- assign-mix materialized during REC: NO.

Baseline:
- Line Output 3: source=Playback 3, stereo=true, assignMix=UNKNOWN[never-observed];
- Line Output 4: source=None / Unassigned, stereo=false, assignMix=UNKNOWN[never-observed].

Repeated stereo unlink/relink observation:
- when Output 3 stereo changed true -> false, Output 4 source changed None / Unassigned -> Playback 4;
- when Output 3 stereo changed false -> true, Output 4 source changed Playback 4 -> None / Unassigned;
- this cycle was observed repeatedly and restored.
Classification: SESSION_STATE_OBSERVED hardware behavior for Line Outputs 3-4 in this configuration; do not generalize to every pair without another real pair observation.

Direct/source-routing observations on Line Output 3 while recording:
- Playback 3 -> Playback 1;
- Playback 1 -> Playback 19;
- Playback 19 -> Analogue 1;
- Analogue 1 -> Analogue 7;
- Analogue 7 -> Mix D L;
- Mix D L -> Playback 1;
- Playback 1 -> Playback 3;
- final baseline restore confirmed.

Important routing conclusion:
- selecting a Custom Mix route was server-visible as ordinary source readback (`sourceName=Mix D L`) on Line Output 3;
- assign-mix remained UNKNOWN[never-observed] throughout Stereo changes, multiple direct sources, Custom Mix selection, and restoration;
- therefore this tested routing path does not require inventing an assign-mix value to represent the observed Custom Mix selection;
- do not infer that assign-mix is absent from all firmware/configurations; its schema remains present but its value semantics/write transaction remain unknown.

LATEST ASSIGN-MIX READ-ONLY RESULT — ACTIVE TEST SUPERSEDES PASSIVE-ONLY EVIDENCE
- assign-mix schema/descriptor coverage remains 26/26 SCHEMA_PRESENT;
- earlier passive server-observed value coverage was 0/26;
- the targeted active Line 3-4 recorder has now exercised Stereo, multiple direct Source choices and Custom Mix while assign-mix still never materialized;
- exact raw value semantics remain UNKNOWN;
- official write transaction remains UNKNOWN;
- no public/raw assign-mix write surface exists or may be added from this evidence;
- NAVIGATE_MIXES was passive historical research only and must not be rerun for this objective.
Classification: SCHEMA_PRESENT + ACTIVE_SESSION_STATE_UNOBSERVED for the tested Line 3-4 path, not unsupported and not writable.

OUTPUT AVAILABILITY — CURRENT CONFIGURATION
Latest report observes output_available=false for zero-based options 20..23, corresponding to human Outputs 21-24 / ADAT 2.1-2.4.
Classification: CONFIGURATION_UNAVAILABLE in this configuration, NOT unsupported.
Implementation requirement:
- never hardcode Outputs 21-24 as unavailable;
- follow server-confirmed availability dynamically;
- block writes while available=false;
- if a future valid configuration reports available=true, use the same dynamic parser/policy path;
- real-hardware testing is required before claiming that future configuration hardware-tested.
Do not change sample rate or digital-I/O mode merely for coverage.

METER STATUS RETAINED
- input_meter: 8/8 HARDWARE_DYNAMIC_CLOSED;
- output_meter: 22 paths hardware-closed in the current configuration;
- Outputs 21-24 are CONFIGURATION_UNAVAILABLE, so no write-driven closure is allowed there;
- mix_meter: 6/12 HARDWARE_DYNAMIC_CLOSED;
- remaining Mix paths missing only floor evidence: Mix B L/R, Mix C L/R, Mix E R, Mix F R;
- persistent meter mismatch: 0.
Do not run broad RUN_METER_ROUTING_EXACT_RESTORE.cmd as-is.

LINE 3-4 WORKFLOW STATUS
The staged 1/6..6/6 workflow is retired and must not be used again.
The free-running recorder successfully captured the required routing behavior and restored the baseline. Do not rerun it merely to chase assign-mix: the active test already left assign-mix unobserved while source routing, including Mix D L, was clearly visible.
If the sanitized JSON report is supplied, reconcile its exact event list before any code/release change that depends on this capture.

REMOTE DEVICES AUTHORIZATION
Before any write-capable hardware campaign, Focusrite Control -> Device Settings -> Remote Devices must show the existing Companion Focusrite client approved. Authorization must match this module's own server-assigned client ID. Missing approval is AUTHORIZATION/PREFLIGHT BLOCKED, not hardware failure. Never reuse/copy the Companion client key in another process. No extra direct clients by default.

PERMANENT SAFETY / FEATURE BOUNDARIES
- Scarlett 18i20 (3rd Gen) only unless scope expands after real testing.
- Monitor gain item 1677 remains read-only. Never add Monitor set/adjust actions, presets, or raw-write access without new hardware proof.
- Never invent analogue input preamp gain.
- Never invent direct per-input hardware mute.
- Never invent per-channel phantom power.
- Never invent Mic Kill.
- Never invent physical Monitor level control.
- Focusrite Control Server TCP port and device ID are dynamic; never hardcode them.
- Writes require Remote Devices authorization matched to this module's own server-assigned client ID.
- Feedback/state must be server-confirmed; no optimistic success.
- Do not expose unknown/unsafe raw writes, firmware/reset/restore/snapshot commands, or meter/status writes.
- Never write to explicit UNKNOWN or available=false output availability.
- Do not update Focusrite software/firmware or unrelated routing/settings without explicit user agreement.
- Reversible hardware tests require exact baseline and exact restoration; restore failure = quarantine/hard abort.
- Never publish serial, private hostname, client key, raw private captures/XML/diagnostics, private IDs, or user-specific paths.
- Preserve relevant MIT/third-party attribution. Do not claim all protocol knowledge was independently discovered.

NO GITHUB ACTIONS IN THIS PERSONAL DEVELOPMENT REPOSITORY
Validation is local via checked-in Node/Yarn/Windows launchers. Use official Bitfocus CI only after an official Bitfocus repository exists and maintainers require it.

PUBLICATION STATE
Repository request already posted in Bitfocus Companion Slack #module-development. Bryce Seifert suggested focusrite-control may be the better scope because transport is Focusrite Control Server and offered hardware for future testing. Validated hardware scope remains Scarlett 18i20 (3rd Gen) only. Wait for official repository/naming decision before changing public scope. Stable public target remains v1.0.0 unless maintainers direct otherwise.

IMMEDIATE NEXT ACTION
1. Prefer receiving/reconciling testbench\results\LATEST_OUTPUT_ROUTING_LINE34_CAPTURE.json if available; do not rerun the completed Line 3-4 recorder just to reproduce the console result.
2. Treat assign-mix as active-session-unobserved and keep it read-only/research-only; do not add a write path.
3. Return to the parent hardware objective: remaining Mixer topology evidence and the six residual Mix meter floor paths.
4. Do not run another broad sweep and do not rerun NAVIGATE_MIXES.
5. Before any new write-capable campaign, use the checked-in launcher/preflight and exact-restoration guards.

Living-state rule: after every material software/hardware/user result or blocker, update BOTH root HANDOFF and docs/CURRENT_HANDOFF.md. Pending is never PASS.
