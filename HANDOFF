FOCUSRITE CONTROL / COMPANION — UNIVERSAL AI RESUME ENTRYPOINT

Trigger word: HANDOFF
Updated: 2026-08-26
Current objective branch: testbench/meter-routing-exact-restore
Current development build: 0.1.20
Supported hardware claim: Scarlett 18i20 (3rd Gen) ONLY.

MANDATORY STARTUP FRESHNESS GATE
When resuming, verify the LIVE repository first. Inspect repository-wide branch movement, resolve the current remote HEAD of the objective-owning branch, inspect newer commits/diff, then read this file, docs/CURRENT_HANDOFF.md, docs/PUBLIC_ACTION_SURFACE_AUDIT_2026-08-26.md, docs/FEEDBACK_HARDWARE_CLOSURE_MATRIX.md, and relevant current source/tests/evidence.

Evidence order: newest explicit physical hardware/user-host result -> completed direct-write evidence/current code/tests -> current handoff -> matrix/docs -> older captures/assumptions.

Always distinguish HARDWARE_DYNAMIC_CLOSED, HARDWARE_WRITE_CONFIRMED, SESSION_STATE_OBSERVED, SCHEMA_PRESENT, IMPLEMENTED, RESEARCH_ONLY, CONFIGURATION_UNAVAILABLE, UNKNOWN, and UNSUPPORTED. UNKNOWN/blank/neverObserved is never proof of absence.

PROJECT LAUNCHERS FIRST
- UPDATE.bat = normal sync.
- UPDATE_AND_RUN.bat = sync + full local software gate.
- RUN.bat = software gate when already current.
- exact testbench\RUN_*.cmd = targeted TestBench/hardware work.
- manual Git/PowerShell/Node = last resort only.

CURRENT OBJECTIVE
The broad hardware feedback/protocol investigation is CLOSED FOR THE V1 SCOPE by explicit evidence or deliberate write withholding. No further broad hardware campaign is required before the next software gate.

Current objective:
VALIDATE THE RESTRICTIVE 0.1.20 V1 PUBLIC WRITE SURFACE END-TO-END IN SOFTWARE, then perform the normal package/privacy/forbidden-feature release audit.

The objective changed only after accounting for every remaining material unproven/disruptive write family and withholding it rather than promoting readback evidence into write support.

LATEST FULLY GREEN SOFTWARE CHECKPOINT
Exact user-host HEAD e8d7e72ec5e50e42903cf8057acbeb63aaca4ba7 passed the complete local gate on 2026-08-26 for 0.1.19: Node 22.23.2, Yarn 4.17.0, dependencies PASS, Prettier PASS, ESLint PASS, source manifest PASS, 279/279 Node tests PASS, Companion package PASS, focusrite-scarlett-18i20-0.1.19.tgz. No hardware write from that gate.

CURRENT 0.1.20 SOFTWARE STATE — GATE PENDING
The public write-surface audit changed production policy and bumped package.json to 0.1.20.

Material changes:
- src/hardware-policy.js tightens direct Output policy;
- src/definition-policy.js freezes the v1 public action/preset surface;
- src/main.js removes the public Advanced Raw configuration checkbox and keeps mixer diagnostics read-only;
- test/production-output-availability-policy.test.js adds v1 policy regressions;
- companion/HELP.md, QUICKSTART_FR.md, CHANGELOG.md and current docs are reconciled.

Targeted isolated policy/syntax validation passed during the audit, including 6/6 targeted policy test groups. No physical hardware write was performed. This is NOT the repository-wide green gate.

0.1.20 remains SOFTWARE-GATE-PENDING until UPDATE_AND_RUN.bat passes dependencies, Prettier, ESLint, source manifest, ALL Node tests and Companion package build. Pending is never PASS.

NEWEST HARDWARE RESULT
Latest sanitized read-only REC: 2026-08-26T06:29:16.831Z, module 0.1.19.
Exact report fingerprint:
- size 606632 bytes;
- SHA-256 308a78f3b48391dec292f634a8eb0082ee0111da42a2977c9ea61e074bfa06f9.

Result:
- read-only harness;
- zero harness hardware writes;
- zero Companion button presses;
- 829 probes / 31 feedback definitions / 46 meters;
- duration 165060 ms;
- 11 transitions / 11 PASS / 0 race / 0 mismatch.
Tracked summary: docs/HARDWARE_VALIDATION_2026-08-26_ALT_METERS.md.

ALT / SPEAKER SWITCHING
Feedback/readback is HARDWARE_DYNAMIC_CLOSED:
- monitor_alt_enable: both states, 3 PASS transitions;
- monitor_alt: both states, 4 PASS transitions;
- human Output 3 availability changed with Speaker Switching ownership;
- 0 race / 0 mismatch.

V1 decision: keep ALT/ALT Enable feedbacks/readback, WITHHOLD their Companion write actions and ALT preset because the latest REC did not execute Companion writes and completed direct-write evidence did not close those transactions.

METERS
All currently available meter paths are closed:
- Inputs 8/8;
- available Outputs 22/22;
- Custom Mix 12/12;
- total 42/46 floor + movement closed;
- human Outputs 21-24 remain floor-only because available=false: CONFIGURATION_UNAVAILABLE, not unsupported.

No remaining meter test. Do not change sample rate or Digital I/O merely to expose Outputs 21-24.

CUSTOM MIX / USER TERMINOLOGY
User-facing instructions MUST use Focusrite Control terms: Custom Mix, Hardware Inputs, Software (DAW) Playback, Outputs, Stereo, Mute, MAIN, ALT. Do not ask the user to manipulate internal protocol/TestBench Mix A-F names.

The latest physical sessions provide strong server-confirmed readback for Custom Mix fader, pan, Mute, Solo, source/stereo topology, Talkback state and 12/12 meters.

Simply viewing another Output/Custom Mix page does not need to create server/device state. Do not chase a "currently viewed Custom Mix" field.

PASSIVE REC STATE RULE
A read-only/passive recorder does NOT require the user to restore Focusrite Control to its starting state merely because the final snapshot differs. Do not ask for passive-REC restoration unless the specific research question truly depends on a known start/end comparison.

Exact baseline/restoration remains mandatory for write-capable reversible hardware tests where rollback is part of the safety contract.

FINAL V1 PUBLIC WRITE SURFACE
Authoritative audit: docs/PUBLIC_ACTION_SURFACE_AUDIT_2026-08-26.md.

KEPT PUBLIC WRITES
Monitor:
- monitor_mute
- monitor_dim
- monitor_talkback
- monitor_preset

Hardware Inputs:
- input_air
- input_pad
- input_mode
- input_mode_cycle
- input_nickname

Outputs, filtered by exact model/evidence/server availability:
- output_mute on validated direct members only
- output_gain_set / output_gain_adjust on validated analogue gain targets
- output_source on validated direct targets and direct source families
- output_pair_source on validated pairs/direct stereo source families
- output_nickname on validated direct targets

Device/settings:
- device_nickname
- phantom_persistence
- talkback_source
- reconnect

Nickname writes need no new physical test: completed V8 evidence already contains write-confirmed input/device nickname paths and control-specific Output nickname evidence.

WITHHELD PUBLIC WRITES FOR V1
Readable state may remain, but normal v1 write actions/presets are removed:
- monitor_alt_enable
- monitor_alt
- output_stereo
- mixer_slot_source
- mixer_slot_stereo
- mix_mute
- mix_solo
- mix_gain_set
- mix_gain_adjust
- mix_pan
- mix_talkback
- device_preset
- clock_source
- sample_rate
- spdif_mode
- advanced_raw_set

This is deliberate v1 scope control, not a claim that readable capabilities do not exist.

CUSTOM MIX ROUTING WRITE DECISION
Output Source/Pair Source actions no longer offer internal Custom Mix source IDs.
Reason:
- Focusrite Control presents simply Custom Mix;
- private server exposes multiple internal mix IDs;
- assign-mix remains 26/26 schema-present but 0/26 materialised;
- there is no reliable user-visible mapping that justifies guessing which internal source corresponds to the user's Custom Mix selection.

Direct Hardware Input / Software (DAW) Playback / digital source routing remains available where hardware-tested. Stale saved actions trying an internal Custom Mix source are callback-blocked.

OUTPUT POLICY
- every right/pair-owned member is withheld for direct Mute writes;
- pair-owned right Source remains withheld from direct source writes while dedicated validated pair routing remains available;
- Monitor Outputs 1-2 direct Gain remains withheld;
- known no-effect direct Gain/Nickname paths remain withheld;
- Output Stereo write is withheld globally for v1;
- human Outputs 21-24 are write-blocked even if a future configuration later reports available=true, until that available configuration receives explicit real-hardware validation;
- explicit available=false / UNKNOWN remains a write block where an availability descriptor exists.

DISRUPTIVE SETTINGS
No further physical test is required for v1. Device Preset, Clock Source, Sample Rate and Digital I/O/S/PDIF Mode write actions are withheld instead of altering real routing/clocking merely for coverage. Readback may remain.

ADVANCED RAW
The public connection configuration no longer exposes the Advanced Raw checkbox. advanced_raw_set is removed from the v1 action/preset definitions. Hardware policy also fails closed for withheld mix/settings/ALT/Stereo raw paths. Dedicated research/TestBench workflows stay separate.

ASSIGN-MIX
Final v1 classification:
- 26/26 SCHEMA_PRESENT;
- 0/26 materialised values through active routing sessions;
- raw semantics UNKNOWN;
- official write transaction UNKNOWN;
- no public action/preset/feedback;
- no raw write.
Do not rerun NAVIGATE_MIXES; do not write assign-mix; it is not a v1 blocker.

PERMANENT SAFETY / FEATURE BOUNDARIES
- Scarlett 18i20 (3rd Gen) only unless real hardware testing expands scope.
- Monitor gain item 1677 read-only; never re-add Monitor set/adjust actions, presets or raw write without new hardware proof.
- Never invent analogue input preamp gain, direct per-input hardware mute, per-channel phantom power, Mic Kill or physical Monitor level control.
- Focusrite Control Server TCP port and device ID are dynamic.
- Writes require Remote Devices authorisation matched to this module's own server-assigned client ID.
- Feedback/state must be server-confirmed; never optimistic.
- No write to UNKNOWN or explicit available=false.
- No unknown/unsafe raw writes, firmware/reset/restore/snapshot or meter/status writes.
- Do not update Focusrite software/firmware without explicit agreement.
- Preserve privacy and required MIT/third-party attribution; do not claim all protocol knowledge was independently discovered.

RESULT RETENTION / PRIVACY
Local testbench/results/ remains intentionally gitignored. Do not publish arbitrary generated reports/screenshots/raw captures. Material sanitized evidence is preserved by tracked summary + timestamp + SHA-256. Never publish serial, private hostname, client key, endpoints, private IDs, raw private XML/captures, private diagnostics or user-specific paths.

PUBLICATION STATE
Repository request is already posted in Bitfocus Companion Slack #module-development. Bryce Seifert suggested focusrite-control may be the better repository scope and offered hardware. Keep public hardware scope at Scarlett 18i20 (3rd Gen) only. Wait for the official repository/naming decision. Stable public target remains v1.0.0 unless maintainers direct otherwise.

EXACT NEXT ACTION
Run UPDATE_AND_RUN.bat on testbench/meter-routing-exact-restore.

Require the complete 0.1.20 gate:
- dependencies;
- Prettier;
- ESLint;
- source manifest;
- ALL Node tests;
- Companion package build.

Expected package after a clean gate:
focusrite-scarlett-18i20-0.1.20.tgz

Do NOT run another broad hardware REC after that gate merely for coverage. If the software gate is clean, move directly to final package/privacy/forbidden-feature audit and official-repository readiness work.

Living-state rule: after every material result or blocker, update BOTH root HANDOFF and docs/CURRENT_HANDOFF.md. Pending is never PASS.
