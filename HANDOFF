FOCUSRITE CONTROL / COMPANION — UNIVERSAL AI RESUME ENTRYPOINT

Trigger word: HANDOFF
Updated: 2026-08-26
Current objective branch: testbench/meter-routing-exact-restore
Supported hardware claim: Scarlett 18i20 (3rd Gen) ONLY.
Parent objective: explicit hardware feedback/protocol closure before public release.
Publication is NOT the current objective.

MANDATORY STARTUP FRESHNESS GATE
When the user says HANDOFF or asks to resume, verify the LIVE repository first. Do not resume from chat history, copied handoffs, uploaded historical files, embedded SHAs, or main alone. Resolve the current remote HEAD of the objective-owning branch, inspect newer commits/diff, then read this file plus docs/CURRENT_HANDOFF.md and relevant current source/tests/evidence. Reconcile any newer completed user/hardware result before choosing the next action. An embedded SHA is context only, never permission to skip live Git verification.

REPO-WIDE RECENCY FIRST
Before proposing code, hardware work, release work, branch changes, or asking the user to run anything:
1. inspect remote branch movement across the repository, not only `main`;
2. identify the newest MATERIAL movements by commit time;
3. choose the objective-owning branch using BOTH recency and relevance;
4. resolve that branch's current remote HEAD and inspect newer commits/diff since the last validated checkpoint;
5. reconcile any newer completed physical/human or user/hardware result before continuing.
A default-branch commit search can miss work on another branch. Live Git verification is mandatory.

EVIDENCE ORDER
When information conflicts, prioritize:
1. newest explicit physical-hardware / completed user-host result;
2. current checked-in code/tests;
3. docs/CURRENT_HANDOFF.md;
4. broader current docs/matrices;
5. older captures/assumptions.
Always distinguish HARDWARE_DYNAMIC_CLOSED, HARDWARE_WRITE_CONFIRMED, SESSION_STATE_OBSERVED, SCHEMA_PRESENT, IMPLEMENTED, RESEARCH_ONLY, CONFIGURATION_UNAVAILABLE, UNKNOWN, and UNSUPPORTED. UNKNOWN/blank/neverObserved is never proof of absence.

PROJECT LAUNCHERS FIRST
- UPDATE.bat = normal sync.
- UPDATE_AND_RUN.bat = sync + full local software gate.
- RUN.bat = software gate when already current.
- exact testbench\RUN_*.cmd = targeted TestBench/hardware work.
- manual Git/PowerShell/Node is last resort only when a checked-in launcher is broken or insufficient.

Do NOT make the user type ad-hoc PowerShell, raw Git commands, Node commands when the checked-in launcher already performs the required workflow.
Do not rebuild a second tool/workflow for behavior already present in the repository.

TEXT-WRITE HYGIENE
After modifying tracked text through GitHub, preserve the final newline and verify the resulting file/expected formatting before asking the user to rerun the full gate. The user-host gate must not be used merely to discover avoidable formatting mistakes.

OBJECTIVE-CONTINUITY / NO PREMATURE CLOSURE
Closing a sub-question never closes its parent validation objective. A tooling fix, one research hypothesis, one meter family, one green software gate, or one solved routing sub-question does not close the parent hardware-validation objective while material EVAL_ONLY, MANUAL_PENDING, BASELINE_UNKNOWN, neverObserved, unexercised, or otherwise open rows remain.
Tooling/release/documentation work may interrupt the hardware objective only when it is a direct blocker for the next safe validation step. Once that direct blocker is removed, return to the parent hardware objective. Before any objective change, re-open the parent matrix and account for the remaining open matrix rows. If those rows and the evidence closing the current objective cannot be stated, the objective change is forbidden.
The parent objective remains explicit hardware feedback closure before public release.

LATEST FULLY GREEN SOFTWARE CHECKPOINT
Exact user-host HEAD e8d7e72ec5e50e42903cf8057acbeb63aaca4ba7 passed the complete local gate on 2026-08-26:
- Node 22.23.2;
- Yarn 4.17.0;
- dependencies PASS;
- Prettier 3.9.6 PASS;
- ESLint PASS;
- source manifest PASS;
- Node tests 279/279 PASS / 0 FAIL;
- Companion package build PASS;
- package focusrite-scarlett-18i20-0.1.19.tgz;
- no hardware test/write performed by the gate.
This supersedes 6bbf1b3 as the latest fully validated software checkpoint. The broad REC extension is now SOFTWARE-GATE-VALIDATED. No src/ or production protocol/write path changed in this TestBench extension.

BROAD REC ASPIRATEUR — READY FOR HARDWARE SESSION
Reuse the existing single launcher testbench\RUN_MANUAL_FEEDBACK_SWEEP.cmd. Do not create or teach a second workflow.
The same REC session observes:
- all 783 public non-meter feedback probes;
- all 46 meters;
- parallel safe semantic diagnostics derived from the existing matrix.
Semantic diagnostics include output availability/mute/stereo/source-name/gain classes and assign-mix class/provenance, mixer-slot source-name/stereo, Mix lane/slot gain/pan as opaque V1/V2/... equality classes, Mix talkback, and safe Monitor/device state.
Recognized source names such as Playback/Analogue/Mix remain semantic. Numeric unresolved source IDs are masked as UNRESOLVED_SOURCE. Raw gain/pan/control values, raw mixer-slot source IDs, nicknames, serial/host/endpoint/client/device identity, raw XML, diagnostics and user paths are not published/stored as private material.
The harness itself performs zero Focusrite writes and zero Companion button presses. User clicks in Focusrite Control still change hardware.

LATEST COMPLETED HARDWARE/RECORDER RESULT — MANUAL FEEDBACK SWEEP
Latest reconciled sanitized reportVersion 5 before the new broad REC:
- Scarlett 18i20 (3rd Gen), module 0.1.19;
- 51 feedback transitions;
- 50 confirmed PASS;
- 1 TRANSIENT_RACE (Monitor Talkback fast reversal);
- 0 confirmed feedback mismatch.
Retained closure: input Air 1-8, Pad 1-8, Input Mode 1-2, Monitor DIM, Monitor Mute, and prior stronger Monitor Talkback evidence. No broad retest of those families is needed.

LATEST LINE OUTPUTS 3-4 FREE-RECORDER RESULT — COMPLETED
Read-only recorder captured 19 routing-state changes with final source/stereo restoration confirmed and no assign-mix materialization.
Baseline: Output 3 Playback 3 / stereo=true; Output 4 None / Unassigned / stereo=false; both assignMix UNKNOWN[never-observed].
Repeated unlink/relink behavior: Output 3 stereo true->false accompanied Output 4 source None->Playback 4, and relink restored Output 4 to None.
Output 3 direct/source observations included Playback 1, Playback 19, Analogue 1, Analogue 7, Mix D L, and exact return to Playback 3.
Custom Mix selection was server-visible as sourceName=Mix D L while assign-mix remained never observed.
Classification: SESSION_STATE_OBSERVED for Line 3-4; do not generalize every pair without another real observation.

ASSIGN-MIX STATUS
- 26/26 descriptor/schema coverage = SCHEMA_PRESENT;
- active Line 3-4 source/stereo/Custom Mix exercise still produced no assign-mix value;
- exact raw semantics UNKNOWN;
- official write transaction UNKNOWN;
- no public/raw assign-mix write surface may be added;
- NAVIGATE_MIXES is historical passive research and must not be rerun.
Classification: SCHEMA_PRESENT + ACTIVE_SESSION_STATE_UNOBSERVED for tested Line 3-4, not unsupported and not writable.

OUTPUT AVAILABILITY — CURRENT CONFIGURATION
Outputs 21-24 / ADAT 2.1-2.4 are server-confirmed available=false in the current configuration: CONFIGURATION_UNAVAILABLE, not unsupported.
Never hardcode that state. Follow server-confirmed availability dynamically and never write while false/UNKNOWN. Do not change sample rate or digital mode merely for coverage.

METER STATUS RETAINED
- input_meter: 8/8 HARDWARE_DYNAMIC_CLOSED;
- output_meter: 22 paths hardware-closed in the current configuration;
- Outputs 21-24 configuration-unavailable;
- mix_meter: 6/12 closed;
- residual floor-only need: Mix B L/R, Mix C L/R, Mix E R, Mix F R;
- persistent mismatch: 0.
Do not run broad RUN_METER_ROUTING_EXACT_RESTORE.cmd as-is.

REMAINING OPEN MATRIX FOCUS
The broad REC must leave all safe possibilities open rather than prescribe only one family. Relevant remaining/open families include mixer_slot_stereo/source topology, Mix mute/solo instances, Mix gain/pan semantic transitions, eligible output mute/stereo/source/gain behavior, Monitor ALT/ALT Enable when physically isolated, and the six residual Mix meter floors. mix_talkback remains withheld where transaction semantics are not established.

REMOTE DEVICES AUTHORIZATION
Before any write-capable hardware campaign, Focusrite Control -> Device Settings -> Remote Devices must show the existing Companion Scarlett 18i20 client approved. Reuse the existing Companion Focusrite connection. Missing approval is AUTHORIZATION/PREFLIGHT BLOCKED, not hardware failure. Approval must match this module's own server-assigned client ID. No extra direct clients by default. Never reuse/copy the Companion private client key into another process. Read docs/REMOTE_DEVICES_AUTHORIZATION.md when needed.

PERMANENT SAFETY / FEATURE BOUNDARIES
- Scarlett 18i20 (3rd Gen) only unless real testing expands scope.
- Monitor gain item 1677 is read-only; never re-add Monitor set/adjust actions, presets, or raw-write access without new hardware proof.
- Never invent analogue input preamp gain, direct per-input hardware mute, per-channel phantom power, Mic Kill, or physical Monitor level control.
- Focusrite Control Server TCP port and device ID are dynamic; never hardcode them.
- Writes require Remote Devices authorization matched to this module's own server-assigned client ID.
- Feedback/state must be server-confirmed; never optimistic.
- No unknown/unsafe raw writes, firmware/reset/restore/snapshot commands, or meter/status writes.
- Never write to explicit UNKNOWN or available=false.
- Do not update Focusrite software/firmware or unrelated routing/settings without explicit agreement.
- Reversible hardware tests require exact baseline/restoration; restore failure = quarantine/hard abort.
- Never publish serial, private hostname, client key, raw private captures/XML/diagnostics, private IDs, or user-specific paths.
- Preserve relevant MIT/third-party attribution; do not claim all protocol knowledge was independently discovered.

NO GITHUB ACTIONS IN THIS PERSONAL DEVELOPMENT REPOSITORY
Validation is local via checked-in Node/Yarn/Windows launchers. Official Bitfocus CI only after the official repository exists.

PUBLICATION STATE
Repository request is already posted in Bitfocus Companion Slack #module-development. Bryce Seifert suggested focusrite-control may be the better scope and offered hardware. Keep public hardware scope at Scarlett 18i20 (3rd Gen) only. Wait for official repository/naming decision. Stable public target remains v1.0.0 unless maintainers direct otherwise.

IMMEDIATE NEXT ACTION
1. Do NOT rerun UPDATE_AND_RUN merely because handoff docs move after the green e8d7e72 checkpoint; no production/TestBench code changed after that green gate.
2. Run testbench\RUN_MANUAL_FEEDBACK_SWEEP.cmd.
3. Before REC ON, physically isolate/quiet speakers, headphones and sensitive outputs.
4. During REC ON, freely explore the remaining safe clickable controls with no required order; leave each state about 2 seconds.
5. Do not touch Device Preset, Clock Source, Sample Rate, S/PDIF mode, firmware/reset/restore/snapshot, Monitor gain 1677, or outputs currently unavailable. Nicknames are intentionally ignored.
6. Include a few seconds of silence where practical for the residual Mix meter floors.
7. Stop with Enter and provide testbench\results\LATEST_MANUAL_FEEDBACK_SWEEP.json.
8. Reconcile everything actually observed before deciding what remains. Do not rerun NAVIGATE_MIXES.

Living-state rule: after every material software/hardware/user result or blocker, update BOTH root HANDOFF and docs/CURRENT_HANDOFF.md. Pending is never PASS.
