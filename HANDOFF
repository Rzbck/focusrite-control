FOCUSRITE CONTROL / COMPANION — UNIVERSAL AI RESUME ENTRYPOINT

Trigger word: HANDOFF

MANDATORY STARTUP FRESHNESS GATE — REPO-WIDE RECENCY FIRST
When the user says only `HANDOFF`, do not resume from chat history, uploaded files, an embedded SHA, a copied handoff, or the default branch. Resume from the LIVE repository.

Before proposing code, hardware work, release work, branch changes, or asking the user to run anything:
1. Establish the current date/time.
2. Inspect recent REMOTE branch movement across the repository, not only `main` and not only default-branch commit search results.
3. Identify the newest MATERIAL movements by commit time.
4. Choose the objective-owning branch using BOTH recency and relevance.
5. Resolve that branch's current remote HEAD and inspect newer commits/diff since the last validated checkpoint; inspect divergence/merge-base if needed.
6. Read this root `HANDOFF`, `AI_PROJECT_RULES.md`, and `docs/CURRENT_HANDOFF.md` from that LIVE ref.
7. Inspect current affected code/tests and newest sanitized validation evidence.
8. Reconcile any newer completed user/hardware result; a newer completed physical/human result outranks stale written state.
9. Only then state where the project is and choose the next action.

A default-branch commit search can miss work on another branch. An embedded SHA or document timestamp is context only, never permission to skip live Git verification. Do not guess from chat history when live Git verification is available.

MANDATORY EVIDENCE / INFERENCE GATE — NEVER TURN UNKNOWN INTO UNSUPPORTED
Before declaring a control unsupported, non-actionable, closed, fake, absent, or impossible because a value is missing, the AI MUST read the live versions of:
- `docs/PROTOCOL.md`;
- `docs/STATE_CONTRACT.md`;
- `docs/COLD_START_READBACK.md`;
- the relevant current schema/parser/action/feedback code;
- the current hardware matrix/evidence for that control.

Then explicitly separate these evidence levels:
1. OFFICIAL PRODUCT BEHAVIOUR — Focusrite documentation for the exact generation/model;
2. SCHEMA_PRESENT — Control Server declares the item/control;
3. SESSION_STATE_OBSERVED — current value was actually observed in device-arrival or later `<set>` state;
4. IMPLEMENTED — module code exposes/uses the control;
5. HARDWARE_WRITE_CONFIRMED — an approved physical test proved the write path;
6. HARDWARE_DYNAMIC_CLOSED — action + server-confirmed feedback + exact restore were physically exercised.

`UNKNOWN`, blank variable, missing cache entry, `BASELINE_UNKNOWN`, `SKIP_BASELINE_UNKNOWN`, or a sparse initial snapshot means only `not observed in this client session` unless stronger evidence proves otherwise. It is NOT evidence that the schema item is absent, the value is false, the function is unsupported, or the hardware cannot perform it.

If older physical/session evidence contradicts current cache coverage, classify the question as READBACK/MATERIALISATION RESEARCH OPEN until reconciled. Never erase contradictory evidence to simplify the matrix.

A reversible hardware test must require only the server-confirmed state genuinely necessary for exact restoration of the property being changed. Do not invent an oversized prerequisite tuple for convenience. Related properties may be observed for collateral effects without automatically becoming restoration prerequisites.

OPERATOR WORKFLOW PRIORITY — PROJECT LAUNCHERS FIRST
- Prefer `UPDATE.bat` for normal branch update/sync.
- Prefer `UPDATE_AND_RUN.bat` for update + normal software validation.
- Prefer `RUN.bat` when the checkout is already current and the normal software gate is required.
- Prefer the exact `testbench\RUN_*.cmd` launcher for targeted TestBench/hardware work.
- Do NOT make the user type ad-hoc PowerShell, raw Git commands, Node commands, or one-off shell snippets when an existing project launcher already performs the operation.
- Manual shell/Git/PowerShell commands are LAST RESORT only when the launcher itself is blocked/broken or cannot expose the required information.
- Do not rebuild a second tool/workflow for behavior already present in the repository.
- Keep linked-worktree handling conservative. If a different selected branch is already active in another worktree, report the owning worktree and stop safely; do not attach the same branch twice.
- Exception for a stale updater bootstrap: if the running updater predates the remote updater fix, has already fetched the current remote branch, and cannot replace/update itself safely, one minimal `git restore --source=origin/<branch> -- UPDATE.bat` is allowed solely to restore the launcher. Immediately return to `UPDATE_AND_RUN.bat`; do not use this exception for general repository manipulation.

IMMUTABLE OBJECTIVE-CONTINUITY / NO-PREMATURE-CLOSURE RULE
- Closing a sub-question never closes its parent validation objective.
- A green software gate, complete inventory, 0 FAIL, or static feedback oracle PASS never means hardware validation is complete while material EVAL_ONLY, MANUAL_PENDING, BASELINE_UNKNOWN, neverObserved, unexercised, or otherwise open rows remain.
- Before changing objectives, re-open the parent matrix/checklist and explicitly account for all remaining open matrix rows.
- A local conclusion applies only to that sub-question and does not close the parent hardware-validation objective.
- Tooling/release/publication/documentation work may interrupt an active hardware objective only when it is a direct blocker for the next safe validation step. Once the blocker is removed, immediately return to the parent hardware objective.
- For the current Scarlett 18i20 work, the parent objective remains explicit hardware feedback closure until all 31 public feedback definitions/instances have been classified and every remaining safe/actionable gap has either been tested or genuinely proven non-actionable/unsupported.

CURRENT LIVE OBJECTIVE
Branch: `testbench/meter-routing-exact-restore`
Parent objective: explicit hardware feedback closure.
Canonical production candidate currently in Companion: exact audited **0.1.16**.
Readback-provenance research build in source: **0.1.17 — pending local software gate; not loaded in Companion**.
Do not rerun FULL blindly.

LATEST UPDATE BOOTSTRAP BLOCKER — 2026-08-24 19:43+02:00
The user ran the normal updater from a stale local checkout immediately before the updater worktree fix.

Observed:
- local branch was `testbench/meter-routing-exact-restore`;
- remote fetch successfully advanced the remote-tracking ref from local checkpoint `9c12a4e` to then-live remote `303c3fd`;
- the running local updater printed `HEAD local: UNKNOWN` and `HEAD distant: UNKNOWN` even though fetch/branch detection worked;
- it falsely treated the branch-owning worktree as a different worktree and printed `Bascule automatique` to the same logical worktree;
- local `UPDATE.bat` was detected modified and was saved in a safety stash;
- the old updater then reported dirty state remaining after stash and stopped;
- no merge/reset was performed;
- no hardware, Companion Page2, Focusrite state, routing, software or firmware was changed.

Root cause is source-known, not speculative:
- local checkpoint `9c12a4e` is immediately followed by `efbd738` (`fix: simplify updater worktree handling and restore canonical LF blob`);
- that fix removes auto-jumping and resolves the canonical Git root before dirty-state decisions;
- current remote `UPDATE.bat` already contains the conservative fixed behavior and a regression test forbids `Bascule automatique vers`.

Recovery classification:
- the normal launcher is itself the blocked component, so the permanent LAST RESORT exception applies;
- because the remote branch was already fetched successfully, the smallest safe recovery is to restore only tracked `UPDATE.bat` from `origin/testbench/meter-routing-exact-restore`;
- do NOT pop the safety stash now; it preserves the pre-recovery local updater state;
- after restoring the launcher, immediately return to `UPDATE_AND_RUN.bat` and normal launcher workflow.

RESEARCH CORRECTION — MIX MUTE/SOLO — 2026-08-24
The earlier conclusion that `mix_mute` / `mix_solo` were closed as `EVAL_ONLY_NONACTIONABLE` was too strong and is RETRACTED.

The completed targeted run proved only:
- exact 18i20 / authorised existing Companion client / Page2 preparation passed;
- Playback dynamically detected as slot 3 / Playback 1 / stereo;
- complete gain+mute+solo tuple available on 0/12 lanes in that session;
- 24 `SKIP_BASELINE_UNKNOWN`;
- hardware writes 0; FAIL 0; restore quarantine 0.

It did NOT prove Mix Mute/Solo are unsupported or inherently non-actionable.

Evidence keeping these controls open:
- official Focusrite Scarlett 18i20 3rd Gen documentation confirms per-Custom-Mix channel Mute/Solo and the 12-mono-mix / 24-input custom-mix shape;
- current 18i20 Control Server schema exposes distinct per-strip `gain`, `pan`, `mute`, `solo` IDs;
- current actions use the explicit mute/solo IDs, not gain aliases;
- independent older Control Server research corroborates separate mixer controls and sparse/partial state materialisation, but remains research-only for this hardware;
- earlier normal Companion evidence saw Mix A L/R Playback-strip gain/mute/solo all KNOWN/exact, while the later targeted session saw 0/12 complete tuples.

Current status:
- `mix_mute`: **RESEARCH_OPEN / EVAL_ONLY**;
- `mix_solo`: **RESEARCH_OPEN / EVAL_ONLY**.

CURRENT STATE MODEL
`device-arrival` schema declaration and observed current state are separate evidence. `src/device-parser.js` only seeds `initialState` from explicit `value=` attributes. `src/focusrite-client.js` then builds the current cache from those initial values and later `<set>` messages. Missing cache value means not observed in this session, not unsupported.

READBACK-PROVENANCE IMPLEMENTATION — SOURCE COMPLETE, VALIDATION PENDING
The direct blocker work has now been implemented by extending existing infrastructure only.

Implemented:
- `src/focusrite-client.js`: separate per-item observation provenance; `arrival`, `set`, `arrival+set`, or never observed; normal `getValue()` semantics unchanged; no write/auth behavior changed.
- `src/variables.js`: Mix gain/mute/solo provenance variables exposed only under the existing `Expose all mixer slot variables` diagnostic option.
- `testbench/MeterMixPlaybackBaselineReadOnlyProbe.js`: existing read-only probe extended to report schema/value/provenance state; no button press, no Focusrite write, no routing mutation; sanitized report stores no raw values/private IDs.
- `test/state-provenance.test.js`: new regression coverage.
- `test/meter-mix-playback-baseline-readonly.test.js`: provenance/privacy/no-write regression coverage.
- `package.json`: research build version **0.1.17**, intentionally distinct from audited 0.1.16.
- `yarn.lock`: no package-version edit required because the workspace entry is `0.0.0-use.local`.

VALIDATION STATUS — DO NOT CLAIM PASS YET
- source implementation: IMPLEMENTED;
- tests: WRITTEN, NOT YET EXECUTED ON THE WINDOWS PROJECT HOST;
- Prettier / ESLint / manifest / full Node tests / Companion package build: PENDING;
- 0.1.17 import/activation in Companion: NOT DONE;
- hardware writes caused by this work: 0;
- Focusrite software/firmware/routing changes: 0.

The current AI runtime could not execute the repository-local toolchain because the project/dependencies were not mounted and external network/dependency resolution was unavailable. This is not a PASS or FAIL of the source.

EXACT IMMEDIATE NEXT STEP
The updater itself is currently the blocker. Do not run hardware or the provenance probe.

One-time LAST RESORT bootstrap, from a terminal opened in the objective repository folder:

`git restore --source=origin/testbench/meter-routing-exact-restore -- UPDATE.bat`

This restores only `UPDATE.bat` from the remote-tracking branch that was already fetched. It does not merge, reset the branch, pop the stash, or touch hardware.

Then immediately return to the normal launcher and run:

`UPDATE_AND_RUN.bat`

Continue branch:

`testbench/meter-routing-exact-restore`

Purpose:
- sync current live HEAD;
- immutable dependency check;
- Prettier check;
- ESLint;
- source manifest check;
- full Node tests including provenance regressions;
- build the distinct 0.1.17 Companion package.

`UPDATE_AND_RUN.bat`/`RUN.bat` does not install the module in Companion and performs no Focusrite hardware write.

If and only if that gate is green:
1. import/select the 0.1.17 module build in Companion;
2. rerun normal read-only preflight;
3. run only `testbench\RUN_METER_MIX_BASELINE_READONLY.cmd`;
4. during the probe navigate only Mix A-F, changing no controls;
5. use the provenance result to explain Mix A L/R KNOWN versus later 0/12;
6. then redesign Mute and Solo closure independently, with only genuinely required exact-restoration state.

No write is permitted merely to manufacture an unknown baseline.

RETAINED PARENT EVIDENCE
- 31 definitions / 829 instances;
- static/oracle 190 PASS / 639 EVAL_ONLY / 0 FAIL;
- dynamic 20 both-state / 12 single-state / 710 neverObserved / 0 FAIL;
- meter closure 14/46: inputs 8/8, outputs 4/26, mixes 2/12, mismatch 0;
- targeted Core: 18/18 SKIP_BASELINE_UNKNOWN, zero writes/FAIL/restore quarantine — a readback observation, not proof of capability absence.

PERMANENT SAFETY
- Supported hardware claim only Scarlett 18i20 (3rd Gen).
- Monitor gain item 1677 read-only.
- No invented input preamp gain, direct per-input hardware mute, per-channel phantom switching, Mic Kill, or physical Monitor level write.
- Dynamic Focusrite Control Server TCP port and device ID only.
- Writes require authorization of this module's own server-assigned client ID.
- Feedback/state only from server-confirmed state; no optimistic success.
- No unknown/unsafe raw writes, firmware/reset/restore/snapshot commands, or writes to read-only/status/meter items.
- No write to explicit UNKNOWN output availability.
- Reuse the existing approved Companion connection; no second direct client by default; never copy/reuse its private client key.
- Do not update Focusrite software, firmware, routing or hardware settings outside an explicitly agreed test.

Living-state rule: after every material user/software/hardware result or blocker, update both this root `HANDOFF` and `docs/CURRENT_HANDOFF.md`. Record actual validation level and do not claim pending work passed.
