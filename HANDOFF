FOCUSRITE CONTROL / COMPANION — UNIVERSAL AI RESUME ENTRYPOINT

Trigger word: HANDOFF

MANDATORY STARTUP FRESHNESS GATE — REPO-WIDE RECENCY FIRST
When the user says only `HANDOFF`, do not resume from chat history, uploaded files, copied handoffs, an embedded SHA, or the default branch. Resume from the LIVE repository.

Before proposing code, hardware work, release work, branch changes, or asking the user to run anything:
1. Establish current date/time.
2. Inspect recent remote branch movement across the repository, not only `main`.
3. Identify the newest MATERIAL movements by commit time.
4. Choose the objective-owning branch using BOTH recency and relevance.
5. Resolve that branch's current remote HEAD and inspect newer commits/diff since the last validated checkpoint.
6. Read live `HANDOFF`, `AI_PROJECT_RULES.md`, `docs/CURRENT_HANDOFF.md`, `docs/PROTOCOL.md`, `docs/STATE_CONTRACT.md`, `docs/COLD_START_READBACK.md`, `docs/FEEDBACK_HARDWARE_CLOSURE_MATRIX.md`, and relevant current source/tests/evidence.
7. Reconcile any newer completed user/hardware result before choosing the next action; newer completed physical/human evidence outranks stale written state.
8. Only then choose the next action.

A default-branch commit search can miss work on another branch. An embedded SHA is context only and is never permission to skip live Git verification.

MANDATORY EVIDENCE / INFERENCE GATE — NEVER TURN UNKNOWN INTO UNSUPPORTED
Keep separate: OFFICIAL PRODUCT BEHAVIOUR / SCHEMA_PRESENT / SESSION_STATE_OBSERVED / IMPLEMENTED / HARDWARE_WRITE_CONFIRMED / HARDWARE_DYNAMIC_CLOSED.
`UNKNOWN`, blank, missing cache, `BASELINE_UNKNOWN`, `SKIP_BASELINE_UNKNOWN`, or sparse initial state means only not observed in this client session absent stronger evidence. It is not proof that a control is false, absent, unsupported or impossible.

PROJECT LAUNCHERS FIRST
- `UPDATE.bat` for normal sync.
- `UPDATE_AND_RUN.bat` for update + normal software gate.
- `RUN.bat` when already current.
- exact `testbench\RUN_*.cmd` launcher for targeted TestBench/hardware work.
- Manual shell/Git/PowerShell/Node is last resort only when the normal launcher is itself broken or cannot expose the required diagnostic.
- Do NOT make the user type ad-hoc PowerShell, raw Git commands, Node commands when the normal launcher workflow can do the work.
- Do not rebuild a second tool/workflow for behavior already present in the repository.

IMMUTABLE OBJECTIVE-CONTINUITY / NO-PREMATURE-CLOSURE RULE
Closing a sub-question never closes its parent validation objective. Parent objective remains explicit hardware feedback closure across all 31 public feedback definitions/instances while material `EVAL_ONLY`, `MANUAL_PENDING`, `BASELINE_UNKNOWN`, `neverObserved`, unexercised or otherwise open rows remain.
- Tooling, release, documentation or repository work may interrupt hardware validation only when it is a direct blocker for the next safe validation step.
- Once that direct blocker is removed, return to the parent hardware objective.
- Before changing objectives, explicitly account for remaining open matrix rows.
- objective change is forbidden while relevant remaining open matrix rows exist unless the user explicitly changes the project objective.

Remote Devices authorization — mandatory before any write
- Focusrite Control → Device Settings → Remote Devices must show the existing `Companion Scarlett 18i20` client approved before any write-capable hardware test.
- Always reuse the existing Companion Focusrite connection; do not delete/recreate it merely to obtain another client identity.
- Missing approval is `AUTHORIZATION/PREFLIGHT BLOCKED`, not a hardware-control failure.
- No extra direct clients by default.
- Never reuse/copy the Companion private client key into another process.
- See `docs/REMOTE_DEVICES_AUTHORIZATION.md`.

CURRENT LIVE OBJECTIVE
Branch: `testbench/meter-routing-exact-restore`.
Supported hardware claim: Scarlett 18i20 (3rd Gen) only.
Parent objective: explicit hardware feedback closure.
Canonical production candidate remains audited 0.1.16.
Research 0.1.17 is software validated, packaged and real-hardware exercised.
Research 0.1.18 module/package checkpoint: `d6df45c59ab825e1ebccae90d98212b561449feb` — dependencies PASS, Prettier PASS, ESLint PASS, source manifest PASS, Node tests 225/225 PASS, package build PASS `focusrite-scarlett-18i20-0.1.18.tgz`.
The operator imported/selected 0.1.18 on the EXISTING authorised Companion connection and enabled the mixer diagnostic variables.

LATEST FULL SOFTWARE GATE — GREEN
User-host `UPDATE_AND_RUN.bat` at source HEAD `e0a477d401b2` completed:
- dependencies PASS;
- Prettier PASS;
- ESLint PASS;
- source manifest PASS;
- Node tests 230/230 PASS;
- package build PASS `focusrite-scarlett-18i20-0.1.18.tgz`;
- no hardware write occurred during the gate.

LATEST MIX ATTEMPT — NO-OP SAFE BEFORE WRITE
Immediately after that green gate, `RUN_MIX_FEEDBACK_CLOSURE.cmd` ran with 0.1.18 selected:
- targeted self-check 63/63 PASS;
- exact Scarlett 18i20 Gen3 / existing Companion connection / Remote Devices authorization PASS;
- Page 2 already current with 768 audited controls; no PAGE2 mutation needed;
- after `MIX_FEEDBACK` + `ALL_ISOLATED`, bootstrap stopped with `No unique adjacent confirmed-mono Playback pair is available for autonomous materialisation.`;
- hardware writes 0;
- no topology write, no Mix Mute/Solo write, no restore incident.

This NO-OP is a TestBench target-selection blocker, not hardware evidence about mono/stereo capability. Multiple mono Playback pairs were live and `loadPriorPlaybackHint()` depended only on the local `LATEST_MIX_FEEDBACK_CLOSURE.json`, so the bootstrap could lose the earlier Playback 1 target hint.

TESTBENCH-ONLY TARGET FIX IMPLEMENTED AFTER THE GREEN CHECKPOINT
No module `src/` code changed.
`MixTopologyMaterialize.js` now resolves the target in this order:
1. prior sanitized closure target if still live;
2. otherwise one unique runtime `Playback 1` with its adjacent runtime `Playback 2`, both confirmed mono;
3. otherwise one unique adjacent mono Playback pair;
4. otherwise STOP / no write.

The Playback 1 anchor is campaign-specific and semantic: no slot number is hardcoded and it is not a universal hardware rule. Duplicate Playback 1 candidates remain ambiguous and fail closed. New regressions cover multiple mono pairs with one Playback 1 anchor and duplicate Playback 1 refusal.

These latest TestBench/test changes are SOURCE_IMPLEMENTED but USER-HOST REVALIDATION PENDING. Do not run hardware until the normal gate is green again.

LATEST STRONG MIX HARDWARE RESULT RETAINED FROM 0.1.17
Playback slot 3 / Playback 1 stereo:
- Mix A Left Mute: HARDWARE_DYNAMIC_CLOSED, server variable + rendered feedback false→true→false, exact restore.
- Mix A Left Solo: HARDWARE_DYNAMIC_CLOSED, same full closure.
- Mix A Right Mute direct write: no transition under that tested stereo topology; exact baseline restored.
- Mix A Right Solo direct write: same.
- Mix B-F: 20 `SKIP_BASELINE_UNKNOWN`.
- restore quarantine 0; hardware restore YES; Page 2 restore YES.
Do not infer a global Right-lane ownership/unsupported rule from that stereo-only result.

RUNTIME MONO/STEREO CORRECTION
UI_OBSERVED evidence shows runtime mono/stereo presentation for Software Playback and other source families where available. Current known operator state remains Playback 1 + Playback 2 mono unless newer live evidence says otherwise. Old SINGLE-ITEM `mixer_slot_stereo` no-effect evidence does not prove capability absence. `mixer_slot_stereo` and `mixer_slot_source` remain RESEARCH_OPEN / EVAL_ONLY; public/raw writes remain withheld.

CURRENT MIX STATUS
- `mix_mute`: PARTIAL.
- `mix_solo`: PARTIAL.
- `mixer_slot_stereo`: RESEARCH_OPEN; grouped pair semantics hardware pending.
- `mixer_slot_source`: RESEARCH_OPEN; no source write exposed/attempted.

RETAINED PARENT EVIDENCE
31 definitions / 829 instances; historical static/oracle 190 PASS / 639 EVAL_ONLY / 0 FAIL; historical dynamic 20 both-state / 12 single-state / 710 neverObserved / 0 FAIL; meter closure 14/46 (inputs 8/8, outputs 4/26, mixes 2/12); targeted Core 18/18 SKIP_BASELINE_UNKNOWN with zero writes/FAIL/restore quarantine.

EXACT IMMEDIATE NEXT ACTION
1. Keep existing 0.1.18 selected; do not recreate the connection and do not manually change mono/stereo/Mute/Solo.
2. Run `UPDATE_AND_RUN.bat`, stay on `testbench/meter-routing-exact-restore`. Required: dependencies PASS, Prettier PASS, ESLint PASS, manifest PASS, all Node tests PASS, package build PASS.
3. If green, no package re-import is required solely for these TestBench/docs changes because no module `src/` code changed after `d6df45c...`.
4. Pause YouTube/DAW playback and keep Monitor/speakers/headphones physically safe.
5. Run only `testbench\RUN_MIX_FEEDBACK_CLOSURE.cmd`, follow PAGE2_AUTO only if positively recognized, then `MIX_FEEDBACK` and `ALL_ISOLATED`; touch nothing in Focusrite Control during hardware.
6. Capture the complete bootstrap + closure result. If bootstrap returns NO-OP SAFE after exact restore, preserve that evidence and research official-client grouped/atomic semantics next; do not repeat blindly.

PERMANENT SAFETY
Scarlett 18i20 (3rd Gen) only. Monitor gain item 1677 read-only. No invented input preamp gain, direct per-input hardware mute, per-channel phantom, Mic Kill or physical Monitor level write. Dynamic Control Server port/device ID only. Server-confirmed feedback/state only. No unsafe raw writes, firmware/reset/restore/snapshot commands, or meter/status writes. No write to explicit UNKNOWN output availability. No Focusrite software/firmware update or unrelated routing change without explicit agreement. Preserve privacy and attribution.

Living-state rule: after every material software/hardware/user result or blocker, update both root `HANDOFF` and `docs/CURRENT_HANDOFF.md`. Pending work is never PASS.
