FOCUSRITE CONTROL / COMPANION — UNIVERSAL AI RESUME ENTRYPOINT

Trigger word: HANDOFF

MANDATORY STARTUP FRESHNESS GATE — REPO-WIDE RECENCY FIRST
When the user says only `HANDOFF`, do not resume from chat history, uploaded files, copied handoffs, an embedded SHA, or the default branch. Resume from the LIVE repository.

Before proposing code, hardware work, release work, branch changes, or asking the user to run anything:
1. Establish current date/time.
2. Inspect recent remote branch movement across the repository, not only `main`.
3. Identify the newest MATERIAL movements by commit time.
4. Choose the objective-owning branch using BOTH recency and relevance.
5. Resolve that branch's current remote HEAD and inspect newer commits/diff since the last validated checkpoint.
6. Read live `HANDOFF`, `AI_PROJECT_RULES.md`, `docs/CURRENT_HANDOFF.md`, `docs/PROTOCOL.md`, `docs/STATE_CONTRACT.md`, `docs/COLD_START_READBACK.md`, `docs/FEEDBACK_HARDWARE_CLOSURE_MATRIX.md`, and relevant current source/tests/evidence.
7. Reconcile any newer completed user/hardware result before choosing the next action; newer completed physical/human evidence outranks stale written state.
8. Only then choose the next action.

A default-branch commit search can miss work on another branch. An embedded SHA is context only and is never permission to skip live Git verification.

MANDATORY EVIDENCE / INFERENCE GATE — NEVER TURN UNKNOWN INTO UNSUPPORTED
Keep separate: OFFICIAL PRODUCT BEHAVIOUR / SCHEMA_PRESENT / SESSION_STATE_OBSERVED / IMPLEMENTED / HARDWARE_WRITE_CONFIRMED / HARDWARE_DYNAMIC_CLOSED.
`UNKNOWN`, blank, missing cache, `BASELINE_UNKNOWN`, `SKIP_BASELINE_UNKNOWN`, or sparse initial state means only not observed in this client session absent stronger evidence. It is not proof that a control is false, absent, unsupported or impossible.

PROJECT LAUNCHERS FIRST
- `UPDATE.bat` for normal sync.
- `UPDATE_AND_RUN.bat` for update + normal software gate.
- `RUN.bat` when already current.
- exact `testbench\RUN_*.cmd` launcher for targeted TestBench/hardware work.
- Manual shell/Git/PowerShell/Node is last resort only when the normal launcher is itself broken or cannot expose the required diagnostic.
- Do NOT rebuild a second workflow for behaviour already present in the repository.

IMMUTABLE OBJECTIVE CONTINUITY
Parent objective remains explicit hardware feedback closure across all 31 public feedback definitions/instances while material `EVAL_ONLY`, `MANUAL_PENDING`, `BASELINE_UNKNOWN`, `neverObserved`, unexercised or otherwise open rows remain. Closing one Mix/topology sub-question never closes the parent objective. Publication is not the current objective.

REMOTE DEVICES AUTHORIZATION — MANDATORY BEFORE ANY WRITE
- Focusrite Control → Device Settings → Remote Devices must show the existing `Companion Scarlett 18i20` client approved.
- Reuse the existing Companion Focusrite connection; do not delete/recreate it for testing.
- Missing approval is AUTHORIZATION/PREFLIGHT BLOCKED, not a hardware failure.
- No extra direct client and never copy/reuse the Companion private client key.

CURRENT LIVE OBJECTIVE
Branch: `testbench/meter-routing-exact-restore`.
Supported hardware claim: Scarlett 18i20 (3rd Gen) only.
Parent objective: explicit hardware feedback closure.
Canonical production candidate remains audited 0.1.16.
Research 0.1.17 is software validated, packaged and real-hardware exercised.
Research 0.1.18 module/package checkpoint: `d6df45c59ab825e1ebccae90d98212b561449feb`; the operator already imported/selected 0.1.18 on the EXISTING authorised Companion connection and enabled the mixer diagnostic variables.

LAST USER-HOST FULL SOFTWARE GATE — GREEN CHECKPOINT
`UPDATE_AND_RUN.bat` at source HEAD `e0a477d401b2` completed dependencies PASS, Prettier PASS, ESLint PASS, source manifest PASS, Node tests 230/230 PASS and package build PASS (`focusrite-scarlett-18i20-0.1.18.tgz`). No hardware write occurred.

LATEST HARDWARE ATTEMPT — NO-OP SAFE BEFORE WRITE
Immediately after that green gate, `RUN_MIX_FEEDBACK_CLOSURE.cmd` ran with 0.1.18 selected:
- targeted self-check 63/63 PASS;
- exact Scarlett 18i20 Gen3 / existing Companion connection / Remote Devices authorization PASS;
- Page 2 current with 768 audited controls;
- after `MIX_FEEDBACK` + `ALL_ISOLATED`, bootstrap stopped with `No unique adjacent confirmed-mono Playback pair is available for autonomous materialisation.`;
- hardware writes 0;
- no topology write, no Mix Mute/Solo write, no restore incident.

CORRECT INTERPRETATION OF THAT NO-OP
The old TestBench incorrectly required canonical Playback channels to occupy adjacent mixer-slot numbers. The result means ZERO candidate pairs satisfied that obsolete slot-adjacency rule. It does NOT prove mono/stereo writes fail and does NOT prove several pairs were ambiguous. Mixer slot numbers are presentation/storage positions; Playback channel pairing must be derived from runtime channel identity (`Playback 1` ↔ `Playback 2`), not `slot N` ↔ `slot N+1`.

TESTBENCH CORRECTION IMPLEMENTED — USER-HOST REVALIDATION PENDING
No production/module `src/` file changed after the validated 0.1.18 package checkpoint. Current changes are TestBench/tests/docs only.

Playback topology targeting now:
1. collects live `mixer_slot_N_source`, `source_name`, `stereo`;
2. pairs canonical channels by runtime names (`Playback 1` with `Playback 2`, etc.) even when their mixer-slot numbers are nonadjacent;
3. requires unique live channel identities, distinct slots, non-zero source IDs and server-confirmed mono/stereo state;
4. duplicate/ambiguous channel identities fail closed before write;
5. diagnostics print only sanitized slot/name/topology, never raw source IDs.

The autonomous topology path still performs only guarded paired `mixer_slot_stereo` explicit ON/OFF research actions, monitors source/name as collateral state, requires exact source/topology restore and never uses `mixer_slot_source`, raw writes, Monitor gain or a direct TCP client.

SECOND MATERIALISATION PATH NOW IN THE SAME LAUNCHER
The project already had a validated Companion pair-routing mechanism and V8 pair restore harness. `output_pair_source` is `Output: Route stereo pair`; it accepts paired physical destinations and paired sources including Mix L/R, writes both output members, and is guarded by the Scarlett 18i20 pair-aware hardware policy.

If Playback topology materialisation returns `NO-OP SAFE`, `RUN_MIX_FEEDBACK_CLOSURE.cmd` now automatically tries `MixOutputRoutingMaterialize.js` before giving up:
- discovers one unique server-observed `Mix A L` source; no raw ID is persisted/logged in the sanitized report;
- excludes Monitor Outputs 1-2 from automatic fallback;
- prioritizes Line Outputs 3-4 only when both members are AVAILABLE or have no availability flag, both original source values are exact and the existing V8 pair restore path exists;
- otherwise searches another eligible non-Monitor output pair;
- explicit UNKNOWN or UNAVAILABLE output availability receives no write;
- temporarily routes the selected output pair to Mix A using exactly one `output_pair_source` Companion action;
- requires server confirmation of Mix A L/R on that pair;
- always restores the exact original left/right source values through the existing V8 exact-restore helper before any fresh snapshot or closure continuation;
- restore not confirmed = HARD ABORT; Page 2 restore not confirmed = stop;
- after exact restore, captures a fresh Mix snapshot and continues only if an exact Mix baseline materialised;
- if neither topology nor routing materialises a baseline, exits `NO-OP SAFE` after verified restores rather than repeating blindly.

The fallback does NOT write mixer-slot source, Mix gain/Mute/Solo, single-channel output source, raw items, Monitor gain, firmware/reset/restore/snapshot, and does not create a direct Control Server client.

CURRENT TARGETED SELF-CHECK CONTRACT
`RUN_MIX_FEEDBACK_CLOSURE.cmd` now syntax-checks `MixOutputRoutingMaterialize.js` and includes `test/mix-output-routing-materialize.test.js` alongside the existing Mix/topology/preparation/safety tests. New regressions cover nonadjacent Playback 1/2 slots, ambiguity fail-closed, sanitized diagnostics, Line 3-4 priority, Monitor 1-2 exclusion, UNKNOWN availability no-write, exact baseline requirement, one `output_pair_source` route and forbidden-family regression.

LATEST USER-HOST REVALIDATION ATTEMPT — ESLINT-ONLY BLOCKER
At source HEAD `d73ec82da70d`, `UPDATE_AND_RUN.bat` completed dependency installation PASS and Prettier PASS, then stopped at ESLint with exactly one error in `testbench/MixOutputRoutingMaterialize.js`: the initial `outputRestored = false` assignment was never read before being overwritten. Source manifest, Node tests and package build did not run. No hardware write occurred.

The correction is intentionally non-functional: `outputRestored` is now declared without an initial value. In the `finally`, it is still assigned from `restored.restored` whenever routing was attempted, or explicitly `true` when no routing write was attempted. Exact restore/HARD ABORT semantics are unchanged. This correction is SOURCE_IMPLEMENTED but still requires a fresh complete user-host `UPDATE_AND_RUN.bat` gate before hardware.

LATEST STRONG MIX HARDWARE RESULT RETAINED FROM 0.1.17
Playback slot 3 / Playback 1 under the then-tested stereo topology:
- Mix A Left Mute: HARDWARE_DYNAMIC_CLOSED, server variable + rendered feedback false→true→false, exact restore.
- Mix A Left Solo: HARDWARE_DYNAMIC_CLOSED, same.
- Mix A Right Mute direct write: no transition under that stereo topology; exact baseline restored.
- Mix A Right Solo direct write: same.
- Mix B-F: 20 `SKIP_BASELINE_UNKNOWN`.
- restore quarantine 0; hardware restore YES; Page 2 restore YES.
Do not infer global Right-lane ownership/unwritability/unsupported from that stereo-only result.

RUNTIME MONO/STEREO EVIDENCE
Operator UI evidence shows runtime mono/stereo presentation for Software Playback and other source families where available. Current known operator state remains Playback 1 + Playback 2 mono unless newer live evidence says otherwise. Old SINGLE-ITEM `mixer_slot_stereo` no-effect evidence proves only those single-item attempts had no useful effect. `mixer_slot_stereo` and `mixer_slot_source` remain RESEARCH_OPEN / EVAL_ONLY; public/raw writes remain withheld.

CURRENT MIX STATUS
- `mix_mute`: PARTIAL.
- `mix_solo`: PARTIAL.
- `mixer_slot_stereo`: RESEARCH_OPEN; grouped/channel-pair semantics hardware pending.
- `mixer_slot_source`: RESEARCH_OPEN; no source write exposed/attempted.
- `output_pair_source`: existing pair-aware Companion path; temporary fallback use is implemented but this new materialisation strategy is hardware pending.

RETAINED PARENT EVIDENCE
31 definitions / 829 instances; historical static/oracle 190 PASS / 639 EVAL_ONLY / 0 FAIL; historical dynamic 20 both-state / 12 single-state / 710 neverObserved / 0 FAIL; meter closure 14/46 (inputs 8/8, outputs 4/26, mixes 2/12); targeted Core 18/18 SKIP_BASELINE_UNKNOWN with zero writes/FAIL/restore quarantine.

EXACT IMMEDIATE NEXT ACTION
1. Keep the existing authorised Companion Focusrite connection and selected 0.1.18. Do not recreate it and do not manually change mono/stereo, Mute, Solo, faders or routing.
2. Run `UPDATE_AND_RUN.bat` and stay on `testbench/meter-routing-exact-restore`.
3. Required before hardware: dependencies PASS, Prettier PASS, ESLint PASS, source manifest PASS, ALL Node tests PASS, package build PASS.
4. Because current changes after `d6df45c...` are TestBench/tests/docs only, do NOT re-import the 0.1.18 package solely for these changes if the gate is green.
5. Pause YouTube/DAW playback. Keep physical Monitor low, speakers muted/off if possible and headphones safe/removed.
6. Run only `testbench\RUN_MIX_FEEDBACK_CLOSURE.cmd`.
7. Use `PAGE2_AUTO` only if the launcher positively recognizes a stale Focusrite TestBench Page 2.
8. Confirm `MIX_FEEDBACK`, then `ALL_ISOLATED`, then touch nothing in Focusrite Control.
9. Capture/paste the complete output. The expected flow may be topology materialisation → exact restore → routing fallback if needed → exact restore → Mix closure if a baseline becomes available.
10. Any restore HARD ABORT = stop all further hardware testing until diagnosed. A two-path `NO-OP SAFE` is valid research evidence and must not be repeated blindly.

PERMANENT SAFETY
Scarlett 18i20 (3rd Gen) only. Monitor gain item 1677 read-only. No invented input preamp gain, direct per-input hardware mute, per-channel phantom, Mic Kill or physical Monitor level write. Dynamic Control Server port/device ID only. Server-confirmed feedback/state only. No unsafe raw writes, firmware/reset/restore/snapshot commands, or meter/status writes. No write to explicit UNKNOWN output availability. No Focusrite software/firmware update or unrelated routing change without explicit agreement. Preserve privacy and attribution.

Living-state rule: after every material software/hardware/user result or blocker, update both root `HANDOFF` and `docs/CURRENT_HANDOFF.md`. Pending work is never PASS.
